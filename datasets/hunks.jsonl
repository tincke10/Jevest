{"id":"zod-9446b5c-1","repo":"colinhacks/zod","license":"MIT","commit":"9446b5cc14c5bf137790f1f66abf602044871223","parent":"0c483c58849fdb6445aea4f54b1bac6b57ab3d22","file":"packages/zod/src/v4/classic/tests/prefault.test.ts","language":"typescript","hunk_header":"@@ -1,8 +1,78 @@","before":"import { expect, expectTypeOf, test } from \"vitest\";\nimport { z } from \"zod/v4\";\n\ntest(\"basic prefault\", () => {\n  const a = z.prefault(z.string().trim(), \"  default  \");\n  expect(a).toBeInstanceOf(z.ZodPrefault);\n  expect(a.parse(\"  asdf  \")).toEqual(\"asdf\");\n  expect(a.parse(undefined)).toEqual(\"default\");","after":"import { expect, expectTypeOf, test } from \"vitest\";\nimport { z } from \"zod/v4\";\n\ntest.each([false, true])(\"undefined prefault preserves object keys (jitless: %s)\", async (jitless) => {\n  const previous = z.config().jitless;\n  z.config({ jitless });\n  try {\n    let calls = 0;\n    const field = z.union([z.string(), z.undefined()]).prefault(() => {\n      calls++;\n      return undefined;\n    });\n    const schema = z.object({ a: field });\n    expectTypeOf<z.output<typeof field>>().toEqualTypeOf<string | undefined>();\n    expectTypeOf<z.input<typeof schema>>().toEqualTypeOf<{ a?: string | undefined }>();\n    expectTypeOf<z.output<typeof schema>>().toEqualTypeOf<{ a: string | undefined }>();\n    expect(schema.parse({})).toStrictEqual({ a: undefined });\n    expect(calls).toBe(1);\n    expect(schema.parse({ a: undefined })).toStrictEqual({ a: undefined });\n    expect(calls).toBe(2);\n    expect(await schema.parseAsync({})).toStrictEqual({ a: undefined });\n    expect(calls).toBe(3);\n    expect(schema.parse({ a: \"value\" })).toStrictEqual({ a: \"value\" });\n    expect(schema.safeParse({ a: 123 }).success).toBe(false);\n    expect(calls).toBe(3);\n\n    const literal = z.prefault(z.string().optional(), undefined);\n    expect(z.core.compileFn(z.object({ a: literal }))({})).toStrictEqual({ a: undefined });\n    for (const wrapped of [literal, literal.readonly(), literal.nullable(), z.lazy(() => literal)]) {\n      expect(z.object({ a: wrapped }).parse({})).toStrictEqual({ a: undefined });\n    }\n    expect(z.object({ a: literal.optional() }).parse({})).toStrictEqual({});\n    expect(z.object({ a: literal.optional() }).parse({ a: undefined })).toStrictEqual({ a: undefined });\n    expect(z.tuple([literal]).parse([])).toStrictEqual([undefined]);\n  } finally {\n    z.config({ jitless: previous });\n  }\n});\n\ntest(\"prefault preserves transformed undefined output\", async () => {\n  const field = z\n    .string()\n    .transform(() => undefined)\n    .prefault(\"fallback\");\n  expectTypeOf<z.output<typeof field>>().toEqualTypeOf<undefined>();\n  expect(field.parse(undefined)).toBeUndefined();\n  const schema = z.object({ a: field, optional: z.string().optional() });\n  expectTypeOf<z.output<typeof schema>>().toEqualTypeOf<{ a: undefined; optional?: string | undefined }>();\n  expect(schema.parse({})).toStrictEqual({ a: undefined });\n  expect(z.core.compileFn(schema)({})).toStrictEqual({ a: undefined });\n  expect(z.compile(schema).parse({})).toStrictEqual({ a: undefined });\n  expect(\n    await z\n      .object({\n        a: z\n          .string()\n          .transform(async () => undefined)\n          .prefault(\"fallback\"),\n      })\n      .parseAsync({})\n  ).toStrictEqual({ a: undefined });\n  expect(\n    z\n      .object({\n        a: z\n          .string()\n          .prefault(\"fallback\")\n          .transform(() => undefined),\n      })\n      .parse({})\n  ).toStrictEqual({ a: undefined });\n});\n\ntest(\"basic prefault\", () => {\n  const a = z.prefault(z.string().trim(), \"  default  \");\n  expect(a).toBeInstanceOf(z.ZodPrefault);\n  expect(a.parse(\"  asdf  \")).toEqual(\"asdf\");\n  expect(a.parse(undefined)).toEqual(\"default\");","diff":"@@ -1,8 +1,78 @@\n import { expect, expectTypeOf, test } from \"vitest\";\n import { z } from \"zod/v4\";\n \n+test.each([false, true])(\"undefined prefault preserves object keys (jitless: %s)\", async (jitless) => {\n+  const previous = z.config().jitless;\n+  z.config({ jitless });\n+  try {\n+    let calls = 0;\n+    const field = z.union([z.string(), z.undefined()]).prefault(() => {\n+      calls++;\n+      return undefined;\n+    });\n+    const schema = z.object({ a: field });\n+    expectTypeOf<z.output<typeof field>>().toEqualTypeOf<string | undefined>();\n+    expectTypeOf<z.input<typeof schema>>().toEqualTypeOf<{ a?: string | undefined }>();\n+    expectTypeOf<z.output<typeof schema>>().toEqualTypeOf<{ a: string | undefined }>();\n+    expect(schema.parse({})).toStrictEqual({ a: undefined });\n+    expect(calls).toBe(1);\n+    expect(schema.parse({ a: undefined })).toStrictEqual({ a: undefined });\n+    expect(calls).toBe(2);\n+    expect(await schema.parseAsync({})).toStrictEqual({ a: undefined });\n+    expect(calls).toBe(3);\n+    expect(schema.parse({ a: \"value\" })).toStrictEqual({ a: \"value\" });\n+    expect(schema.safeParse({ a: 123 }).success).toBe(false);\n+    expect(calls).toBe(3);\n+\n+    const literal = z.prefault(z.string().optional(), undefined);\n+    expect(z.core.compileFn(z.object({ a: literal }))({})).toStrictEqual({ a: undefined });\n+    for (const wrapped of [literal, literal.readonly(), literal.nullable(), z.lazy(() => literal)]) {\n+      expect(z.object({ a: wrapped }).parse({})).toStrictEqual({ a: undefined });\n+    }\n+    expect(z.object({ a: literal.optional() }).parse({})).toStrictEqual({});\n+    expect(z.object({ a: literal.optional() }).parse({ a: undefined })).toStrictEqual({ a: undefined });\n+    expect(z.tuple([literal]).parse([])).toStrictEqual([undefined]);\n+  } finally {\n+    z.config({ jitless: previous });\n+  }\n+});\n+\n+test(\"prefault preserves transformed undefined output\", async () => {\n+  const field = z\n+    .string()\n+    .transform(() => undefined)\n+    .prefault(\"fallback\");\n+  expectTypeOf<z.output<typeof field>>().toEqualTypeOf<undefined>();\n+  expect(field.parse(undefined)).toBeUndefined();\n+  const schema = z.object({ a: field, optional: z.string().optional() });\n+  expectTypeOf<z.output<typeof schema>>().toEqualTypeOf<{ a: undefined; optional?: string | undefined }>();\n+  expect(schema.parse({})).toStrictEqual({ a: undefined });\n+  expect(z.core.compileFn(schema)({})).toStrictEqual({ a: undefined });\n+  expect(z.compile(schema).parse({})).toStrictEqual({ a: undefined });\n+  expect(\n+    await z\n+      .object({\n+        a: z\n+          .string()\n+          .transform(async () => undefined)\n+          .prefault(\"fallback\"),\n+      })\n+      .parseAsync({})\n+  ).toStrictEqual({ a: undefined });\n+  expect(\n+    z\n+      .object({\n+        a: z\n+          .string()\n+          .prefault(\"fallback\")\n+          .transform(() => undefined),\n+      })\n+      .parse({})\n+  ).toStrictEqual({ a: undefined });\n+});\n+\n test(\"basic prefault\", () => {\n   const a = z.prefault(z.string().trim(), \"  default  \");\n   expect(a).toBeInstanceOf(z.ZodPrefault);\n   expect(a.parse(\"  asdf  \")).toEqual(\"asdf\");\n   expect(a.parse(undefined)).toEqual(\"default\");","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix: preserve undefined prefault outputs and object keys (#6587)","issue_url":"https://github.com/colinhacks/zod/issues/6585","pr_url":"https://github.com/colinhacks/zod/pull/6587"},"needs_manual_review":true}
{"id":"zod-b12aa52-1","repo":"colinhacks/zod","license":"MIT","commit":"b12aa523e7e2617c4296cccf9b24d6558ed23e95","parent":"dd9c36fa2ddcee79f71d19f9635586c7b65ead86","file":"packages/zod/src/v4/classic/tests/discriminated-unions.test.ts","language":"typescript","hunk_header":"@@ -738,10 +738,30 @@ test(\"encode with codec discriminator\", () => {","before":"  // encode (backward) should also work — the discriminator values differ between forward (1, 2) and backward (\"one\", \"two\") directions\n  const encoded = z.encode(schema, { type: \"one\", value: \"hello\" });\n  expect(encoded).toEqual({ type: 1, value: \"hello\" });\n});\n\ntest(\"getDiscriminatedOption\", () => {\n  const fruit = z.object({ type: z.literal(\"fruit\"), seeds: z.boolean() });\n  const veg = z.object({ type: z.literal(\"vegetable\"), leafy: z.boolean() });\n  const schema = z.discriminatedUnion(\"type\", [fruit, veg]);\n","after":"  // encode (backward) should also work — the discriminator values differ between forward (1, 2) and backward (\"one\", \"two\") directions\n  const encoded = z.encode(schema, { type: \"one\", value: \"hello\" });\n  expect(encoded).toEqual({ type: 1, value: \"hello\" });\n});\n\ntest(\"nested encoding does not select a sibling from forward discriminator values\", async () => {\n  const tag = (value: \"a\" | \"b\") =>\n    z.codec(z.literal(value).default(value), z.undefined(), {\n      decode: () => undefined,\n      encode: () => value,\n    });\n  const inner = z.discriminatedUnion(\"type\", [\n    z.object({ type: tag(\"a\"), value: z.literal(\"a\") }),\n    z.object({ type: tag(\"b\"), value: z.literal(\"b\") }),\n  ]);\n  const outer = z.discriminatedUnion(\"type\", [inner, z.object({ type: z.undefined(), value: z.string() })]);\n  for (const value of [\"a\", \"b\"] as const) {\n    const input = { type: undefined, value };\n    const expected = { type: value, value };\n    expect(z.encode(inner, input)).toEqual(expected);\n    expect(z.encode(outer, input)).toEqual(expected);\n    expect(await z.encodeAsync(outer, input)).toEqual(expected);\n  }\n});\n\ntest(\"getDiscriminatedOption\", () => {\n  const fruit = z.object({ type: z.literal(\"fruit\"), seeds: z.boolean() });\n  const veg = z.object({ type: z.literal(\"vegetable\"), leafy: z.boolean() });\n  const schema = z.discriminatedUnion(\"type\", [fruit, veg]);\n","diff":"@@ -738,10 +738,30 @@ test(\"encode with codec discriminator\", () => {\n   // encode (backward) should also work — the discriminator values differ between forward (1, 2) and backward (\"one\", \"two\") directions\n   const encoded = z.encode(schema, { type: \"one\", value: \"hello\" });\n   expect(encoded).toEqual({ type: 1, value: \"hello\" });\n });\n \n+test(\"nested encoding does not select a sibling from forward discriminator values\", async () => {\n+  const tag = (value: \"a\" | \"b\") =>\n+    z.codec(z.literal(value).default(value), z.undefined(), {\n+      decode: () => undefined,\n+      encode: () => value,\n+    });\n+  const inner = z.discriminatedUnion(\"type\", [\n+    z.object({ type: tag(\"a\"), value: z.literal(\"a\") }),\n+    z.object({ type: tag(\"b\"), value: z.literal(\"b\") }),\n+  ]);\n+  const outer = z.discriminatedUnion(\"type\", [inner, z.object({ type: z.undefined(), value: z.string() })]);\n+  for (const value of [\"a\", \"b\"] as const) {\n+    const input = { type: undefined, value };\n+    const expected = { type: value, value };\n+    expect(z.encode(inner, input)).toEqual(expected);\n+    expect(z.encode(outer, input)).toEqual(expected);\n+    expect(await z.encodeAsync(outer, input)).toEqual(expected);\n+  }\n+});\n+\n test(\"getDiscriminatedOption\", () => {\n   const fruit = z.object({ type: z.literal(\"fruit\"), seeds: z.boolean() });\n   const veg = z.object({ type: z.literal(\"vegetable\"), leafy: z.boolean() });\n   const schema = z.discriminatedUnion(\"type\", [fruit, veg]);\n ","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix: preserve unique tags with defaulted discriminators (#6582)","issue_url":"https://github.com/colinhacks/zod/issues/6577","pr_url":"https://github.com/colinhacks/zod/pull/6582"},"needs_manual_review":true}
{"id":"zod-574d480-1","repo":"colinhacks/zod","license":"MIT","commit":"574d480fb443d3fd396efbcb8230f3dfee4a2cd2","parent":"c532d766de54e13aea5d03698980bf7d976fc544","file":"packages/zod/src/v4/core/tests/locales/tg.test.ts","language":"typescript","hunk_header":"@@ -65,11 +65,11 @@ test(\"locales - tg\", () => {","before":"    [z.stringFormat(\"custom\", () => false), \"xy\", \"custom-и нодуруст\"],\n    [z.union([z.string(), z.number()]), null, \"Вуруди нодуруст\"],\n    [\n      z.discriminatedUnion(\"kind\", [z.object({ kind: z.literal(\"a\") }), z.object({ kind: z.literal(\"b\") })]),\n      { kind: \"c\" },\n      \"Қимати дискриминатори нодуруст: 'a' | 'b' интизор мерафт\",\n    ],\n    [z.record(z.string().min(2), z.number()), { a: 1 }, \"Калиди нодуруст дар record\"],\n    [z.map(z.string(), z.number()), new Map([[{}, 1]]), \"Калиди нодуруст дар map\"],\n    [z.map(z.object({}), z.number()), new Map([[{}, \"a\"]]), \"Қимати нодуруст дар map\"],\n    [z.custom(() => false), \"a\", \"Вуруди нодуруст\"],","after":"    [z.stringFormat(\"custom\", () => false), \"xy\", \"custom-и нодуруст\"],\n    [z.union([z.string(), z.number()]), null, \"Вуруди нодуруст\"],\n    [\n      z.discriminatedUnion(\"kind\", [z.object({ kind: z.literal(\"a\") }), z.object({ kind: z.literal(\"b\") })]),\n      { kind: \"c\" },\n      \"Қимати нодурусти дискриминатор: 'a' | 'b' интизор мерафт\",\n    ],\n    [z.record(z.string().min(2), z.number()), { a: 1 }, \"Калиди нодуруст дар record\"],\n    [z.map(z.string(), z.number()), new Map([[{}, 1]]), \"Калиди нодуруст дар map\"],\n    [z.map(z.object({}), z.number()), new Map([[{}, \"a\"]]), \"Қимати нодуруст дар map\"],\n    [z.custom(() => false), \"a\", \"Вуруди нодуруст\"],","diff":"@@ -65,11 +65,11 @@ test(\"locales - tg\", () => {\n     [z.stringFormat(\"custom\", () => false), \"xy\", \"custom-и нодуруст\"],\n     [z.union([z.string(), z.number()]), null, \"Вуруди нодуруст\"],\n     [\n       z.discriminatedUnion(\"kind\", [z.object({ kind: z.literal(\"a\") }), z.object({ kind: z.literal(\"b\") })]),\n       { kind: \"c\" },\n-      \"Қимати дискриминатори нодуруст: 'a' | 'b' интизор мерафт\",\n+      \"Қимати нодурусти дискриминатор: 'a' | 'b' интизор мерафт\",\n     ],\n     [z.record(z.string().min(2), z.number()), { a: 1 }, \"Калиди нодуруст дар record\"],\n     [z.map(z.string(), z.number()), new Map([[{}, 1]]), \"Калиди нодуруст дар map\"],\n     [z.map(z.object({}), z.number()), new Map([[{}, \"a\"]]), \"Қимати нодуруст дар map\"],\n     [z.custom(() => false), \"a\", \"Вуруди нодуруст\"],","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix(locales): clarify Tajik discriminator value message","issue_url":"https://github.com/colinhacks/zod/issues/6578","pr_url":"https://github.com/colinhacks/zod/pull/6581"},"needs_manual_review":true}
{"id":"zod-36f1796-1","repo":"colinhacks/zod","license":"MIT","commit":"36f17960d1defca5d0896d9424f4e1059fbbf081","parent":"22bed613281143efa3815fe26ab666c6c56586af","file":"packages/zod/src/v4/classic/tests/cyclic-data.test.ts","language":"typescript","hunk_header":"@@ -958,5 +960,35 @@ test(\"parses a factory-built recursive schema through z.lazy\", () => {","before":"    });\n\n  const out = z.object({ root: Node() }).parse({ root: { id: 1, child: { id: 2, child: undefined } } });\n  expect(out.root.child.id).toBe(2);\n});","after":"    });\n\n  const out = z.object({ root: Node() }).parse({ root: { id: 1, child: { id: 2, child: undefined } } });\n  expect(out.root.child.id).toBe(2);\n});\n\ntest(\"a finished parse pins nothing on the schema\", async () => {\n  // es2020 is the target, and its lib predates WeakRef\n  type Weak<T extends object> = { deref(): T | undefined };\n  const { WeakRef: Weak } = globalThis as unknown as { WeakRef: new <T extends object>(target: T) => Weak<T> };\n  // vitest carries no --expose-gc, so reach the collector the way node's own tests do\n  v8.setFlagsFromString(\"--expose-gc\");\n  const gc = vm.runInNewContext(\"gc\") as () => void;\n\n  const Node: any = z.object({\n    id: z.number(),\n    get next() {\n      return z.optional(Node);\n    },\n  });\n\n  const ref = ((): Weak<object> => {\n    const input = { id: 1, next: { id: 2, next: undefined } };\n    Node.parse(input);\n    return new Weak(input);\n  })();\n\n  for (let attempt = 0; attempt < 10 && ref.deref(); attempt++) {\n    // the input stays on the stack until a macrotask boundary\n    await new Promise((resolve) => setTimeout(resolve, 0));\n    gc();\n  }\n\n  expect(ref.deref()).toBeUndefined();\n});","diff":"@@ -958,5 +960,35 @@ test(\"parses a factory-built recursive schema through z.lazy\", () => {\n     });\n \n   const out = z.object({ root: Node() }).parse({ root: { id: 1, child: { id: 2, child: undefined } } });\n   expect(out.root.child.id).toBe(2);\n });\n+\n+test(\"a finished parse pins nothing on the schema\", async () => {\n+  // es2020 is the target, and its lib predates WeakRef\n+  type Weak<T extends object> = { deref(): T | undefined };\n+  const { WeakRef: Weak } = globalThis as unknown as { WeakRef: new <T extends object>(target: T) => Weak<T> };\n+  // vitest carries no --expose-gc, so reach the collector the way node's own tests do\n+  v8.setFlagsFromString(\"--expose-gc\");\n+  const gc = vm.runInNewContext(\"gc\") as () => void;\n+\n+  const Node: any = z.object({\n+    id: z.number(),\n+    get next() {\n+      return z.optional(Node);\n+    },\n+  });\n+\n+  const ref = ((): Weak<object> => {\n+    const input = { id: 1, next: { id: 2, next: undefined } };\n+    Node.parse(input);\n+    return new Weak(input);\n+  })();\n+\n+  for (let attempt = 0; attempt < 10 && ref.deref(); attempt++) {\n+    // the input stays on the stack until a macrotask boundary\n+    await new Promise((resolve) => setTimeout(resolve, 0));\n+    gc();\n+  }\n+\n+  expect(ref.deref()).toBeUndefined();\n+});","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix(v4): stop the memoizer from pinning a finished parse (#6572)","issue_url":"https://github.com/colinhacks/zod/issues/6557","pr_url":"https://github.com/colinhacks/zod/pull/6572"},"needs_manual_review":true}
{"id":"zod-dcbcf05-1","repo":"colinhacks/zod","license":"MIT","commit":"dcbcf0529ca1e5424541d5d4b58e0a7b79507f51","parent":"277613a61a11f299d9897ddd88490002a995f746","file":"packages/zod/src/v4/core/doc.ts","language":"typescript","hunk_header":"@@ -10,14 +10,18 @@ export class Doc {","before":"  constructor(args: string[] = [], closed: Record<string, unknown> = {}) {\n    this.args = args;\n    this.closed = closed;\n  }\n\n  indented(fn: (doc: Doc) => void) {\n    this.indent += 1;\n    fn(this);\n    this.indent -= 1;\n  }\n\n  write(fn: ModeWriter): void;\n  write(line: string): void;\n  write(arg: any) {","after":"  constructor(args: string[] = [], closed: Record<string, unknown> = {}) {\n    this.args = args;\n    this.closed = closed;\n  }\n\n  // the compiler catches a child's throw and keeps writing into this doc, so the indent has to unwind with it\n  indented(fn: (doc: Doc) => void) {\n    this.indent += 1;\n    try {\n      fn(this);\n    } finally {\n      this.indent -= 1;\n    }\n  }\n\n  write(fn: ModeWriter): void;\n  write(line: string): void;\n  write(arg: any) {","diff":"@@ -10,14 +10,18 @@ export class Doc {\n   constructor(args: string[] = [], closed: Record<string, unknown> = {}) {\n     this.args = args;\n     this.closed = closed;\n   }\n \n+  // the compiler catches a child's throw and keeps writing into this doc, so the indent has to unwind with it\n   indented(fn: (doc: Doc) => void) {\n     this.indent += 1;\n-    fn(this);\n-    this.indent -= 1;\n+    try {\n+      fn(this);\n+    } finally {\n+      this.indent -= 1;\n+    }\n   }\n \n   write(fn: ModeWriter): void;\n   write(line: string): void;\n   write(arg: any) {","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix(compile): unwind the doc indent when a child generator throws (#6570)","issue_url":"https://github.com/colinhacks/zod/issues/6561","pr_url":"https://github.com/colinhacks/zod/pull/6570"},"needs_manual_review":true}
{"id":"zod-7a00236-1","repo":"colinhacks/zod","license":"MIT","commit":"7a00236683c79000dbab0d92f6faf0b7fba39f59","parent":"5489a532e65ee3f5ab0959ff8a5da346ced96f2a","file":"packages/zod/src/v4/classic/tests/bigint.test.ts","language":"typescript","hunk_header":"@@ -51,10 +51,16 @@ test(\"min max getters\", () => {","before":"\n  expect(z.bigint().max(BigInt(5)).maxValue).toEqual(BigInt(5));\n  expect(z.bigint().max(BigInt(5)).max(BigInt(1)).maxValue).toEqual(BigInt(1));\n});\n\ntest(\"bigint formats are distinct at the type level\", () => {\n  expectTypeOf(z.int64()._zod.def.format).toEqualTypeOf<\"int64\">();\n  expectTypeOf(z.uint64()._zod.def.format).toEqualTypeOf<\"uint64\">();\n\n  z.int64() satisfies z.ZodBigIntFormat;","after":"\n  expect(z.bigint().max(BigInt(5)).maxValue).toEqual(BigInt(5));\n  expect(z.bigint().max(BigInt(5)).max(BigInt(1)).maxValue).toEqual(BigInt(1));\n});\n\ntest(\"a format check applied after min/max does not widen minValue/maxValue back out\", () => {\n  const schema = z.bigint().min(0n).max(23n).check(z.int64());\n  expect(schema.minValue).toEqual(0n);\n  expect(schema.maxValue).toEqual(23n);\n});\n\ntest(\"bigint formats are distinct at the type level\", () => {\n  expectTypeOf(z.int64()._zod.def.format).toEqualTypeOf<\"int64\">();\n  expectTypeOf(z.uint64()._zod.def.format).toEqualTypeOf<\"uint64\">();\n\n  z.int64() satisfies z.ZodBigIntFormat;","diff":"@@ -51,10 +51,16 @@ test(\"min max getters\", () => {\n \n   expect(z.bigint().max(BigInt(5)).maxValue).toEqual(BigInt(5));\n   expect(z.bigint().max(BigInt(5)).max(BigInt(1)).maxValue).toEqual(BigInt(1));\n });\n \n+test(\"a format check applied after min/max does not widen minValue/maxValue back out\", () => {\n+  const schema = z.bigint().min(0n).max(23n).check(z.int64());\n+  expect(schema.minValue).toEqual(0n);\n+  expect(schema.maxValue).toEqual(23n);\n+});\n+\n test(\"bigint formats are distinct at the type level\", () => {\n   expectTypeOf(z.int64()._zod.def.format).toEqualTypeOf<\"int64\">();\n   expectTypeOf(z.uint64()._zod.def.format).toEqualTypeOf<\"uint64\">();\n \n   z.int64() satisfies z.ZodBigIntFormat;","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix(v4): don't let format checks overwrite tighter min/max bounds (#6553)","issue_url":"https://github.com/colinhacks/zod/issues/6550","pr_url":"https://github.com/colinhacks/zod/pull/6553"},"needs_manual_review":true}
{"id":"zod-cafbee4-1","repo":"colinhacks/zod","license":"MIT","commit":"cafbee4772ef481611fef6f5fb9ed9a1b9597069","parent":"4d7308828b1a56768741d7cfd6cf476eaead45af","file":"packages/zod/src/v4/classic/schemas.ts","language":"typescript","hunk_header":"@@ -1633,23 +1633,24 @@ export const ZodObject: core.$constructor<ZodObject> = /*@__PURE__*/ core.$const","before":"  {\n    keyof() {\n      return _enum(Object.keys(this._zod.def.shape));\n    },\n    catchall(catchall) {\n      return this.clone({ ...this._zod.def, catchall: catchall as any });\n    },\n    passthrough() {\n      return this.clone({ ...this._zod.def, catchall: unknown() });\n    },\n    loose() {\n      return this.clone({ ...this._zod.def, catchall: unknown() });\n    },\n    strict() {\n      return this.clone({ ...this._zod.def, catchall: never() });\n    },\n    strip() {\n      return this.clone({ ...this._zod.def, catchall: undefined });\n    },\n    extend(incoming) {\n      return util.extend(this, incoming);\n    },\n    safeExtend(incoming) {","after":"  {\n    keyof() {\n      return _enum(Object.keys(this._zod.def.shape));\n    },\n    catchall(catchall) {\n      // `mergeDefs` rather than a spread: spreading reads `shape`, and resolving it can mint a whole fresh subtree\n      return this.clone(util.mergeDefs(this._zod.def, { catchall: catchall as any }));\n    },\n    passthrough() {\n      return this.clone(util.mergeDefs(this._zod.def, { catchall: unknown() }));\n    },\n    loose() {\n      return this.clone(util.mergeDefs(this._zod.def, { catchall: unknown() }));\n    },\n    strict() {\n      return this.clone(util.mergeDefs(this._zod.def, { catchall: never() }));\n    },\n    strip() {\n      return this.clone(util.mergeDefs(this._zod.def, { catchall: undefined }));\n    },\n    extend(incoming) {\n      return util.extend(this, incoming);\n    },\n    safeExtend(incoming) {","diff":"@@ -1633,23 +1633,24 @@ export const ZodObject: core.$constructor<ZodObject> = /*@__PURE__*/ core.$const\n   {\n     keyof() {\n       return _enum(Object.keys(this._zod.def.shape));\n     },\n     catchall(catchall) {\n-      return this.clone({ ...this._zod.def, catchall: catchall as any });\n+      // `mergeDefs` rather than a spread: spreading reads `shape`, and resolving it can mint a whole fresh subtree\n+      return this.clone(util.mergeDefs(this._zod.def, { catchall: catchall as any }));\n     },\n     passthrough() {\n-      return this.clone({ ...this._zod.def, catchall: unknown() });\n+      return this.clone(util.mergeDefs(this._zod.def, { catchall: unknown() }));\n     },\n     loose() {\n-      return this.clone({ ...this._zod.def, catchall: unknown() });\n+      return this.clone(util.mergeDefs(this._zod.def, { catchall: unknown() }));\n     },\n     strict() {\n-      return this.clone({ ...this._zod.def, catchall: never() });\n+      return this.clone(util.mergeDefs(this._zod.def, { catchall: never() }));\n     },\n     strip() {\n-      return this.clone({ ...this._zod.def, catchall: undefined });\n+      return this.clone(util.mergeDefs(this._zod.def, { catchall: undefined }));\n     },\n     extend(incoming) {\n       return util.extend(this, incoming);\n     },\n     safeExtend(incoming) {","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix(v4): parse recursive schemas built by a factory (#6530)","issue_url":"https://github.com/colinhacks/zod/issues/6526","pr_url":"https://github.com/colinhacks/zod/pull/6530"},"needs_manual_review":true}
{"id":"zod-eca9687-1","repo":"colinhacks/zod","license":"MIT","commit":"eca96871536603076bdd00b8779b947144133aa4","parent":"81ded9918fe39d51b232be72f23ba0d5fb6a837e","file":"packages/zod/src/v4/classic/from-json-schema.ts","language":"typescript","hunk_header":"@@ -517,24 +708,29 @@ function convertBaseSchema(schema: JSONSchema.JSONSchema, ctx: ConversionContext","before":"          zodSchema = objectSchema.passthrough();\n        }\n      }\n\n      // propertyNames constrains key *names* only, and says nothing about which keys are required or how their values validate. Layering it on top of the result keeps properties/patternProperties/additionalProperties composing underneath. `true` allows every name, so it needs no guard.\n      if (schema.propertyNames !== undefined && schema.propertyNames !== true) {\n        // Keys are always strings, so a propertyNames subschema that omits `type` still constrains them — without this it would convert to z.any().\n        const keyJSONSchema =\n          typeof schema.propertyNames === \"object\" && schema.propertyNames.type === undefined\n            ? { type: \"string\", ...schema.propertyNames }\n            : schema.propertyNames;\n        zodSchema = checkPropertyNames(zodSchema, convertSchema(keyJSONSchema as JSONSchema.JSONSchema, ctx));\n      }\n      break;\n    }\n\n    case \"array\": {\n      // TODO: uniqueItems and contains/minContains/maxContains are not supported\n\n      // Check if this is a tuple (prefixItems or items as array)\n      const prefixItems = schema.prefixItems;\n      const items = schema.items;\n\n      if (prefixItems && Array.isArray(prefixItems)) {","after":"          zodSchema = objectSchema.passthrough();\n        }\n      }\n\n      // propertyNames constrains key *names* only, and says nothing about which keys are required or how their values validate. Layering it on top of the result keeps properties/patternProperties/additionalProperties composing underneath. `true` allows every name, so it needs no guard.\n      const hasKeyGuard = schema.propertyNames !== undefined && schema.propertyNames !== true;\n      const minProperties = typeof schema.minProperties === \"number\" ? schema.minProperties : undefined;\n      const maxProperties = typeof schema.maxProperties === \"number\" ? schema.maxProperties : undefined;\n      if (hasKeyGuard || minProperties !== undefined || maxProperties !== undefined) {\n        let keySchema: ZodType | undefined;\n        if (hasKeyGuard) {\n          // Keys are always strings, so a propertyNames subschema that omits `type` still constrains them — without this it would convert to z.any().\n          const keyJSONSchema =\n            typeof schema.propertyNames === \"object\" && schema.propertyNames.type === undefined\n              ? { type: \"string\", ...schema.propertyNames }\n              : schema.propertyNames;\n          keySchema = convertSchema(keyJSONSchema as JSONSchema.JSONSchema, ctx);\n        }\n        zodSchema = checkObjectGuards(zodSchema, { keySchema, minProperties, maxProperties });\n      }\n      break;\n    }\n\n    case \"array\": {\n      // Check if this is a tuple (prefixItems or items as array)\n      const prefixItems = schema.prefixItems;\n      const items = schema.items;\n\n      if (prefixItems && Array.isArray(prefixItems)) {","diff":"@@ -517,24 +708,29 @@ function convertBaseSchema(schema: JSONSchema.JSONSchema, ctx: ConversionContext\n           zodSchema = objectSchema.passthrough();\n         }\n       }\n \n       // propertyNames constrains key *names* only, and says nothing about which keys are required or how their values validate. Layering it on top of the result keeps properties/patternProperties/additionalProperties composing underneath. `true` allows every name, so it needs no guard.\n-      if (schema.propertyNames !== undefined && schema.propertyNames !== true) {\n-        // Keys are always strings, so a propertyNames subschema that omits `type` still constrains them — without this it would convert to z.any().\n-        const keyJSONSchema =\n-          typeof schema.propertyNames === \"object\" && schema.propertyNames.type === undefined\n-            ? { type: \"string\", ...schema.propertyNames }\n-            : schema.propertyNames;\n-        zodSchema = checkPropertyNames(zodSchema, convertSchema(keyJSONSchema as JSONSchema.JSONSchema, ctx));\n+      const hasKeyGuard = schema.propertyNames !== undefined && schema.propertyNames !== true;\n+      const minProperties = typeof schema.minProperties === \"number\" ? schema.minProperties : undefined;\n+      const maxProperties = typeof schema.maxProperties === \"number\" ? schema.maxProperties : undefined;\n+      if (hasKeyGuard || minProperties !== undefined || maxProperties !== undefined) {\n+        let keySchema: ZodType | undefined;\n+        if (hasKeyGuard) {\n+          // Keys are always strings, so a propertyNames subschema that omits `type` still constrains them — without this it would convert to z.any().\n+          const keyJSONSchema =\n+            typeof schema.propertyNames === \"object\" && schema.propertyNames.type === undefined\n+              ? { type: \"string\", ...schema.propertyNames }\n+              : schema.propertyNames;\n+          keySchema = convertSchema(keyJSONSchema as JSONSchema.JSONSchema, ctx);\n+        }\n+        zodSchema = checkObjectGuards(zodSchema, { keySchema, minProperties, maxProperties });\n       }\n       break;\n     }\n \n     case \"array\": {\n-      // TODO: uniqueItems and contains/minContains/maxContains are not supported\n-\n       // Check if this is a tuple (prefixItems or items as array)\n       const prefixItems = schema.prefixItems;\n       const items = schema.items;\n \n       if (prefixItems && Array.isArray(prefixItems)) {","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix(v4): enforce the six JSON Schema keywords fromJSONSchema silently dropped (#6535)","issue_url":"https://github.com/colinhacks/zod/issues/6463","pr_url":"https://github.com/colinhacks/zod/pull/6535"},"needs_manual_review":true}
{"id":"zod-f83ab51-1","repo":"colinhacks/zod","license":"MIT","commit":"f83ab51129038a2f868ee11dda906ed78ae2e478","parent":"74f9a6d3f2818f24e6df5394b8b6e60acb176463","file":"packages/zod/src/v4/classic/tests/continuability.test.ts","language":"typescript","hunk_header":"@@ -228,11 +228,11 @@ test(\"continuability\", () => {","before":"        \"code\": \"invalid_format\",\n        \"format\": \"emoji\",\n        \"message\": \"Invalid emoji\",\n        \"origin\": \"string\",\n        \"path\": [],\n        \"pattern\": \"/^[\\\\p{Extended_Pictographic}\\\\p{Emoji_Component}]+$/u\",\n      },\n      {\n        \"code\": \"custom\",\n        \"message\": \"Invalid input\",\n        \"path\": [],","after":"        \"code\": \"invalid_format\",\n        \"format\": \"emoji\",\n        \"message\": \"Invalid emoji\",\n        \"origin\": \"string\",\n        \"path\": [],\n        \"pattern\": \"/^(?=[\\\\s\\\\S]*[\\\\p{Extended_Pictographic}\\\\p{Regional_Indicator}\\\\u20E3])[\\\\p{Extended_Pictographic}\\\\p{Emoji_Component}]+$/u\",\n      },\n      {\n        \"code\": \"custom\",\n        \"message\": \"Invalid input\",\n        \"path\": [],","diff":"@@ -228,11 +228,11 @@ test(\"continuability\", () => {\n         \"code\": \"invalid_format\",\n         \"format\": \"emoji\",\n         \"message\": \"Invalid emoji\",\n         \"origin\": \"string\",\n         \"path\": [],\n-        \"pattern\": \"/^[\\\\p{Extended_Pictographic}\\\\p{Emoji_Component}]+$/u\",\n+        \"pattern\": \"/^(?=[\\\\s\\\\S]*[\\\\p{Extended_Pictographic}\\\\p{Regional_Indicator}\\\\u20E3])[\\\\p{Extended_Pictographic}\\\\p{Emoji_Component}]+$/u\",\n       },\n       {\n         \"code\": \"custom\",\n         \"message\": \"Invalid input\",\n         \"path\": [],","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix(v4): reject component-only strings from z.emoji() (#6532)","issue_url":"https://github.com/colinhacks/zod/issues/6515","pr_url":"https://github.com/colinhacks/zod/pull/6532"},"needs_manual_review":true}
{"id":"zod-84e416f-1","repo":"colinhacks/zod","license":"MIT","commit":"84e416fbf4740527bbc8f319634f4e1b065bb42c","parent":"1a16102a494b03ce1df7b80b663ae2df465f419e","file":"packages/zod/src/v4/classic/tests/cyclic-data.test.ts","language":"typescript","hunk_header":"@@ -674,5 +674,47 @@ test(\"guards a transform built before any container\", () => {","before":"  const input: any = { name: \"x\" };\n  input.self = input;\n\n  expect(() => Wrapped.parse(input)).toThrow(/reference cycle/);\n});","after":"  const input: any = { name: \"x\" };\n  input.self = input;\n\n  expect(() => Wrapped.parse(input)).toThrow(/reference cycle/);\n});\n\ntest(\"detects a cycle reachable only through a merged catchall\", () => {\n  const Cyclic: any = z.object({\n    id: z.string(),\n    get next() {\n      return z.optional(Root);\n    },\n  });\n  const Root: any = z.object({ tag: z.string() }).merge(z.object({}).catchall(Cyclic));\n\n  const input: any = { tag: \"root\" };\n  input.child = { id: \"1\", next: input };\n\n  const out: any = Root.parse(input);\n  expect(out.child.next).toBe(out);\n});\n\n// the per-type switch can only enumerate kinds Zod ships, so an unknown `def.type` falls back to scanning the def; without it a cycle through a third-party container overflows the stack\ntest(\"detects a cycle through a user-defined container type\", () => {\n  const MyBox: any = z.core.$constructor(\"MyBox\", (inst: any, def: any) => {\n    z.core.$ZodType.init(inst, def);\n    inst._zod.parse = (payload: any, ctx: any) => {\n      if (payload.value === null || typeof payload.value !== \"object\") return payload;\n      const inner = def.inner._zod.run({ value: payload.value.v, issues: [] }, ctx);\n      payload.value = { v: inner.value };\n      return payload;\n    };\n  });\n\n  const Node: any = z.object({\n    id: z.string(),\n    get boxed() {\n      return z.optional(new MyBox({ type: \"mybox\", inner: z.lazy(() => Node) }));\n    },\n  });\n\n  const input: any = { id: \"1\" };\n  input.boxed = { v: input };\n\n  const out: any = Node.parse(input);\n  expect(out.boxed.v).toBe(out);\n});","diff":"@@ -674,5 +674,47 @@ test(\"guards a transform built before any container\", () => {\n   const input: any = { name: \"x\" };\n   input.self = input;\n \n   expect(() => Wrapped.parse(input)).toThrow(/reference cycle/);\n });\n+\n+test(\"detects a cycle reachable only through a merged catchall\", () => {\n+  const Cyclic: any = z.object({\n+    id: z.string(),\n+    get next() {\n+      return z.optional(Root);\n+    },\n+  });\n+  const Root: any = z.object({ tag: z.string() }).merge(z.object({}).catchall(Cyclic));\n+\n+  const input: any = { tag: \"root\" };\n+  input.child = { id: \"1\", next: input };\n+\n+  const out: any = Root.parse(input);\n+  expect(out.child.next).toBe(out);\n+});\n+\n+// the per-type switch can only enumerate kinds Zod ships, so an unknown `def.type` falls back to scanning the def; without it a cycle through a third-party container overflows the stack\n+test(\"detects a cycle through a user-defined container type\", () => {\n+  const MyBox: any = z.core.$constructor(\"MyBox\", (inst: any, def: any) => {\n+    z.core.$ZodType.init(inst, def);\n+    inst._zod.parse = (payload: any, ctx: any) => {\n+      if (payload.value === null || typeof payload.value !== \"object\") return payload;\n+      const inner = def.inner._zod.run({ value: payload.value.v, issues: [] }, ctx);\n+      payload.value = { v: inner.value };\n+      return payload;\n+    };\n+  });\n+\n+  const Node: any = z.object({\n+    id: z.string(),\n+    get boxed() {\n+      return z.optional(new MyBox({ type: \"mybox\", inner: z.lazy(() => Node) }));\n+    },\n+  });\n+\n+  const input: any = { id: \"1\" };\n+  input.boxed = { v: input };\n+\n+  const out: any = Node.parse(input);\n+  expect(out.boxed.v).toBe(out);\n+});","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix(v4): stop the cycle walk from firing a default factory (#6500)","issue_url":"https://github.com/colinhacks/zod/issues/6495","pr_url":"https://github.com/colinhacks/zod/pull/6500"},"needs_manual_review":true}
{"id":"zod-eab51ff-1","repo":"colinhacks/zod","license":"MIT","commit":"eab51ff3592b2d11d863f4ee4d5452f31a3de1b6","parent":"7e24a24288183ce02554f1ded7775d0650a7b7e6","file":"packages/zod/src/v4/classic/tests/to-json-schema.test.ts","language":"typescript","hunk_header":"@@ -1147,11 +1147,11 @@ describe(\"toJSONSchema\", () => {","before":"      \"key1\",\n      \"key2\",\n    ]);\n  });\n\n  test(\"record filters enum values to strings and numbers for required\", () => {\n    enum NumberEnum {\n      Zero = 0,\n      One = 1,\n    }\n    const schema = z.record(z.enum(NumberEnum), z.string());","after":"      \"key1\",\n      \"key2\",\n    ]);\n  });\n\n  test(\"record stringifies numeric enum keys for propertyNames and required\", () => {\n    enum NumberEnum {\n      Zero = 0,\n      One = 1,\n    }\n    const schema = z.record(z.enum(NumberEnum), z.string());","diff":"@@ -1147,11 +1147,11 @@ describe(\"toJSONSchema\", () => {\n       \"key1\",\n       \"key2\",\n     ]);\n   });\n \n-  test(\"record filters enum values to strings and numbers for required\", () => {\n+  test(\"record stringifies numeric enum keys for propertyNames and required\", () => {\n     enum NumberEnum {\n       Zero = 0,\n       One = 1,\n     }\n     const schema = z.record(z.enum(NumberEnum), z.string());","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix(v4): emit record numeric keys as strings in toJSONSchema (#6497)","issue_url":"https://github.com/colinhacks/zod/issues/6496","pr_url":"https://github.com/colinhacks/zod/pull/6497"},"needs_manual_review":true}
{"id":"zod-7b612b5-1","repo":"colinhacks/zod","license":"MIT","commit":"7b612b536d8bcb1dd160a705cc2d9a4109cb50b3","parent":"1c43b7745a47e2a8c46361cf92280a9c03d52797","file":"packages/zod/src/v4/classic/tests/to-json-schema.test.ts","language":"typescript","hunk_header":"@@ -1050,36 +1050,24 @@ describe(\"toJSONSchema\", () => {","before":"    const schema = z.intersection(z.object({ name: z.string() }), z.object({ age: z.number() }));\n\n    expect(z.toJSONSchema(schema)).toMatchInlineSnapshot(`\n      {\n        \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n        \"allOf\": [\n          {\n            \"additionalProperties\": false,\n            \"properties\": {\n              \"name\": {\n                \"type\": \"string\",\n              },\n            },\n            \"required\": [\n              \"name\",\n            ],\n            \"type\": \"object\",\n          },\n          {\n            \"additionalProperties\": false,\n            \"properties\": {\n              \"age\": {\n                \"type\": \"number\",\n              },\n            },\n            \"required\": [\n              \"age\",\n            ],\n            \"type\": \"object\",\n          },\n        ],\n      }\n    `);\n  });\n\n  test(\"record\", () => {","after":"    const schema = z.intersection(z.object({ name: z.string() }), z.object({ age: z.number() }));\n\n    expect(z.toJSONSchema(schema)).toMatchInlineSnapshot(`\n      {\n        \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n        \"additionalProperties\": false,\n        \"properties\": {\n          \"age\": {\n            \"type\": \"number\",\n          },\n          \"name\": {\n            \"type\": \"string\",\n          },\n        },\n        \"required\": [\n          \"name\",\n          \"age\",\n        ],\n        \"type\": \"object\",\n      }\n    `);\n  });\n\n  test(\"record\", () => {","diff":"@@ -1050,36 +1050,24 @@ describe(\"toJSONSchema\", () => {\n     const schema = z.intersection(z.object({ name: z.string() }), z.object({ age: z.number() }));\n \n     expect(z.toJSONSchema(schema)).toMatchInlineSnapshot(`\n       {\n         \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n-        \"allOf\": [\n-          {\n-            \"additionalProperties\": false,\n-            \"properties\": {\n-              \"name\": {\n-                \"type\": \"string\",\n-              },\n-            },\n-            \"required\": [\n-              \"name\",\n-            ],\n-            \"type\": \"object\",\n+        \"additionalProperties\": false,\n+        \"properties\": {\n+          \"age\": {\n+            \"type\": \"number\",\n           },\n-          {\n-            \"additionalProperties\": false,\n-            \"properties\": {\n-              \"age\": {\n-                \"type\": \"number\",\n-              },\n-            },\n-            \"required\": [\n-              \"age\",\n-            ],\n-            \"type\": \"object\",\n+          \"name\": {\n+            \"type\": \"string\",\n           },\n+        },\n+        \"required\": [\n+          \"name\",\n+          \"age\",\n         ],\n+        \"type\": \"object\",\n       }\n     `);\n   });\n \n   test(\"record\", () => {","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix(v4): fold an intersection of object schemas into one object (#6461)","issue_url":"https://github.com/colinhacks/zod/issues/5390","pr_url":"https://github.com/colinhacks/zod/pull/6461"},"needs_manual_review":true}
{"id":"zod-a87ac36-1","repo":"colinhacks/zod","license":"MIT","commit":"a87ac3665f90144aecf8860030a59eb45a41a2eb","parent":"6726c1dd5bf47a39a9b7f22a80df421630b95b9b","file":"packages/zod/src/v4/classic/schemas.ts","language":"typescript","hunk_header":"@@ -1193,49 +1194,49 @@ function _zodNumberMethods(): _LazyMethodsOf<ZodNumber> {","before":"export function number(params?: string | core.$ZodNumberParams): ZodNumber {\n  return core._number(ZodNumber, params);\n}\n\n// ZodNumberFormat\nexport interface ZodNumberFormat extends ZodNumber {\n  _zod: core.$ZodNumberFormatInternals;\n}\nexport const ZodNumberFormat: core.$constructor<ZodNumberFormat> = /*@__PURE__*/ core.$constructor(\n  \"ZodNumberFormat\",\n  (inst, def) => {\n    core.$ZodNumberFormat.init(inst, def);\n    ZodNumber.init(inst, def);\n  }\n);\n\n// int\nexport interface ZodInt extends ZodNumberFormat {}\nexport function int(params?: string | core.$ZodCheckNumberFormatParams): ZodInt {\n  return core._int(ZodNumberFormat, params);\n}\n\n// float32\nexport interface ZodFloat32 extends ZodNumberFormat {}\nexport function float32(params?: string | core.$ZodCheckNumberFormatParams): ZodFloat32 {\n  return core._float32(ZodNumberFormat, params);\n}\n\n// float64\nexport interface ZodFloat64 extends ZodNumberFormat {}\nexport function float64(params?: string | core.$ZodCheckNumberFormatParams): ZodFloat64 {\n  return core._float64(ZodNumberFormat, params);\n}\n\n// int32\nexport interface ZodInt32 extends ZodNumberFormat {}\nexport function int32(params?: string | core.$ZodCheckNumberFormatParams): ZodInt32 {\n  return core._int32(ZodNumberFormat, params);\n}\n\n// uint32\nexport interface ZodUInt32 extends ZodNumberFormat {}\nexport function uint32(params?: string | core.$ZodCheckNumberFormatParams): ZodUInt32 {\n  return core._uint32(ZodNumberFormat, params);\n}\n\n// boolean\nexport interface _ZodBoolean<T extends core.$ZodBooleanInternals = core.$ZodBooleanInternals> extends _ZodType<T> {}\nexport interface ZodBoolean extends _ZodBoolean<core.$ZodBooleanInternals<boolean>> {}","after":"export function number(params?: string | core.$ZodNumberParams): ZodNumber {\n  return core._number(ZodNumber, params);\n}\n\n// ZodNumberFormat\nexport interface ZodNumberFormat<Format extends $ZodNumberFormats = $ZodNumberFormats> extends ZodNumber {\n  _zod: core.$ZodNumberFormatInternals<Format>;\n}\nexport const ZodNumberFormat: core.$constructor<ZodNumberFormat> = /*@__PURE__*/ core.$constructor(\n  \"ZodNumberFormat\",\n  (inst, def) => {\n    core.$ZodNumberFormat.init(inst, def);\n    ZodNumber.init(inst, def);\n  }\n);\n\n// int\nexport interface ZodInt extends ZodNumberFormat<\"safeint\"> {}\nexport function int(params?: string | core.$ZodCheckNumberFormatParams): ZodInt {\n  return core._int(ZodNumberFormat, params) as ZodInt;\n}\n\n// float32\nexport interface ZodFloat32 extends ZodNumberFormat<\"float32\"> {}\nexport function float32(params?: string | core.$ZodCheckNumberFormatParams): ZodFloat32 {\n  return core._float32(ZodNumberFormat, params) as ZodFloat32;\n}\n\n// float64\nexport interface ZodFloat64 extends ZodNumberFormat<\"float64\"> {}\nexport function float64(params?: string | core.$ZodCheckNumberFormatParams): ZodFloat64 {\n  return core._float64(ZodNumberFormat, params) as ZodFloat64;\n}\n\n// int32\nexport interface ZodInt32 extends ZodNumberFormat<\"int32\"> {}\nexport function int32(params?: string | core.$ZodCheckNumberFormatParams): ZodInt32 {\n  return core._int32(ZodNumberFormat, params) as ZodInt32;\n}\n\n// uint32\nexport interface ZodUInt32 extends ZodNumberFormat<\"uint32\"> {}\nexport function uint32(params?: string | core.$ZodCheckNumberFormatParams): ZodUInt32 {\n  return core._uint32(ZodNumberFormat, params) as ZodUInt32;\n}\n\n// boolean\nexport interface _ZodBoolean<T extends core.$ZodBooleanInternals = core.$ZodBooleanInternals> extends _ZodType<T> {}\nexport interface ZodBoolean extends _ZodBoolean<core.$ZodBooleanInternals<boolean>> {}","diff":"@@ -1193,49 +1194,49 @@ function _zodNumberMethods(): _LazyMethodsOf<ZodNumber> {\n export function number(params?: string | core.$ZodNumberParams): ZodNumber {\n   return core._number(ZodNumber, params);\n }\n \n // ZodNumberFormat\n-export interface ZodNumberFormat extends ZodNumber {\n-  _zod: core.$ZodNumberFormatInternals;\n+export interface ZodNumberFormat<Format extends $ZodNumberFormats = $ZodNumberFormats> extends ZodNumber {\n+  _zod: core.$ZodNumberFormatInternals<Format>;\n }\n export const ZodNumberFormat: core.$constructor<ZodNumberFormat> = /*@__PURE__*/ core.$constructor(\n   \"ZodNumberFormat\",\n   (inst, def) => {\n     core.$ZodNumberFormat.init(inst, def);\n     ZodNumber.init(inst, def);\n   }\n );\n \n // int\n-export interface ZodInt extends ZodNumberFormat {}\n+export interface ZodInt extends ZodNumberFormat<\"safeint\"> {}\n export function int(params?: string | core.$ZodCheckNumberFormatParams): ZodInt {\n-  return core._int(ZodNumberFormat, params);\n+  return core._int(ZodNumberFormat, params) as ZodInt;\n }\n \n // float32\n-export interface ZodFloat32 extends ZodNumberFormat {}\n+export interface ZodFloat32 extends ZodNumberFormat<\"float32\"> {}\n export function float32(params?: string | core.$ZodCheckNumberFormatParams): ZodFloat32 {\n-  return core._float32(ZodNumberFormat, params);\n+  return core._float32(ZodNumberFormat, params) as ZodFloat32;\n }\n \n // float64\n-export interface ZodFloat64 extends ZodNumberFormat {}\n+export interface ZodFloat64 extends ZodNumberFormat<\"float64\"> {}\n export function float64(params?: string | core.$ZodCheckNumberFormatParams): ZodFloat64 {\n-  return core._float64(ZodNumberFormat, params);\n+  return core._float64(ZodNumberFormat, params) as ZodFloat64;\n }\n \n // int32\n-export interface ZodInt32 extends ZodNumberFormat {}\n+export interface ZodInt32 extends ZodNumberFormat<\"int32\"> {}\n export function int32(params?: string | core.$ZodCheckNumberFormatParams): ZodInt32 {\n-  return core._int32(ZodNumberFormat, params);\n+  return core._int32(ZodNumberFormat, params) as ZodInt32;\n }\n \n // uint32\n-export interface ZodUInt32 extends ZodNumberFormat {}\n+export interface ZodUInt32 extends ZodNumberFormat<\"uint32\"> {}\n export function uint32(params?: string | core.$ZodCheckNumberFormatParams): ZodUInt32 {\n-  return core._uint32(ZodNumberFormat, params);\n+  return core._uint32(ZodNumberFormat, params) as ZodUInt32;\n }\n \n // boolean\n export interface _ZodBoolean<T extends core.$ZodBooleanInternals = core.$ZodBooleanInternals> extends _ZodType<T> {}\n export interface ZodBoolean extends _ZodBoolean<core.$ZodBooleanInternals<boolean>> {}","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix(v4)!: distinguish number and bigint formats at the type level (#6052)","issue_url":"https://github.com/colinhacks/zod/issues/6045","pr_url":"https://github.com/colinhacks/zod/pull/6052"},"needs_manual_review":true}
{"id":"zod-3a49696-1","repo":"colinhacks/zod","license":"MIT","commit":"3a49696865f3347a70852b544b0274692a00ad15","parent":"53cec2a0d0941a69d8add934ac3a8695fb5fb14c","file":"packages/zod/src/v4/classic/tests/record.test.ts","language":"typescript","hunk_header":"@@ -74,10 +74,41 @@ test(\"enum exhaustiveness\", () => {","before":"      \"success\": false,\n    }\n  `);\n});\n\ntest(\"typescript enum exhaustiveness\", () => {\n  enum BigFish {\n    Tuna = 0,\n    Salmon = \"Shark\",\n  }","after":"      \"success\": false,\n    }\n  `);\n});\n\ntest(\"optional-in value type\", () => {\n  const defaulted = z.record(z.enum([\"Tuna\", \"Salmon\"]), z.string().default(\"unknown\"));\n  expectTypeOf<z.input<typeof defaulted>>().toEqualTypeOf<Partial<Record<\"Tuna\" | \"Salmon\", string | undefined>>>();\n  expectTypeOf<z.output<typeof defaulted>>().toEqualTypeOf<Record<\"Tuna\" | \"Salmon\", string>>();\n  expect(defaulted.parse({ Tuna: \"asdf\" })).toEqual({ Tuna: \"asdf\", Salmon: \"unknown\" });\n\n  const prefaulted = z.record(z.enum([\"Tuna\", \"Salmon\"]), z.string().prefault(\"unknown\"));\n  expectTypeOf<z.input<typeof prefaulted>>().toEqualTypeOf<Partial<Record<\"Tuna\" | \"Salmon\", string | undefined>>>();\n  expect(prefaulted.parse({ Tuna: \"asdf\" })).toEqual({ Tuna: \"asdf\", Salmon: \"unknown\" });\n\n  const optional = z.record(z.enum([\"Tuna\", \"Salmon\"]), z.string().optional());\n  expectTypeOf<z.input<typeof optional>>().toEqualTypeOf<Partial<Record<\"Tuna\" | \"Salmon\", string | undefined>>>();\n  // toStrictEqual, not toEqual: an exhaustive record assigns every key, and toEqual cannot tell an absent key from one holding undefined.\n  expect(optional.parse({ Tuna: \"asdf\" })).toStrictEqual({ Tuna: \"asdf\", Salmon: undefined });\n\n  // A value that needs a slot keeps the key required, and a non-enumerable key stays an index signature either way.\n  const required = z.record(z.enum([\"Tuna\", \"Salmon\"]), z.string());\n  expectTypeOf<z.input<typeof required>>().toEqualTypeOf<Record<\"Tuna\" | \"Salmon\", string>>();\n  const indexed = z.record(z.string(), z.string().default(\"unknown\"));\n  expectTypeOf<z.input<typeof indexed>>().toEqualTypeOf<Record<string, string | undefined>>();\n\n  // A catch and a preprocess pipe declare no static optin, so the key stays required even where the parser can fill it. The static predicate and the one the JSON Schema emitter uses resolve these differently, and the two have to keep agreeing.\n  const caught = z.record(z.enum([\"Tuna\", \"Salmon\"]), z.string().catch(\"unknown\"));\n  expectTypeOf<z.input<typeof caught>>().toEqualTypeOf<Record<\"Tuna\" | \"Salmon\", string>>();\n  const preprocessed = z.record(\n    z.enum([\"Tuna\", \"Salmon\"]),\n    z.preprocess((v) => v, z.string())\n  );\n  expectTypeOf<z.input<typeof preprocessed>>().toEqualTypeOf<Record<\"Tuna\" | \"Salmon\", unknown>>();\n});\n\ntest(\"typescript enum exhaustiveness\", () => {\n  enum BigFish {\n    Tuna = 0,\n    Salmon = \"Shark\",\n  }","diff":"@@ -74,10 +74,41 @@ test(\"enum exhaustiveness\", () => {\n       \"success\": false,\n     }\n   `);\n });\n \n+test(\"optional-in value type\", () => {\n+  const defaulted = z.record(z.enum([\"Tuna\", \"Salmon\"]), z.string().default(\"unknown\"));\n+  expectTypeOf<z.input<typeof defaulted>>().toEqualTypeOf<Partial<Record<\"Tuna\" | \"Salmon\", string | undefined>>>();\n+  expectTypeOf<z.output<typeof defaulted>>().toEqualTypeOf<Record<\"Tuna\" | \"Salmon\", string>>();\n+  expect(defaulted.parse({ Tuna: \"asdf\" })).toEqual({ Tuna: \"asdf\", Salmon: \"unknown\" });\n+\n+  const prefaulted = z.record(z.enum([\"Tuna\", \"Salmon\"]), z.string().prefault(\"unknown\"));\n+  expectTypeOf<z.input<typeof prefaulted>>().toEqualTypeOf<Partial<Record<\"Tuna\" | \"Salmon\", string | undefined>>>();\n+  expect(prefaulted.parse({ Tuna: \"asdf\" })).toEqual({ Tuna: \"asdf\", Salmon: \"unknown\" });\n+\n+  const optional = z.record(z.enum([\"Tuna\", \"Salmon\"]), z.string().optional());\n+  expectTypeOf<z.input<typeof optional>>().toEqualTypeOf<Partial<Record<\"Tuna\" | \"Salmon\", string | undefined>>>();\n+  // toStrictEqual, not toEqual: an exhaustive record assigns every key, and toEqual cannot tell an absent key from one holding undefined.\n+  expect(optional.parse({ Tuna: \"asdf\" })).toStrictEqual({ Tuna: \"asdf\", Salmon: undefined });\n+\n+  // A value that needs a slot keeps the key required, and a non-enumerable key stays an index signature either way.\n+  const required = z.record(z.enum([\"Tuna\", \"Salmon\"]), z.string());\n+  expectTypeOf<z.input<typeof required>>().toEqualTypeOf<Record<\"Tuna\" | \"Salmon\", string>>();\n+  const indexed = z.record(z.string(), z.string().default(\"unknown\"));\n+  expectTypeOf<z.input<typeof indexed>>().toEqualTypeOf<Record<string, string | undefined>>();\n+\n+  // A catch and a preprocess pipe declare no static optin, so the key stays required even where the parser can fill it. The static predicate and the one the JSON Schema emitter uses resolve these differently, and the two have to keep agreeing.\n+  const caught = z.record(z.enum([\"Tuna\", \"Salmon\"]), z.string().catch(\"unknown\"));\n+  expectTypeOf<z.input<typeof caught>>().toEqualTypeOf<Record<\"Tuna\" | \"Salmon\", string>>();\n+  const preprocessed = z.record(\n+    z.enum([\"Tuna\", \"Salmon\"]),\n+    z.preprocess((v) => v, z.string())\n+  );\n+  expectTypeOf<z.input<typeof preprocessed>>().toEqualTypeOf<Record<\"Tuna\" | \"Salmon\", unknown>>();\n+});\n+\n test(\"typescript enum exhaustiveness\", () => {\n   enum BigFish {\n     Tuna = 0,\n     Salmon = \"Shark\",\n   }","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix(v4): make record input keys optional when the value can fill them (#6460)","issue_url":"https://github.com/colinhacks/zod/issues/6163","pr_url":"https://github.com/colinhacks/zod/pull/6460"},"needs_manual_review":true}
{"id":"zod-56222cd-1","repo":"colinhacks/zod","license":"MIT","commit":"56222cd1532c07bcb91b67df529cab4c0a215330","parent":"de65a5cb39ed22a507fac935788f718fa88d104f","file":"packages/zod/src/v4/classic/schemas.ts","language":"typescript","hunk_header":"@@ -2839,11 +2839,12 @@ type ZodInstanceOfParams = core.Params<","before":"  \"type\" | \"check\" | \"checks\" | \"fn\" | \"abort\" | \"error\" | \"params\" | \"path\"\n>;\n\n// ZodInstanceOf\nexport interface ZodInstanceOf<T = unknown> extends ZodCustom<T, T> {\n  properties<Shape extends core.$ZodShape>(\n    shape: Shape,\n    params?: string | core.$ZodCheckPropertiesParams\n  ): ZodInstanceOf<T & core.$InferObjectInput<Shape, {}>>;\n}\nexport const ZodInstanceOf: core.$constructor<ZodInstanceOf> = /*@__PURE__*/ core.$constructor(","after":"  \"type\" | \"check\" | \"checks\" | \"fn\" | \"abort\" | \"error\" | \"params\" | \"path\"\n>;\n\n// ZodInstanceOf\nexport interface ZodInstanceOf<T = unknown> extends ZodCustom<T, T> {\n  // the shape is keyed off the instance type, so keys autocomplete and a schema that can't accept the property's type is an error. Methods are excluded: every object literal inherits Object.prototype.toString, which would otherwise collide with the constraint's own toString entry and reject every shape.\n  properties<Shape extends { [k in keyof T as T[k] extends Function ? never : k]?: core.$ZodType<unknown, T[k]> }>(\n    shape: Shape,\n    params?: string | core.$ZodCheckPropertiesParams\n  ): ZodInstanceOf<T & core.$InferObjectInput<Shape, {}>>;\n}\nexport const ZodInstanceOf: core.$constructor<ZodInstanceOf> = /*@__PURE__*/ core.$constructor(","diff":"@@ -2839,11 +2839,12 @@ type ZodInstanceOfParams = core.Params<\n   \"type\" | \"check\" | \"checks\" | \"fn\" | \"abort\" | \"error\" | \"params\" | \"path\"\n >;\n \n // ZodInstanceOf\n export interface ZodInstanceOf<T = unknown> extends ZodCustom<T, T> {\n-  properties<Shape extends core.$ZodShape>(\n+  // the shape is keyed off the instance type, so keys autocomplete and a schema that can't accept the property's type is an error. Methods are excluded: every object literal inherits Object.prototype.toString, which would otherwise collide with the constraint's own toString entry and reject every shape.\n+  properties<Shape extends { [k in keyof T as T[k] extends Function ? never : k]?: core.$ZodType<unknown, T[k]> }>(\n     shape: Shape,\n     params?: string | core.$ZodCheckPropertiesParams\n   ): ZodInstanceOf<T & core.$InferObjectInput<Shape, {}>>;\n }\n export const ZodInstanceOf: core.$constructor<ZodInstanceOf> = /*@__PURE__*/ core.$constructor(","label":{"defect":false,"category":"feature","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"feat(instanceof): key the .properties() shape off the instance type (#6600)","issue_url":null,"pr_url":"https://github.com/colinhacks/zod/pull/6600"},"needs_manual_review":true}
{"id":"zod-d6bc1e3-1","repo":"colinhacks/zod","license":"MIT","commit":"d6bc1e30ac35fe8d2dfcb2c929dc76c87b274e94","parent":"ad32d7512ce4fe4d4b3cfc0d1f3ce93b2e553519","file":"packages/treeshake/bundle-size.test.ts","language":"typescript","hunk_header":"@@ -106,5 +106,21 @@ test(\"a mini bundle carries nothing from features it never uses\", async () => {","before":"  const code = await bundle(\"zod-mini-boolean\");\n  for (const identifier of MUST_NOT_APPEAR) {\n    expect(code, `${identifier} reached a bundle that only uses z.boolean()`).not.toContain(identifier);\n  }\n});","after":"  const code = await bundle(\"zod-mini-boolean\");\n  for (const identifier of MUST_NOT_APPEAR) {\n    expect(code, `${identifier} reached a bundle that only uses z.boolean()`).not.toContain(identifier);\n  }\n});\n\n// the custom formats read `core/regexes.ts` through a direct namespace import; reaching it as `core.regexes.x` made esbuild materialize every regex in the module, 2 KB on a hostname bundle\ntest(\"a hostname bundle carries no other format's regex\", async () => {\n  const result = await build({\n    stdin: { contents: `import * as z from \"zod/mini\"; console.log(z.hostname().parse(\"a.com\"));`, resolveDir: __dirname, loader: \"ts\" },\n    bundle: true,\n    minify: true,\n    format: \"esm\",\n    write: false,\n    logLevel: \"silent\",\n  });\n  const code = Buffer.from(result.outputFiles[0]!.contents).toString(\"utf8\");\n  // the xid regex, which nothing on a hostname path can need\n  expect(code, \"another format's regex reached a bundle that only uses z.hostname()\").not.toContain(\"[0-9a-vA-V]{20}\");\n});\n","diff":"@@ -106,5 +106,21 @@ test(\"a mini bundle carries nothing from features it never uses\", async () => {\n   const code = await bundle(\"zod-mini-boolean\");\n   for (const identifier of MUST_NOT_APPEAR) {\n     expect(code, `${identifier} reached a bundle that only uses z.boolean()`).not.toContain(identifier);\n   }\n });\n+\n+// the custom formats read `core/regexes.ts` through a direct namespace import; reaching it as `core.regexes.x` made esbuild materialize every regex in the module, 2 KB on a hostname bundle\n+test(\"a hostname bundle carries no other format's regex\", async () => {\n+  const result = await build({\n+    stdin: { contents: `import * as z from \"zod/mini\"; console.log(z.hostname().parse(\"a.com\"));`, resolveDir: __dirname, loader: \"ts\" },\n+    bundle: true,\n+    minify: true,\n+    format: \"esm\",\n+    write: false,\n+    logLevel: \"silent\",\n+  });\n+  const code = Buffer.from(result.outputFiles[0]!.contents).toString(\"utf8\");\n+  // the xid regex, which nothing on a hostname path can need\n+  expect(code, \"another format's regex reached a bundle that only uses z.hostname()\").not.toContain(\"[0-9a-vA-V]{20}\");\n+});\n+","label":{"defect":false,"category":"feature","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"feat: add z.currencyCode() over a vendored ISO 4217 list, refreshed weekly by CI (#6595)","issue_url":null,"pr_url":"https://github.com/colinhacks/zod/pull/6595"},"needs_manual_review":true}
{"id":"zod-ad32d75-1","repo":"colinhacks/zod","license":"MIT","commit":"ad32d7512ce4fe4d4b3cfc0d1f3ce93b2e553519","parent":"12e6272746eb79c9713f4818e85a15231bd368b6","file":"packages/treeshake/bundle-size.test.ts","language":"typescript","hunk_header":"@@ -48,21 +48,24 @@ const CEILINGS: Record<string, number> = {","before":"  // Also carries the Standard Schema issue bag: a failing `~standard.validate` parses with a plain issue holder instead of constructing a ZodError. Measured 2974 / 3455 / 4540 locally plus 28 headroom.\n  // Also carries the lazy `safeParse` error: a getter and a setter on the failing result, and the async wrapper that keeps a sync throw a rejection. Measured 3021 / 3497 / 4581 locally plus 18 headroom.\n  // Also carries the symbol-key loop in `util.members`, which is what installs `$ZodProperties`'s `Symbol.iterator` on its prototype, and the memoizer's shared reference predicate. Measured 3036 / 3516 / 4611 locally plus 18 headroom.\n  // Lowered by the measured −105 when checks stopped writing metadata into the bag at attach time: the string length/format closures and the per-instance bounded regex left every string bundle, and the template literal now derives its own part patterns.\n  // Lowered by the measured −17 / −16 / −16 from making z.properties() a check again: the symbol-key loop in `util.members` and the memoizer's callable arm left with the schema role. Measured 3044 / 3418 / 4629 locally plus 18 headroom; the object ceiling had been overtaken to zero headroom on main, so its number moves up by two while the bundle moves down.\n  \"zod-mini-string\": 3436,\n  // Also carries the construction-time discriminator check, which writes a WeakMap entry from `$ZodObject`, so every bundle containing `z.object` pays for it whether or not it builds a discriminated union.\n  // Also carries the declared symbol keys from #6448: `normalizeDef` collects the shape's own symbols and the parse loop walks them. Almost none of that is the `Reflect.ownKeys` conversions — reverting all eight of them measures a byte larger.\n  // Also carries the memoizer seam from #6482: each container init reads `globalConfig.memoizer` and calls `attach`, which is what lets `zod/mini` opt into cycle support. Measured 4512 locally but 4533 on CI — the gzip stream differs across zlib builds — so the headroom rides on the CI number.\n  // Also carries the Standard Schema issue bag: a failing `~standard.validate` parses with a plain issue holder instead of constructing a ZodError. Measured 2974 / 3455 / 4540 locally plus 28 headroom.\n  // Also carries the lazy `safeParse` error: a getter and a setter on the failing result, and the async wrapper that keeps a sync throw a rejection. Measured 3021 / 3497 / 4581 locally plus 18 headroom.\n  // Also carries the symbol-key loop in `util.members`, which is what installs `$ZodProperties`'s `Symbol.iterator` on its prototype, and the memoizer's shared reference predicate. Measured 3036 / 3516 / 4611 locally plus 18 headroom.\n  // Also carries the guards `validate` parses under: an `aborted` check in each container loop, and two early exits in the generated object parser. Only a bundle containing a container pays — boolean is byte-identical to main and string measures three bytes smaller — so this ceiling rises by the measured +76 and the other two do not move. Measured 4696 locally plus 18 headroom.\n  // Lowered by the measured −69 for the same bag strip: the string and number inits no longer carry pattern derivation or attach-time metadata writes.\n  // Lowered by the measured −17 / −16 / −16 from making z.properties() a check again: the symbol-key loop in `util.members` and the memoizer's callable arm left with the schema role. Measured 3044 / 3418 / 4629 locally plus 18 headroom; the object ceiling had been overtaken to zero headroom on main, so its number moves up by two while the bundle moves down.\n  \"zod-mini-object\": 4647,\n};\n\n/**\n * Identifiers that must not appear in a bundle that never uses the feature they\n * belong to. A byte ceiling drifts with unrelated churn; this does not, and it","after":"  // Also carries the Standard Schema issue bag: a failing `~standard.validate` parses with a plain issue holder instead of constructing a ZodError. Measured 2974 / 3455 / 4540 locally plus 28 headroom.\n  // Also carries the lazy `safeParse` error: a getter and a setter on the failing result, and the async wrapper that keeps a sync throw a rejection. Measured 3021 / 3497 / 4581 locally plus 18 headroom.\n  // Also carries the symbol-key loop in `util.members`, which is what installs `$ZodProperties`'s `Symbol.iterator` on its prototype, and the memoizer's shared reference predicate. Measured 3036 / 3516 / 4611 locally plus 18 headroom.\n  // Lowered by the measured −105 when checks stopped writing metadata into the bag at attach time: the string length/format closures and the per-instance bounded regex left every string bundle, and the template literal now derives its own part patterns.\n  // Lowered by the measured −17 / −16 / −16 from making z.properties() a check again: the symbol-key loop in `util.members` and the memoizer's callable arm left with the schema role. Measured 3044 / 3418 / 4629 locally plus 18 headroom; the object ceiling had been overtaken to zero headroom on main, so its number moves up by two while the bundle moves down.\n\n  // factory check snapshots on the check-only properties base measure 3438 gzip bytes plus 28 headroom\n  \"zod-mini-string\": 3466,\n  // Also carries the construction-time discriminator check, which writes a WeakMap entry from `$ZodObject`, so every bundle containing `z.object` pays for it whether or not it builds a discriminated union.\n  // Also carries the declared symbol keys from #6448: `normalizeDef` collects the shape's own symbols and the parse loop walks them. Almost none of that is the `Reflect.ownKeys` conversions — reverting all eight of them measures a byte larger.\n  // Also carries the memoizer seam from #6482: each container init reads `globalConfig.memoizer` and calls `attach`, which is what lets `zod/mini` opt into cycle support. Measured 4512 locally but 4533 on CI — the gzip stream differs across zlib builds — so the headroom rides on the CI number.\n  // Also carries the Standard Schema issue bag: a failing `~standard.validate` parses with a plain issue holder instead of constructing a ZodError. Measured 2974 / 3455 / 4540 locally plus 28 headroom.\n  // Also carries the lazy `safeParse` error: a getter and a setter on the failing result, and the async wrapper that keeps a sync throw a rejection. Measured 3021 / 3497 / 4581 locally plus 18 headroom.\n  // Also carries the symbol-key loop in `util.members`, which is what installs `$ZodProperties`'s `Symbol.iterator` on its prototype, and the memoizer's shared reference predicate. Measured 3036 / 3516 / 4611 locally plus 18 headroom.\n  // Also carries the guards `validate` parses under: an `aborted` check in each container loop, and two early exits in the generated object parser. Only a bundle containing a container pays — boolean is byte-identical to main and string measures three bytes smaller — so this ceiling rises by the measured +76 and the other two do not move. Measured 4696 locally plus 18 headroom.\n  // Lowered by the measured −69 for the same bag strip: the string and number inits no longer carry pattern derivation or attach-time metadata writes.\n\n  // factory check snapshots on the check-only properties base measure 4654 gzip bytes plus 28 headroom\n  \"zod-mini-object\": 4682,\n};\n\n/**\n * Identifiers that must not appear in a bundle that never uses the feature they\n * belong to. A byte ceiling drifts with unrelated churn; this does not, and it","diff":"@@ -48,21 +48,24 @@ const CEILINGS: Record<string, number> = {\n   // Also carries the Standard Schema issue bag: a failing `~standard.validate` parses with a plain issue holder instead of constructing a ZodError. Measured 2974 / 3455 / 4540 locally plus 28 headroom.\n   // Also carries the lazy `safeParse` error: a getter and a setter on the failing result, and the async wrapper that keeps a sync throw a rejection. Measured 3021 / 3497 / 4581 locally plus 18 headroom.\n   // Also carries the symbol-key loop in `util.members`, which is what installs `$ZodProperties`'s `Symbol.iterator` on its prototype, and the memoizer's shared reference predicate. Measured 3036 / 3516 / 4611 locally plus 18 headroom.\n   // Lowered by the measured −105 when checks stopped writing metadata into the bag at attach time: the string length/format closures and the per-instance bounded regex left every string bundle, and the template literal now derives its own part patterns.\n   // Lowered by the measured −17 / −16 / −16 from making z.properties() a check again: the symbol-key loop in `util.members` and the memoizer's callable arm left with the schema role. Measured 3044 / 3418 / 4629 locally plus 18 headroom; the object ceiling had been overtaken to zero headroom on main, so its number moves up by two while the bundle moves down.\n-  \"zod-mini-string\": 3436,\n+\n+  // factory check snapshots on the check-only properties base measure 3438 gzip bytes plus 28 headroom\n+  \"zod-mini-string\": 3466,\n   // Also carries the construction-time discriminator check, which writes a WeakMap entry from `$ZodObject`, so every bundle containing `z.object` pays for it whether or not it builds a discriminated union.\n   // Also carries the declared symbol keys from #6448: `normalizeDef` collects the shape's own symbols and the parse loop walks them. Almost none of that is the `Reflect.ownKeys` conversions — reverting all eight of them measures a byte larger.\n   // Also carries the memoizer seam from #6482: each container init reads `globalConfig.memoizer` and calls `attach`, which is what lets `zod/mini` opt into cycle support. Measured 4512 locally but 4533 on CI — the gzip stream differs across zlib builds — so the headroom rides on the CI number.\n   // Also carries the Standard Schema issue bag: a failing `~standard.validate` parses with a plain issue holder instead of constructing a ZodError. Measured 2974 / 3455 / 4540 locally plus 28 headroom.\n   // Also carries the lazy `safeParse` error: a getter and a setter on the failing result, and the async wrapper that keeps a sync throw a rejection. Measured 3021 / 3497 / 4581 locally plus 18 headroom.\n   // Also carries the symbol-key loop in `util.members`, which is what installs `$ZodProperties`'s `Symbol.iterator` on its prototype, and the memoizer's shared reference predicate. Measured 3036 / 3516 / 4611 locally plus 18 headroom.\n   // Also carries the guards `validate` parses under: an `aborted` check in each container loop, and two early exits in the generated object parser. Only a bundle containing a container pays — boolean is byte-identical to main and string measures three bytes smaller — so this ceiling rises by the measured +76 and the other two do not move. Measured 4696 locally plus 18 headroom.\n   // Lowered by the measured −69 for the same bag strip: the string and number inits no longer carry pattern derivation or attach-time metadata writes.\n-  // Lowered by the measured −17 / −16 / −16 from making z.properties() a check again: the symbol-key loop in `util.members` and the memoizer's callable arm left with the schema role. Measured 3044 / 3418 / 4629 locally plus 18 headroom; the object ceiling had been overtaken to zero headroom on main, so its number moves up by two while the bundle moves down.\n-  \"zod-mini-object\": 4647,\n+\n+  // factory check snapshots on the check-only properties base measure 4654 gzip bytes plus 28 headroom\n+  \"zod-mini-object\": 4682,\n };\n \n /**\n  * Identifiers that must not appear in a bundle that never uses the feature they\n  * belong to. A byte ceiling drifts with unrelated churn; this does not, and it","label":{"defect":false,"category":"refactor","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"perf: reduce validation and schema allocation overhead (#6588)","issue_url":null,"pr_url":"https://github.com/colinhacks/zod/pull/6588"},"needs_manual_review":true}
{"id":"zod-213ee75-1","repo":"colinhacks/zod","license":"MIT","commit":"213ee75d2ad6e41508587c3fab8246967f006993","parent":"f9465d4e6497f92fe85ef7118ea64d7f86c28676","file":"packages/zod/src/v4/classic/external.ts","language":"typescript","hunk_header":"@@ -24,12 +24,14 @@ export {","before":"  formatError,\n  flattenError,\n  TimePrecision,\n  util,\n  NEVER,\n  toZod,\n  compile,\n  ZodCompileAsyncError,\n  ZodCompileUnsupportedError,\n  getDiscriminatedOption,\n} from \"../core/index.js\";\nexport { toJSONSchema } from \"../core/json-schema-processors.js\";","after":"  formatError,\n  flattenError,\n  TimePrecision,\n  util,\n  NEVER,\n  INVALID,\n  toZod,\n  compile,\n  withParser,\n  ZodCompileAsyncError,\n  ZodCompileUnsupportedError,\n  getDiscriminatedOption,\n} from \"../core/index.js\";\nexport { toJSONSchema } from \"../core/json-schema-processors.js\";","diff":"@@ -24,12 +24,14 @@ export {\n   formatError,\n   flattenError,\n   TimePrecision,\n   util,\n   NEVER,\n+  INVALID,\n   toZod,\n   compile,\n+  withParser,\n   ZodCompileAsyncError,\n   ZodCompileUnsupportedError,\n   getDiscriminatedOption,\n } from \"../core/index.js\";\n export { toJSONSchema } from \"../core/json-schema-processors.js\";","label":{"defect":false,"category":"feature","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"feat(compile): add z.withParser for externally generated parsers (#6575)","issue_url":null,"pr_url":"https://github.com/colinhacks/zod/pull/6575"},"needs_manual_review":true}
{"id":"zod-f7fd554-1","repo":"colinhacks/zod","license":"MIT","commit":"f7fd5548063f55a74847b2711630c9b3f8ea00d4","parent":"36f17960d1defca5d0896d9424f4e1059fbbf081","file":"packages/zod/src/v4/classic/tests/continuability.test.ts","language":"typescript","hunk_header":"@@ -38,11 +38,11 @@ test(\"continuability\", () => {","before":"        \"code\": \"invalid_format\",\n        \"format\": \"email\",\n        \"message\": \"Invalid email address\",\n        \"origin\": \"string\",\n        \"path\": [],\n        \"pattern\": \"/^(?!\\\\.)(?!.*\\\\.\\\\.)([A-Za-z0-9_'+\\\\-\\\\.]*)[A-Za-z0-9_+-]@([A-Za-z0-9][A-Za-z0-9\\\\-]*\\\\.)+[A-Za-z]{2,}$/\",\n      },\n      {\n        \"code\": \"custom\",\n        \"message\": \"Invalid input\",\n        \"path\": [],","after":"        \"code\": \"invalid_format\",\n        \"format\": \"email\",\n        \"message\": \"Invalid email address\",\n        \"origin\": \"string\",\n        \"path\": [],\n        \"pattern\": \"/^(?:[A-Za-z0-9_'+\\\\-]+\\\\.)*[A-Za-z0-9_'+\\\\-]*[A-Za-z0-9_+-]@(?:[A-Za-z0-9][A-Za-z0-9\\\\-]*\\\\.)+[A-Za-z]{2,}$/\",\n      },\n      {\n        \"code\": \"custom\",\n        \"message\": \"Invalid input\",\n        \"path\": [],","diff":"@@ -38,11 +38,11 @@ test(\"continuability\", () => {\n         \"code\": \"invalid_format\",\n         \"format\": \"email\",\n         \"message\": \"Invalid email address\",\n         \"origin\": \"string\",\n         \"path\": [],\n-        \"pattern\": \"/^(?!\\\\.)(?!.*\\\\.\\\\.)([A-Za-z0-9_'+\\\\-\\\\.]*)[A-Za-z0-9_+-]@([A-Za-z0-9][A-Za-z0-9\\\\-]*\\\\.)+[A-Za-z]{2,}$/\",\n+        \"pattern\": \"/^(?:[A-Za-z0-9_'+\\\\-]+\\\\.)*[A-Za-z0-9_'+\\\\-]*[A-Za-z0-9_+-]@(?:[A-Za-z0-9][A-Za-z0-9\\\\-]*\\\\.)+[A-Za-z]{2,}$/\",\n       },\n       {\n         \"code\": \"custom\",\n         \"message\": \"Invalid input\",\n         \"path\": [],","label":{"defect":false,"category":"refactor","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"perf(v4): drop the lookaheads from the email regex (#6573)","issue_url":null,"pr_url":"https://github.com/colinhacks/zod/pull/6573"},"needs_manual_review":true}
{"id":"zod-741981f-1","repo":"colinhacks/zod","license":"MIT","commit":"741981ffdbc8772cffc38cc4eaf84d5a45fa7384","parent":"804e0f522747345d6b37581888899be420baa3e9","file":"packages/treeshake/bundle-size.test.ts","language":"typescript","hunk_header":"@@ -40,17 +40,17 @@ const BUILT_ENTRY = path.join(__dirname, \"node_modules\", \"zod\", \"index.js\");","before":"const CEILINGS: Record<string, number> = {\n  // raised from 2903 / 3366 / 4437 by the commit that caused it: installing a trait's members from `$constructor` rather than from its initializer costs mini +70 / +83 / +95, and every bundle carries `core.ts`; measured 2946 / 3420 / 4503 plus 28 headroom, and the per-fixture notes below record what each already carried. About half of it is the guard that keeps the install off a user subclass's prototype; the rest is writing the members as `this`-methods, since `this` is not renameable and a block body cannot collapse to an arrow.\n  // Also carries the Standard Schema issue bag: a failing `~standard.validate` parses with a plain issue holder instead of constructing a ZodError. Measured 2974 / 3455 / 4540 locally plus 28 headroom.\n  // Also carries the lazy `safeParse` error: a getter and a setter on the failing result, and the async wrapper that keeps a sync throw a rejection. Measured 3021 / 3497 / 4581 locally plus 18 headroom.\n  // Also carries the symbol-key loop in `util.members`, which is what installs `$ZodProperties`'s `Symbol.iterator` on its prototype, and the memoizer's shared reference predicate. Measured 3036 / 3516 / 4611 locally plus 18 headroom.\n  \"zod-mini-boolean\": 3054,\n  // Also carries the code-point string length scan: `.min`/`.max`/`.length` on a string pulls in the surrogate walk.\n  // Also carries the Standard Schema issue bag: a failing `~standard.validate` parses with a plain issue holder instead of constructing a ZodError. Measured 2974 / 3455 / 4540 locally plus 28 headroom.\n  // Also carries the lazy `safeParse` error: a getter and a setter on the failing result, and the async wrapper that keeps a sync throw a rejection. Measured 3021 / 3497 / 4581 locally plus 18 headroom.\n  // Also carries the symbol-key loop in `util.members`, which is what installs `$ZodProperties`'s `Symbol.iterator` on its prototype, and the memoizer's shared reference predicate. Measured 3036 / 3516 / 4611 locally plus 18 headroom.\n  // Lowered by the measured −105 when checks stopped writing metadata into the bag at attach time: the string length/format closures and the per-instance bounded regex left every string bundle, and the template literal now derives its own part patterns.\n  \"zod-mini-string\": 3429,\n  // Also carries the construction-time discriminator check, which writes a WeakMap entry from `$ZodObject`, so every bundle containing `z.object` pays for it whether or not it builds a discriminated union.\n  // Also carries the declared symbol keys from #6448: `normalizeDef` collects the shape's own symbols and the parse loop walks them. Almost none of that is the `Reflect.ownKeys` conversions — reverting all eight of them measures a byte larger.\n  // Also carries the memoizer seam from #6482: each container init reads `globalConfig.memoizer` and calls `attach`, which is what lets `zod/mini` opt into cycle support. Measured 4512 locally but 4533 on CI — the gzip stream differs across zlib builds — so the headroom rides on the CI number.\n  // Also carries the Standard Schema issue bag: a failing `~standard.validate` parses with a plain issue holder instead of constructing a ZodError. Measured 2974 / 3455 / 4540 locally plus 28 headroom.\n  // Also carries the lazy `safeParse` error: a getter and a setter on the failing result, and the async wrapper that keeps a sync throw a rejection. Measured 3021 / 3497 / 4581 locally plus 18 headroom.","after":"const CEILINGS: Record<string, number> = {\n  // raised from 2903 / 3366 / 4437 by the commit that caused it: installing a trait's members from `$constructor` rather than from its initializer costs mini +70 / +83 / +95, and every bundle carries `core.ts`; measured 2946 / 3420 / 4503 plus 28 headroom, and the per-fixture notes below record what each already carried. About half of it is the guard that keeps the install off a user subclass's prototype; the rest is writing the members as `this`-methods, since `this` is not renameable and a block body cannot collapse to an arrow.\n  // Also carries the Standard Schema issue bag: a failing `~standard.validate` parses with a plain issue holder instead of constructing a ZodError. Measured 2974 / 3455 / 4540 locally plus 28 headroom.\n  // Also carries the lazy `safeParse` error: a getter and a setter on the failing result, and the async wrapper that keeps a sync throw a rejection. Measured 3021 / 3497 / 4581 locally plus 18 headroom.\n  // Also carries the symbol-key loop in `util.members`, which is what installs `$ZodProperties`'s `Symbol.iterator` on its prototype, and the memoizer's shared reference predicate. Measured 3036 / 3516 / 4611 locally plus 18 headroom.\n  \"zod-mini-boolean\": 3088,\n  // Also carries the code-point string length scan: `.min`/`.max`/`.length` on a string pulls in the surrogate walk.\n  // Also carries the Standard Schema issue bag: a failing `~standard.validate` parses with a plain issue holder instead of constructing a ZodError. Measured 2974 / 3455 / 4540 locally plus 28 headroom.\n  // Also carries the lazy `safeParse` error: a getter and a setter on the failing result, and the async wrapper that keeps a sync throw a rejection. Measured 3021 / 3497 / 4581 locally plus 18 headroom.\n  // Also carries the symbol-key loop in `util.members`, which is what installs `$ZodProperties`'s `Symbol.iterator` on its prototype, and the memoizer's shared reference predicate. Measured 3036 / 3516 / 4611 locally plus 18 headroom.\n  // Lowered by the measured −105 when checks stopped writing metadata into the bag at attach time: the string length/format closures and the per-instance bounded regex left every string bundle, and the template literal now derives its own part patterns.\n  \"zod-mini-string\": 3462,\n  // Also carries the construction-time discriminator check, which writes a WeakMap entry from `$ZodObject`, so every bundle containing `z.object` pays for it whether or not it builds a discriminated union.\n  // Also carries the declared symbol keys from #6448: `normalizeDef` collects the shape's own symbols and the parse loop walks them. Almost none of that is the `Reflect.ownKeys` conversions — reverting all eight of them measures a byte larger.\n  // Also carries the memoizer seam from #6482: each container init reads `globalConfig.memoizer` and calls `attach`, which is what lets `zod/mini` opt into cycle support. Measured 4512 locally but 4533 on CI — the gzip stream differs across zlib builds — so the headroom rides on the CI number.\n  // Also carries the Standard Schema issue bag: a failing `~standard.validate` parses with a plain issue holder instead of constructing a ZodError. Measured 2974 / 3455 / 4540 locally plus 28 headroom.\n  // Also carries the lazy `safeParse` error: a getter and a setter on the failing result, and the async wrapper that keeps a sync throw a rejection. Measured 3021 / 3497 / 4581 locally plus 18 headroom.","diff":"@@ -40,17 +40,17 @@ const BUILT_ENTRY = path.join(__dirname, \"node_modules\", \"zod\", \"index.js\");\n const CEILINGS: Record<string, number> = {\n   // raised from 2903 / 3366 / 4437 by the commit that caused it: installing a trait's members from `$constructor` rather than from its initializer costs mini +70 / +83 / +95, and every bundle carries `core.ts`; measured 2946 / 3420 / 4503 plus 28 headroom, and the per-fixture notes below record what each already carried. About half of it is the guard that keeps the install off a user subclass's prototype; the rest is writing the members as `this`-methods, since `this` is not renameable and a block body cannot collapse to an arrow.\n   // Also carries the Standard Schema issue bag: a failing `~standard.validate` parses with a plain issue holder instead of constructing a ZodError. Measured 2974 / 3455 / 4540 locally plus 28 headroom.\n   // Also carries the lazy `safeParse` error: a getter and a setter on the failing result, and the async wrapper that keeps a sync throw a rejection. Measured 3021 / 3497 / 4581 locally plus 18 headroom.\n   // Also carries the symbol-key loop in `util.members`, which is what installs `$ZodProperties`'s `Symbol.iterator` on its prototype, and the memoizer's shared reference predicate. Measured 3036 / 3516 / 4611 locally plus 18 headroom.\n-  \"zod-mini-boolean\": 3054,\n+  \"zod-mini-boolean\": 3088,\n   // Also carries the code-point string length scan: `.min`/`.max`/`.length` on a string pulls in the surrogate walk.\n   // Also carries the Standard Schema issue bag: a failing `~standard.validate` parses with a plain issue holder instead of constructing a ZodError. Measured 2974 / 3455 / 4540 locally plus 28 headroom.\n   // Also carries the lazy `safeParse` error: a getter and a setter on the failing result, and the async wrapper that keeps a sync throw a rejection. Measured 3021 / 3497 / 4581 locally plus 18 headroom.\n   // Also carries the symbol-key loop in `util.members`, which is what installs `$ZodProperties`'s `Symbol.iterator` on its prototype, and the memoizer's shared reference predicate. Measured 3036 / 3516 / 4611 locally plus 18 headroom.\n   // Lowered by the measured −105 when checks stopped writing metadata into the bag at attach time: the string length/format closures and the per-instance bounded regex left every string bundle, and the template literal now derives its own part patterns.\n-  \"zod-mini-string\": 3429,\n+  \"zod-mini-string\": 3462,\n   // Also carries the construction-time discriminator check, which writes a WeakMap entry from `$ZodObject`, so every bundle containing `z.object` pays for it whether or not it builds a discriminated union.\n   // Also carries the declared symbol keys from #6448: `normalizeDef` collects the shape's own symbols and the parse loop walks them. Almost none of that is the `Reflect.ownKeys` conversions — reverting all eight of them measures a byte larger.\n   // Also carries the memoizer seam from #6482: each container init reads `globalConfig.memoizer` and calls `attach`, which is what lets `zod/mini` opt into cycle support. Measured 4512 locally but 4533 on CI — the gzip stream differs across zlib builds — so the headroom rides on the CI number.\n   // Also carries the Standard Schema issue bag: a failing `~standard.validate` parses with a plain issue holder instead of constructing a ZodError. Measured 2974 / 3455 / 4540 locally plus 28 headroom.\n   // Also carries the lazy `safeParse` error: a getter and a setter on the failing result, and the async wrapper that keeps a sync throw a rejection. Measured 3021 / 3497 / 4581 locally plus 18 headroom.","label":{"defect":false,"category":"refactor","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"perf(compile): for-in record walk, cheaper issue finalization, and a generative compile differential (#6567)","issue_url":null,"pr_url":"https://github.com/colinhacks/zod/pull/6567"},"needs_manual_review":true}
{"id":"zod-5489a53-1","repo":"colinhacks/zod","license":"MIT","commit":"5489a532e65ee3f5ab0959ff8a5da346ced96f2a","parent":"e7604717801fe5cc17c525fb92abe0abeae0ae6e","file":"packages/zod/src/v4/classic/tests/validate.test.ts","language":"typescript","hunk_header":"@@ -172,10 +172,23 @@ test(\"compiled validate agrees with the interpreter, verdict and throw alike\", (","before":"    z.string().refine(thenable),\n    z.string().pipe(z.string().min(3)),\n    // a codec's decode is the pipe branch's transform, and a plain function can still hand back a promise\n    z.codec(z.string(), z.number(), { decode: thenable, encode: String }),\n    z.object({ first: z.string(), second: z.codec(z.string(), z.number(), { decode: thenable, encode: String }) }),\n    // a user callback can throw, and compiled code can reject an earlier sibling before ever reaching it\n    z.object({\n      first: z.string(),\n      second: z.string().refine(() => {\n        throw new RangeError(\"u\");","after":"    z.string().refine(thenable),\n    z.string().pipe(z.string().min(3)),\n    // a codec's decode is the pipe branch's transform, and a plain function can still hand back a promise\n    z.codec(z.string(), z.number(), { decode: thenable, encode: String }),\n    z.object({ first: z.string(), second: z.codec(z.string(), z.number(), { decode: thenable, encode: String }) }),\n    // a continuable check does not settle the interpreter's walk, so it reaches a throwing callback the compiled path short-circuits past — the first-failure exit does not cover the check chain\n    z\n      .string()\n      .min(5)\n      .refine(() => {\n        throw new RangeError(\"u\");\n      }),\n    z\n      .string()\n      .min(5)\n      .superRefine(() => {\n        throw new RangeError(\"u\");\n      }),\n    // a user callback can throw, and compiled code can reject an earlier sibling before ever reaching it\n    z.object({\n      first: z.string(),\n      second: z.string().refine(() => {\n        throw new RangeError(\"u\");","diff":"@@ -172,10 +172,23 @@ test(\"compiled validate agrees with the interpreter, verdict and throw alike\", (\n     z.string().refine(thenable),\n     z.string().pipe(z.string().min(3)),\n     // a codec's decode is the pipe branch's transform, and a plain function can still hand back a promise\n     z.codec(z.string(), z.number(), { decode: thenable, encode: String }),\n     z.object({ first: z.string(), second: z.codec(z.string(), z.number(), { decode: thenable, encode: String }) }),\n+    // a continuable check does not settle the interpreter's walk, so it reaches a throwing callback the compiled path short-circuits past — the first-failure exit does not cover the check chain\n+    z\n+      .string()\n+      .min(5)\n+      .refine(() => {\n+        throw new RangeError(\"u\");\n+      }),\n+    z\n+      .string()\n+      .min(5)\n+      .superRefine(() => {\n+        throw new RangeError(\"u\");\n+      }),\n     // a user callback can throw, and compiled code can reject an earlier sibling before ever reaching it\n     z.object({\n       first: z.string(),\n       second: z.string().refine(() => {\n         throw new RangeError(\"u\");","label":{"defect":false,"category":"test","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"test(v4): pin the check-chain case that keeps compiled validate's definite guard (#6551)","issue_url":null,"pr_url":"https://github.com/colinhacks/zod/pull/6551"},"needs_manual_review":true}
{"id":"zod-764ac59-1","repo":"colinhacks/zod","license":"MIT","commit":"764ac59f1afc1a46e7ecc2d5e8e86c57b7d66164","parent":"07917f4cb396646dff7a1364f9032a668b0d7eb4","file":"packages/treeshake/bundle-size.test.ts","language":"typescript","hunk_header":"@@ -52,11 +52,12 @@ const CEILINGS: Record<string, number> = {","before":"  // Also carries the declared symbol keys from #6448: `normalizeDef` collects the shape's own symbols and the parse loop walks them. Almost none of that is the `Reflect.ownKeys` conversions — reverting all eight of them measures a byte larger.\n  // Also carries the memoizer seam from #6482: each container init reads `globalConfig.memoizer` and calls `attach`, which is what lets `zod/mini` opt into cycle support. Measured 4512 locally but 4533 on CI — the gzip stream differs across zlib builds — so the headroom rides on the CI number.\n  // Also carries the Standard Schema issue bag: a failing `~standard.validate` parses with a plain issue holder instead of constructing a ZodError. Measured 2974 / 3455 / 4540 locally plus 28 headroom.\n  // Also carries the lazy `safeParse` error: a getter and a setter on the failing result, and the async wrapper that keeps a sync throw a rejection. Measured 3021 / 3497 / 4581 locally plus 18 headroom.\n  // Also carries the symbol-key loop in `util.members`, which is what installs `$ZodProperties`'s `Symbol.iterator` on its prototype, and the memoizer's shared reference predicate. Measured 3036 / 3516 / 4611 locally plus 18 headroom.\n  \"zod-mini-object\": 4629,\n};\n\n/**\n * Identifiers that must not appear in a bundle that never uses the feature they\n * belong to. A byte ceiling drifts with unrelated churn; this does not, and it","after":"  // Also carries the declared symbol keys from #6448: `normalizeDef` collects the shape's own symbols and the parse loop walks them. Almost none of that is the `Reflect.ownKeys` conversions — reverting all eight of them measures a byte larger.\n  // Also carries the memoizer seam from #6482: each container init reads `globalConfig.memoizer` and calls `attach`, which is what lets `zod/mini` opt into cycle support. Measured 4512 locally but 4533 on CI — the gzip stream differs across zlib builds — so the headroom rides on the CI number.\n  // Also carries the Standard Schema issue bag: a failing `~standard.validate` parses with a plain issue holder instead of constructing a ZodError. Measured 2974 / 3455 / 4540 locally plus 28 headroom.\n  // Also carries the lazy `safeParse` error: a getter and a setter on the failing result, and the async wrapper that keeps a sync throw a rejection. Measured 3021 / 3497 / 4581 locally plus 18 headroom.\n  // Also carries the symbol-key loop in `util.members`, which is what installs `$ZodProperties`'s `Symbol.iterator` on its prototype, and the memoizer's shared reference predicate. Measured 3036 / 3516 / 4611 locally plus 18 headroom.\n  // Also carries the guards `validate` parses under: an `aborted` check in each container loop, and two early exits in the generated object parser. Only a bundle containing a container pays — boolean is byte-identical to main and string measures three bytes smaller — so this ceiling rises by the measured +76 and the other two do not move. Measured 4696 locally plus 18 headroom.\n  \"zod-mini-object\": 4714,\n};\n\n/**\n * Identifiers that must not appear in a bundle that never uses the feature they\n * belong to. A byte ceiling drifts with unrelated churn; this does not, and it","diff":"@@ -52,11 +52,12 @@ const CEILINGS: Record<string, number> = {\n   // Also carries the declared symbol keys from #6448: `normalizeDef` collects the shape's own symbols and the parse loop walks them. Almost none of that is the `Reflect.ownKeys` conversions — reverting all eight of them measures a byte larger.\n   // Also carries the memoizer seam from #6482: each container init reads `globalConfig.memoizer` and calls `attach`, which is what lets `zod/mini` opt into cycle support. Measured 4512 locally but 4533 on CI — the gzip stream differs across zlib builds — so the headroom rides on the CI number.\n   // Also carries the Standard Schema issue bag: a failing `~standard.validate` parses with a plain issue holder instead of constructing a ZodError. Measured 2974 / 3455 / 4540 locally plus 28 headroom.\n   // Also carries the lazy `safeParse` error: a getter and a setter on the failing result, and the async wrapper that keeps a sync throw a rejection. Measured 3021 / 3497 / 4581 locally plus 18 headroom.\n   // Also carries the symbol-key loop in `util.members`, which is what installs `$ZodProperties`'s `Symbol.iterator` on its prototype, and the memoizer's shared reference predicate. Measured 3036 / 3516 / 4611 locally plus 18 headroom.\n-  \"zod-mini-object\": 4629,\n+  // Also carries the guards `validate` parses under: an `aborted` check in each container loop, and two early exits in the generated object parser. Only a bundle containing a container pays — boolean is byte-identical to main and string measures three bytes smaller — so this ceiling rises by the measured +76 and the other two do not move. Measured 4696 locally plus 18 headroom.\n+  \"zod-mini-object\": 4714,\n };\n \n /**\n  * Identifiers that must not appear in a bundle that never uses the feature they\n  * belong to. A byte ceiling drifts with unrelated churn; this does not, and it","label":{"defect":false,"category":"refactor","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"perf(v4): settle z.validate on the first failure in parse order (#6544)","issue_url":null,"pr_url":"https://github.com/colinhacks/zod/pull/6544"},"needs_manual_review":true}
{"id":"zod-07917f4-1","repo":"colinhacks/zod","license":"MIT","commit":"07917f4cb396646dff7a1364f9032a668b0d7eb4","parent":"62e6624bc72c4882968ac4ea2c4281b1a99a7ad4","file":"packages/zod/src/v4/classic/tests/error.test.ts","language":"typescript","hunk_header":"@@ -1046,6 +1046,37 @@ describe(\"safeParse builds the error on first read\", () => {","before":"      gc();\n    }\n    expect(live()).toBeLessThanOrEqual(1);\n    expect(results).toHaveLength(50);\n  });\n});","after":"      gc();\n    }\n    expect(live()).toBeLessThanOrEqual(1);\n    expect(results).toHaveLength(50);\n  });\n\n  test(\"no reader frames leak into the error, however late it is read\", async () => {\n    const result = z.string().safeParse(12) as { error: z.core.$ZodError };\n\n    // the constructor runs at THIS read: a macrotask later and twenty frames deeper than the parse\n    await new Promise((resolve) => setTimeout(resolve, 0));\n    const readDeep = <T>(n: number, make: () => T): T => (n ? readDeep(n - 1, make) : make());\n    const error = readDeep(20, () => result.error) as unknown as Error;\n\n    // control: a plain Error at that same depth does carry the frames, so the assertions below discriminate rather than pass vacuously\n    const control = readDeep(20, () => new Error(\"control\"));\n    expect(control.stack!.split(\"\\n\").some((line) => line.trim().startsWith(\"at \"))).toBe(true);\n    expect(control.stack!).toContain(\"readDeep\");\n\n    expect(error.stack!.startsWith(\"ZodError: [\")).toBe(true);\n    expect(error.stack!.split(\"\\n\").some((line) => line.trim().startsWith(\"at \"))).toBe(false);\n    expect(error.stack!).not.toContain(\"readDeep\");\n  });\n\n  test(\"a lazy build gives back the caller's stackTraceLimit, not the parse-time one\", () => {\n    const result = z.string().safeParse(12) as { error: z.core.$ZodError };\n    const ambient = Error.stackTraceLimit;\n    // changed between the parse and the read, so restoring a value captured at parse time would clobber it\n    Error.stackTraceLimit = ambient + 7;\n    try {\n      void result.error;\n      expect(Error.stackTraceLimit).toBe(ambient + 7);\n    } finally {\n      Error.stackTraceLimit = ambient;\n    }\n  });\n});","diff":"@@ -1046,6 +1046,37 @@ describe(\"safeParse builds the error on first read\", () => {\n       gc();\n     }\n     expect(live()).toBeLessThanOrEqual(1);\n     expect(results).toHaveLength(50);\n   });\n+\n+  test(\"no reader frames leak into the error, however late it is read\", async () => {\n+    const result = z.string().safeParse(12) as { error: z.core.$ZodError };\n+\n+    // the constructor runs at THIS read: a macrotask later and twenty frames deeper than the parse\n+    await new Promise((resolve) => setTimeout(resolve, 0));\n+    const readDeep = <T>(n: number, make: () => T): T => (n ? readDeep(n - 1, make) : make());\n+    const error = readDeep(20, () => result.error) as unknown as Error;\n+\n+    // control: a plain Error at that same depth does carry the frames, so the assertions below discriminate rather than pass vacuously\n+    const control = readDeep(20, () => new Error(\"control\"));\n+    expect(control.stack!.split(\"\\n\").some((line) => line.trim().startsWith(\"at \"))).toBe(true);\n+    expect(control.stack!).toContain(\"readDeep\");\n+\n+    expect(error.stack!.startsWith(\"ZodError: [\")).toBe(true);\n+    expect(error.stack!.split(\"\\n\").some((line) => line.trim().startsWith(\"at \"))).toBe(false);\n+    expect(error.stack!).not.toContain(\"readDeep\");\n+  });\n+\n+  test(\"a lazy build gives back the caller's stackTraceLimit, not the parse-time one\", () => {\n+    const result = z.string().safeParse(12) as { error: z.core.$ZodError };\n+    const ambient = Error.stackTraceLimit;\n+    // changed between the parse and the read, so restoring a value captured at parse time would clobber it\n+    Error.stackTraceLimit = ambient + 7;\n+    try {\n+      void result.error;\n+      expect(Error.stackTraceLimit).toBe(ambient + 7);\n+    } finally {\n+      Error.stackTraceLimit = ambient;\n+    }\n+  });\n });","label":{"defect":false,"category":"test","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"test(v4): pin the lazy safeParse error's stack behavior (#6548)","issue_url":null,"pr_url":"https://github.com/colinhacks/zod/pull/6548"},"needs_manual_review":true}
{"id":"zod-62e6624-1","repo":"colinhacks/zod","license":"MIT","commit":"62e6624bc72c4882968ac4ea2c4281b1a99a7ad4","parent":"cafbee4772ef481611fef6f5fb9ed9a1b9597069","file":"packages/zod/src/v4/classic/schemas.ts","language":"typescript","hunk_header":"@@ -76,10 +76,12 @@ export interface ZodType<","before":"  ): Promise<parse.ZodSafeParseResult<core.output<this>>>;\n  spa: (\n    data: unknown,\n    params?: core.ParseContext<core.$ZodIssue>\n  ) => Promise<parse.ZodSafeParseResult<core.output<this>>>;\n\n  // encoding/decoding\n  encode(data: core.output<this>, params?: core.ParseContext<core.$ZodIssue>): core.input<this>;\n  decode(data: core.input<this>, params?: core.ParseContext<core.$ZodIssue>): core.output<this>;\n  encodeAsync(data: core.output<this>, params?: core.ParseContext<core.$ZodIssue>): Promise<core.input<this>>;","after":"  ): Promise<parse.ZodSafeParseResult<core.output<this>>>;\n  spa: (\n    data: unknown,\n    params?: core.ParseContext<core.$ZodIssue>\n  ) => Promise<parse.ZodSafeParseResult<core.output<this>>>;\n  validate(data: unknown, params?: core.ParseContext<core.$ZodIssue>): data is core.input<this>;\n  validateAsync(data: unknown, params?: core.ParseContext<core.$ZodIssue>): Promise<boolean>;\n\n  // encoding/decoding\n  encode(data: core.output<this>, params?: core.ParseContext<core.$ZodIssue>): core.input<this>;\n  decode(data: core.input<this>, params?: core.ParseContext<core.$ZodIssue>): core.output<this>;\n  encodeAsync(data: core.output<this>, params?: core.ParseContext<core.$ZodIssue>): Promise<core.input<this>>;","diff":"@@ -76,10 +76,12 @@ export interface ZodType<\n   ): Promise<parse.ZodSafeParseResult<core.output<this>>>;\n   spa: (\n     data: unknown,\n     params?: core.ParseContext<core.$ZodIssue>\n   ) => Promise<parse.ZodSafeParseResult<core.output<this>>>;\n+  validate(data: unknown, params?: core.ParseContext<core.$ZodIssue>): data is core.input<this>;\n+  validateAsync(data: unknown, params?: core.ParseContext<core.$ZodIssue>): Promise<boolean>;\n \n   // encoding/decoding\n   encode(data: core.output<this>, params?: core.ParseContext<core.$ZodIssue>): core.input<this>;\n   decode(data: core.input<this>, params?: core.ParseContext<core.$ZodIssue>): core.output<this>;\n   encodeAsync(data: core.output<this>, params?: core.ParseContext<core.$ZodIssue>): Promise<core.input<this>>;","label":{"defect":false,"category":"feature","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"feat(v4): add .validate() and .validateAsync() to Zod Classic (#6547)","issue_url":null,"pr_url":"https://github.com/colinhacks/zod/pull/6547"},"needs_manual_review":true}
{"id":"zod-81ded99-1","repo":"colinhacks/zod","license":"MIT","commit":"81ded9918fe39d51b232be72f23ba0d5fb6a837e","parent":"51caf01038d3339d7b9db94b959482c549aba2e3","file":"packages/zod/src/v4/core/compile.ts","language":"typescript","hunk_header":"@@ -32,11 +32,15 @@ interface CompileFnOptions {","before":"  debug?: boolean | undefined;\n  /** Emit a validator instead of a parser: skip building the output value where nothing reads it. */\n  assertOnly?: boolean | undefined;\n}\n\ntype CompiledFn<T> = ((input: unknown) => T | INVALID) & { code?: string | undefined };\n\n/** Raised when the schema contains async refinements or transforms. Surfaces only under `compile(schema, { strict: true })`. */\nexport class ZodCompileAsyncError extends Error {\n  constructor(message = \"z.compile does not support async refinements, transforms, or checks\") {\n    super(message);","after":"  debug?: boolean | undefined;\n  /** Emit a validator instead of a parser: skip building the output value where nothing reads it. */\n  assertOnly?: boolean | undefined;\n}\n\ntype CompiledFn<T> = ((input: unknown) => T | INVALID) & {\n  code?: string | undefined;\n  /** False when this function's INVALID does not strictly mean \"the runtime would reject\". */\n  definite?: boolean | undefined;\n};\n\n/** Raised when the schema contains async refinements or transforms. Surfaces only under `compile(schema, { strict: true })`. */\nexport class ZodCompileAsyncError extends Error {\n  constructor(message = \"z.compile does not support async refinements, transforms, or checks\") {\n    super(message);","diff":"@@ -32,11 +32,15 @@ interface CompileFnOptions {\n   debug?: boolean | undefined;\n   /** Emit a validator instead of a parser: skip building the output value where nothing reads it. */\n   assertOnly?: boolean | undefined;\n }\n \n-type CompiledFn<T> = ((input: unknown) => T | INVALID) & { code?: string | undefined };\n+type CompiledFn<T> = ((input: unknown) => T | INVALID) & {\n+  code?: string | undefined;\n+  /** False when this function's INVALID does not strictly mean \"the runtime would reject\". */\n+  definite?: boolean | undefined;\n+};\n \n /** Raised when the schema contains async refinements or transforms. Surfaces only under `compile(schema, { strict: true })`. */\n export class ZodCompileAsyncError extends Error {\n   constructor(message = \"z.compile does not support async refinements, transforms, or checks\") {\n     super(message);","label":{"defect":false,"category":"refactor","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"perf: answer z.validate from the compiled fast path on invalid input (#6538)","issue_url":null,"pr_url":"https://github.com/colinhacks/zod/pull/6538"},"needs_manual_review":true}
{"id":"zod-51caf01-1","repo":"colinhacks/zod","license":"MIT","commit":"51caf01038d3339d7b9db94b959482c549aba2e3","parent":"abfb38977dc28dc9993e3fa6a2ecc2f52a8d0c4c","file":"packages/zod/src/v4/core/schemas.ts","language":"typescript","hunk_header":"@@ -2107,11 +2107,11 @@ export const $ZodObject: core.$constructor<$ZodObject> = /*@__PURE__*/ core.$con","before":"        return newSh;\n      },\n    });\n  }\n\n  const _normalized = util.cachedInternal(() => normalizeDef(def));\n\n  util.defineLazyInternal(inst, \"propValues\", (zod) => {\n    const shape = zod.def.shape;\n    const propValues: util.PropValues = {};\n    for (const key in shape) {","after":"        return newSh;\n      },\n    });\n  }\n\n  const _normalized = util.cached(() => normalizeDef(def));\n\n  util.defineLazyInternal(inst, \"propValues\", (zod) => {\n    const shape = zod.def.shape;\n    const propValues: util.PropValues = {};\n    for (const key in shape) {","diff":"@@ -2107,11 +2107,11 @@ export const $ZodObject: core.$constructor<$ZodObject> = /*@__PURE__*/ core.$con\n         return newSh;\n       },\n     });\n   }\n \n-  const _normalized = util.cachedInternal(() => normalizeDef(def));\n+  const _normalized = util.cached(() => normalizeDef(def));\n \n   util.defineLazyInternal(inst, \"propValues\", (zod) => {\n     const shape = zod.def.shape;\n     const propValues: util.PropValues = {};\n     for (const key in shape) {","label":{"defect":false,"category":"refactor","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"refactor: collapse cachedInternal back into cached (#6540)","issue_url":null,"pr_url":"https://github.com/colinhacks/zod/pull/6540"},"needs_manual_review":true}
{"id":"zod-abfb389-1","repo":"colinhacks/zod","license":"MIT","commit":"abfb38977dc28dc9993e3fa6a2ecc2f52a8d0c4c","parent":"69f2a7ff203e546e3a1350fb9152ace03ba0a6e6","file":"packages/treeshake/bundle-size.test.ts","language":"typescript","hunk_header":"@@ -39,21 +39,24 @@ const BUILT_ENTRY = path.join(__dirname, \"node_modules\", \"zod\", \"index.js\");","before":" */\nconst CEILINGS: Record<string, number> = {\n  // raised from 2903 / 3366 / 4437 by the commit that caused it: installing a trait's members from `$constructor` rather than from its initializer costs mini +70 / +83 / +95, and every bundle carries `core.ts`; measured 2946 / 3420 / 4503 plus 28 headroom, and the per-fixture notes below record what each already carried. About half of it is the guard that keeps the install off a user subclass's prototype; the rest is writing the members as `this`-methods, since `this` is not renameable and a block body cannot collapse to an arrow.\n  // Also carries the Standard Schema issue bag: a failing `~standard.validate` parses with a plain issue holder instead of constructing a ZodError. Measured 2974 / 3455 / 4540 locally plus 28 headroom.\n  // Also carries the lazy `safeParse` error: a getter and a setter on the failing result, and the async wrapper that keeps a sync throw a rejection. Measured 3021 / 3497 / 4581 locally plus 18 headroom.\n  \"zod-mini-boolean\": 3039,\n  // Also carries the code-point string length scan: `.min`/`.max`/`.length` on a string pulls in the surrogate walk.\n  // Also carries the Standard Schema issue bag: a failing `~standard.validate` parses with a plain issue holder instead of constructing a ZodError. Measured 2974 / 3455 / 4540 locally plus 28 headroom.\n  // Also carries the lazy `safeParse` error: a getter and a setter on the failing result, and the async wrapper that keeps a sync throw a rejection. Measured 3021 / 3497 / 4581 locally plus 18 headroom.\n  \"zod-mini-string\": 3518,\n  // Also carries the construction-time discriminator check, which writes a WeakMap entry from `$ZodObject`, so every bundle containing `z.object` pays for it whether or not it builds a discriminated union.\n  // Also carries the declared symbol keys from #6448: `normalizeDef` collects the shape's own symbols and the parse loop walks them. Almost none of that is the `Reflect.ownKeys` conversions — reverting all eight of them measures a byte larger.\n  // Also carries the memoizer seam from #6482: each container init reads `globalConfig.memoizer` and calls `attach`, which is what lets `zod/mini` opt into cycle support. Measured 4512 locally but 4533 on CI — the gzip stream differs across zlib builds — so the headroom rides on the CI number.\n  // Also carries the Standard Schema issue bag: a failing `~standard.validate` parses with a plain issue holder instead of constructing a ZodError. Measured 2974 / 3455 / 4540 locally plus 28 headroom.\n  // Also carries the lazy `safeParse` error: a getter and a setter on the failing result, and the async wrapper that keeps a sync throw a rejection. Measured 3021 / 3497 / 4581 locally plus 18 headroom.\n  \"zod-mini-object\": 4601,\n};\n\n/**\n * Identifiers that must not appear in a bundle that never uses the feature they\n * belong to. A byte ceiling drifts with unrelated churn; this does not, and it","after":" */\nconst CEILINGS: Record<string, number> = {\n  // raised from 2903 / 3366 / 4437 by the commit that caused it: installing a trait's members from `$constructor` rather than from its initializer costs mini +70 / +83 / +95, and every bundle carries `core.ts`; measured 2946 / 3420 / 4503 plus 28 headroom, and the per-fixture notes below record what each already carried. About half of it is the guard that keeps the install off a user subclass's prototype; the rest is writing the members as `this`-methods, since `this` is not renameable and a block body cannot collapse to an arrow.\n  // Also carries the Standard Schema issue bag: a failing `~standard.validate` parses with a plain issue holder instead of constructing a ZodError. Measured 2974 / 3455 / 4540 locally plus 28 headroom.\n  // Also carries the lazy `safeParse` error: a getter and a setter on the failing result, and the async wrapper that keeps a sync throw a rejection. Measured 3021 / 3497 / 4581 locally plus 18 headroom.\n  // Also carries the symbol-key loop in `util.members`, which is what installs `$ZodProperties`'s `Symbol.iterator` on its prototype, and the memoizer's shared reference predicate. Measured 3036 / 3516 / 4611 locally plus 18 headroom.\n  \"zod-mini-boolean\": 3054,\n  // Also carries the code-point string length scan: `.min`/`.max`/`.length` on a string pulls in the surrogate walk.\n  // Also carries the Standard Schema issue bag: a failing `~standard.validate` parses with a plain issue holder instead of constructing a ZodError. Measured 2974 / 3455 / 4540 locally plus 28 headroom.\n  // Also carries the lazy `safeParse` error: a getter and a setter on the failing result, and the async wrapper that keeps a sync throw a rejection. Measured 3021 / 3497 / 4581 locally plus 18 headroom.\n  // Also carries the symbol-key loop in `util.members`, which is what installs `$ZodProperties`'s `Symbol.iterator` on its prototype, and the memoizer's shared reference predicate. Measured 3036 / 3516 / 4611 locally plus 18 headroom.\n  \"zod-mini-string\": 3534,\n  // Also carries the construction-time discriminator check, which writes a WeakMap entry from `$ZodObject`, so every bundle containing `z.object` pays for it whether or not it builds a discriminated union.\n  // Also carries the declared symbol keys from #6448: `normalizeDef` collects the shape's own symbols and the parse loop walks them. Almost none of that is the `Reflect.ownKeys` conversions — reverting all eight of them measures a byte larger.\n  // Also carries the memoizer seam from #6482: each container init reads `globalConfig.memoizer` and calls `attach`, which is what lets `zod/mini` opt into cycle support. Measured 4512 locally but 4533 on CI — the gzip stream differs across zlib builds — so the headroom rides on the CI number.\n  // Also carries the Standard Schema issue bag: a failing `~standard.validate` parses with a plain issue holder instead of constructing a ZodError. Measured 2974 / 3455 / 4540 locally plus 28 headroom.\n  // Also carries the lazy `safeParse` error: a getter and a setter on the failing result, and the async wrapper that keeps a sync throw a rejection. Measured 3021 / 3497 / 4581 locally plus 18 headroom.\n  // Also carries the symbol-key loop in `util.members`, which is what installs `$ZodProperties`'s `Symbol.iterator` on its prototype, and the memoizer's shared reference predicate. Measured 3036 / 3516 / 4611 locally plus 18 headroom.\n  \"zod-mini-object\": 4629,\n};\n\n/**\n * Identifiers that must not appear in a bundle that never uses the feature they\n * belong to. A byte ceiling drifts with unrelated churn; this does not, and it","diff":"@@ -39,21 +39,24 @@ const BUILT_ENTRY = path.join(__dirname, \"node_modules\", \"zod\", \"index.js\");\n  */\n const CEILINGS: Record<string, number> = {\n   // raised from 2903 / 3366 / 4437 by the commit that caused it: installing a trait's members from `$constructor` rather than from its initializer costs mini +70 / +83 / +95, and every bundle carries `core.ts`; measured 2946 / 3420 / 4503 plus 28 headroom, and the per-fixture notes below record what each already carried. About half of it is the guard that keeps the install off a user subclass's prototype; the rest is writing the members as `this`-methods, since `this` is not renameable and a block body cannot collapse to an arrow.\n   // Also carries the Standard Schema issue bag: a failing `~standard.validate` parses with a plain issue holder instead of constructing a ZodError. Measured 2974 / 3455 / 4540 locally plus 28 headroom.\n   // Also carries the lazy `safeParse` error: a getter and a setter on the failing result, and the async wrapper that keeps a sync throw a rejection. Measured 3021 / 3497 / 4581 locally plus 18 headroom.\n-  \"zod-mini-boolean\": 3039,\n+  // Also carries the symbol-key loop in `util.members`, which is what installs `$ZodProperties`'s `Symbol.iterator` on its prototype, and the memoizer's shared reference predicate. Measured 3036 / 3516 / 4611 locally plus 18 headroom.\n+  \"zod-mini-boolean\": 3054,\n   // Also carries the code-point string length scan: `.min`/`.max`/`.length` on a string pulls in the surrogate walk.\n   // Also carries the Standard Schema issue bag: a failing `~standard.validate` parses with a plain issue holder instead of constructing a ZodError. Measured 2974 / 3455 / 4540 locally plus 28 headroom.\n   // Also carries the lazy `safeParse` error: a getter and a setter on the failing result, and the async wrapper that keeps a sync throw a rejection. Measured 3021 / 3497 / 4581 locally plus 18 headroom.\n-  \"zod-mini-string\": 3518,\n+  // Also carries the symbol-key loop in `util.members`, which is what installs `$ZodProperties`'s `Symbol.iterator` on its prototype, and the memoizer's shared reference predicate. Measured 3036 / 3516 / 4611 locally plus 18 headroom.\n+  \"zod-mini-string\": 3534,\n   // Also carries the construction-time discriminator check, which writes a WeakMap entry from `$ZodObject`, so every bundle containing `z.object` pays for it whether or not it builds a discriminated union.\n   // Also carries the declared symbol keys from #6448: `normalizeDef` collects the shape's own symbols and the parse loop walks them. Almost none of that is the `Reflect.ownKeys` conversions — reverting all eight of them measures a byte larger.\n   // Also carries the memoizer seam from #6482: each container init reads `globalConfig.memoizer` and calls `attach`, which is what lets `zod/mini` opt into cycle support. Measured 4512 locally but 4533 on CI — the gzip stream differs across zlib builds — so the headroom rides on the CI number.\n   // Also carries the Standard Schema issue bag: a failing `~standard.validate` parses with a plain issue holder instead of constructing a ZodError. Measured 2974 / 3455 / 4540 locally plus 28 headroom.\n   // Also carries the lazy `safeParse` error: a getter and a setter on the failing result, and the async wrapper that keeps a sync throw a rejection. Measured 3021 / 3497 / 4581 locally plus 18 headroom.\n-  \"zod-mini-object\": 4601,\n+  // Also carries the symbol-key loop in `util.members`, which is what installs `$ZodProperties`'s `Symbol.iterator` on its prototype, and the memoizer's shared reference predicate. Measured 3036 / 3516 / 4611 locally plus 18 headroom.\n+  \"zod-mini-object\": 4629,\n };\n \n /**\n  * Identifiers that must not appear in a bundle that never uses the feature they\n  * belong to. A byte ceiling drifts with unrelated churn; this does not, and it","label":{"defect":false,"category":"feature","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"feat(v4): make z.properties() a schema, and give z.instanceof() a .properties() method (#6536)","issue_url":null,"pr_url":"https://github.com/colinhacks/zod/pull/6536"},"needs_manual_review":true}
{"id":"zod-bf99021-1","repo":"colinhacks/zod","license":"MIT","commit":"bf9902164b5396449540415c2221a24bc9fd0e22","parent":"bec73bea5a8ed19daec15655598fe5501bb797e7","file":"packages/zod/src/v4/core/schemas.ts","language":"typescript","hunk_header":"@@ -2106,11 +2106,11 @@ export const $ZodObject: core.$constructor<$ZodObject> = /*@__PURE__*/ core.$con","before":"        return newSh;\n      },\n    });\n  }\n\n  const _normalized = util.cached(() => normalizeDef(def));\n\n  util.defineLazyInternal(inst, \"propValues\", (zod) => {\n    const shape = zod.def.shape;\n    const propValues: util.PropValues = {};\n    for (const key in shape) {","after":"        return newSh;\n      },\n    });\n  }\n\n  const _normalized = util.cachedInternal(() => normalizeDef(def));\n\n  util.defineLazyInternal(inst, \"propValues\", (zod) => {\n    const shape = zod.def.shape;\n    const propValues: util.PropValues = {};\n    for (const key in shape) {","diff":"@@ -2106,11 +2106,11 @@ export const $ZodObject: core.$constructor<$ZodObject> = /*@__PURE__*/ core.$con\n         return newSh;\n       },\n     });\n   }\n \n-  const _normalized = util.cached(() => normalizeDef(def));\n+  const _normalized = util.cachedInternal(() => normalizeDef(def));\n \n   util.defineLazyInternal(inst, \"propValues\", (zod) => {\n     const shape = zod.def.shape;\n     const propValues: util.PropValues = {};\n     for (const key in shape) {","label":{"defect":false,"category":"refactor","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"perf: move util.cached's accessor to a prototype (#6537)","issue_url":null,"pr_url":"https://github.com/colinhacks/zod/pull/6537"},"needs_manual_review":true}
{"id":"vitest-a0a9396-1","repo":"vitest-dev/vitest","license":"MIT","commit":"a0a939653bc8441848579bb6ac18706372242cd6","parent":"42523289e9158b7a7f413ec3641ee0393c81eaac","file":"packages/spy/src/index.ts","language":"typescript","hunk_header":"@@ -23,10 +23,11 @@ export function isMockFunction(fn: any): fn is Mock {","before":"const MOCK_RESTORE = new Set<() => void>()\n// Jest keeps the state in a separate WeakMap which is good for memory,\n// but it makes the state slower to access and return different values\n// if you stored it before calling `mockClear` where it will be recreated\nconst DIRTY_MOCK_STATES = new Set<Mock<Procedure | Constructable>>()\nconst REGISTERED_MOCKS = new Set<WeakRef<Mock<Procedure | Constructable>>>()\nconst MOCK_FINALIZER = new FinalizationRegistry<WeakRef<Mock<Procedure | Constructable>>>((ref) => {\n  REGISTERED_MOCKS.delete(ref)\n})\nconst MOCK_CONFIGS = new WeakMap<Mock<Procedure | Constructable>, MockConfig>()","after":"const MOCK_RESTORE = new Set<() => void>()\n// Jest keeps the state in a separate WeakMap which is good for memory,\n// but it makes the state slower to access and return different values\n// if you stored it before calling `mockClear` where it will be recreated\nconst DIRTY_MOCK_STATES = new Set<Mock<Procedure | Constructable>>()\nconst addDirtyMock = DIRTY_MOCK_STATES.add.bind(DIRTY_MOCK_STATES)\nconst REGISTERED_MOCKS = new Set<WeakRef<Mock<Procedure | Constructable>>>()\nconst MOCK_FINALIZER = new FinalizationRegistry<WeakRef<Mock<Procedure | Constructable>>>((ref) => {\n  REGISTERED_MOCKS.delete(ref)\n})\nconst MOCK_CONFIGS = new WeakMap<Mock<Procedure | Constructable>, MockConfig>()","diff":"@@ -23,10 +23,11 @@ export function isMockFunction(fn: any): fn is Mock {\n const MOCK_RESTORE = new Set<() => void>()\n // Jest keeps the state in a separate WeakMap which is good for memory,\n // but it makes the state slower to access and return different values\n // if you stored it before calling `mockClear` where it will be recreated\n const DIRTY_MOCK_STATES = new Set<Mock<Procedure | Constructable>>()\n+const addDirtyMock = DIRTY_MOCK_STATES.add.bind(DIRTY_MOCK_STATES)\n const REGISTERED_MOCKS = new Set<WeakRef<Mock<Procedure | Constructable>>>()\n const MOCK_FINALIZER = new FinalizationRegistry<WeakRef<Mock<Procedure | Constructable>>>((ref) => {\n   REGISTERED_MOCKS.delete(ref)\n })\n const MOCK_CONFIGS = new WeakMap<Mock<Procedure | Constructable>, MockConfig>()","label":{"defect":true,"category":"bugfix","touches_public_api":true,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix(spy): fix stack overflow when spying `Set.prototype.add` (#11299)","issue_url":"https://github.com/vitest-dev/vitest/issues/11298","pr_url":"https://github.com/vitest-dev/vitest/pull/11299"},"needs_manual_review":true}
{"id":"vitest-4252328-1","repo":"vitest-dev/vitest","license":"MIT","commit":"42523289e9158b7a7f413ec3641ee0393c81eaac","parent":"5b95efb6dfe378dc6fc8380f5b1e96832f019a5d","file":"packages/expect/src/jest-utils.ts","language":"typescript","hunk_header":"@@ -75,16 +75,19 @@ function asymmetricMatch(a: any, b: any, customTesters: Array<Tester>) {","before":"\n  if (asymmetricA && asymmetricB) {\n    return undefined\n  }\n\n  if (asymmetricA) {\n    return a.asymmetricMatch(b, customTesters)\n  }\n\n  if (asymmetricB) {\n    return b.asymmetricMatch(a, customTesters)\n  }\n}\n\n// https://github.com/jestjs/jest/blob/905bcbced3d40cdf7aadc4cdf6fb731c4bb3dbe3/packages/expect-utils/src/utils.ts#L509\nexport function isError(value: unknown): value is Error {","after":"\n  if (asymmetricA && asymmetricB) {\n    return undefined\n  }\n\n  // Asymmetric matchers define their own subset semantics.\n  const testers = customTesters.filter(tester => !isSubsetEqualityTester(tester))\n\n  if (asymmetricA) {\n    return a.asymmetricMatch(b, testers)\n  }\n\n  if (asymmetricB) {\n    return b.asymmetricMatch(a, testers)\n  }\n}\n\n// https://github.com/jestjs/jest/blob/905bcbced3d40cdf7aadc4cdf6fb731c4bb3dbe3/packages/expect-utils/src/utils.ts#L509\nexport function isError(value: unknown): value is Error {","diff":"@@ -75,16 +75,19 @@ function asymmetricMatch(a: any, b: any, customTesters: Array<Tester>) {\n \n   if (asymmetricA && asymmetricB) {\n     return undefined\n   }\n \n+  // Asymmetric matchers define their own subset semantics.\n+  const testers = customTesters.filter(tester => !isSubsetEqualityTester(tester))\n+\n   if (asymmetricA) {\n-    return a.asymmetricMatch(b, customTesters)\n+    return a.asymmetricMatch(b, testers)\n   }\n \n   if (asymmetricB) {\n-    return b.asymmetricMatch(a, customTesters)\n+    return b.asymmetricMatch(a, testers)\n   }\n }\n \n // https://github.com/jestjs/jest/blob/905bcbced3d40cdf7aadc4cdf6fb731c4bb3dbe3/packages/expect-utils/src/utils.ts#L509\n export function isError(value: unknown): value is Error {","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix(expect): fix `toMatchObject` with asymmetric matchers (#11100)","issue_url":"https://github.com/vitest-dev/vitest/issues/11071","pr_url":"https://github.com/vitest-dev/vitest/pull/11100"},"needs_manual_review":true}
{"id":"vitest-d1c3ecc-1","repo":"vitest-dev/vitest","license":"MIT","commit":"d1c3ecc932dcb4cd50420c54aa1f7fa3051deac7","parent":"e8e556ff7c97fb2ea881868667564028dc824cf4","file":"packages/vitest/src/integrations/env/jsdom.ts","language":"typescript","hunk_header":"@@ -319,12 +319,13 @@ function createCompatUtils(window: DOMWindow): CompatUtils {","before":"        }\n      })\n      return nodeFormData\n    },\n    makeCompatBlob(blob: Blob) {\n      const buffer = (blob as any)[implSymbol]._buffer\n      return new NodeBlob_([buffer], { type: blob.type })\n    },\n  }\n  return utils\n}\n","after":"        }\n      })\n      return nodeFormData\n    },\n    makeCompatBlob(blob: Blob) {\n      const impl = (blob as any)[implSymbol]\n      // jsdom 28 renamed `_buffer` to `_bytes`\n      return new NodeBlob_([impl._bytes ?? impl._buffer], { type: blob.type })\n    },\n  }\n  return utils\n}\n","diff":"@@ -319,12 +319,13 @@ function createCompatUtils(window: DOMWindow): CompatUtils {\n         }\n       })\n       return nodeFormData\n     },\n     makeCompatBlob(blob: Blob) {\n-      const buffer = (blob as any)[implSymbol]._buffer\n-      return new NodeBlob_([buffer], { type: blob.type })\n+      const impl = (blob as any)[implSymbol]\n+      // jsdom 28 renamed `_buffer` to `_bytes`\n+      return new NodeBlob_([impl._bytes ?? impl._buffer], { type: blob.type })\n     },\n   }\n   return utils\n }\n ","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix(jsdom): fix `Request` with `Blob` body on jsdom 28+ (#11295)","issue_url":"https://github.com/vitest-dev/vitest/issues/11294","pr_url":"https://github.com/vitest-dev/vitest/pull/11295"},"needs_manual_review":true}
{"id":"vitest-a47d790-1","repo":"vitest-dev/vitest","license":"MIT","commit":"a47d7908f0635e9cba6cbe5e1a209bb6add5e0ab","parent":"05982297d0fb046d41e7accd89064f900071dd0a","file":"packages/vitest/src/integrations/mock/timers.ts","language":"typescript","hunk_header":"@@ -167,10 +167,18 @@ export class FakeTimers {","before":"    if (toFake === undefined && toNotFake === undefined) {\n      // Do not mock timers internally used by node by default. It can still be mocked through userConfig.\n      toFake = (Object.keys(this._fakeTimers.timers) as FakeMethod[])\n        .filter(timer => timer !== 'nextTick' && timer !== 'queueMicrotask')\n    }\n    if (isChildProcess() && toNotFake && !toNotFake.includes('nextTick')) {\n      toNotFake = [...toNotFake, 'nextTick']\n    }\n\n    this._clock = this._fakeTimers.install({","after":"    if (toFake === undefined && toNotFake === undefined) {\n      // Do not mock timers internally used by node by default. It can still be mocked through userConfig.\n      toFake = (Object.keys(this._fakeTimers.timers) as FakeMethod[])\n        .filter(timer => timer !== 'nextTick' && timer !== 'queueMicrotask')\n    }\n    else if (toFake === undefined && toNotFake !== undefined) {\n      // Do not mock timers internally used by node via `toNotFake`\n      for (const timer of ['nextTick', 'queueMicrotask'] as const) {\n        if (!toNotFake.includes(timer)) {\n          toNotFake = [...toNotFake, timer]\n        }\n      }\n    }\n    if (isChildProcess() && toNotFake && !toNotFake.includes('nextTick')) {\n      toNotFake = [...toNotFake, 'nextTick']\n    }\n\n    this._clock = this._fakeTimers.install({","diff":"@@ -167,10 +167,18 @@ export class FakeTimers {\n     if (toFake === undefined && toNotFake === undefined) {\n       // Do not mock timers internally used by node by default. It can still be mocked through userConfig.\n       toFake = (Object.keys(this._fakeTimers.timers) as FakeMethod[])\n         .filter(timer => timer !== 'nextTick' && timer !== 'queueMicrotask')\n     }\n+    else if (toFake === undefined && toNotFake !== undefined) {\n+      // Do not mock timers internally used by node via `toNotFake`\n+      for (const timer of ['nextTick', 'queueMicrotask'] as const) {\n+        if (!toNotFake.includes(timer)) {\n+          toNotFake = [...toNotFake, timer]\n+        }\n+      }\n+    }\n     if (isChildProcess() && toNotFake && !toNotFake.includes('nextTick')) {\n       toNotFake = [...toNotFake, 'nextTick']\n     }\n \n     this._clock = this._fakeTimers.install({","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix(fakeTimers): force `queueMicrotask` and `nextTick` in `toNotFake` (#11261)","issue_url":"https://github.com/vitest-dev/vitest/issues/11226","pr_url":"https://github.com/vitest-dev/vitest/pull/11261"},"needs_manual_review":true}
{"id":"vitest-91ab158-1","repo":"vitest-dev/vitest","license":"MIT","commit":"91ab1588c72c9c5f7c638368a541cd20e0e8a934","parent":"07670364b45e61f2c542d332a232fe81e4f88d82","file":"packages/vitest/src/node/cli/doctor.ts","language":"typescript","hunk_header":"@@ -47,11 +47,12 @@ export interface DoctorCandidate {","before":"   * only shows once a persistent cache is populated.\n   */\n  primeRuns?: number\n}\n\nconst DOM_ENVIRONMENTS = new Set(['jsdom', 'happy-dom'])\n\n/**\n * Builds the list of configurations worth measuring for this config. The\n * candidates are pruned by what the config already uses - there is no point\n * in measuring `threads` when the config already runs in `threads`.","after":"   * only shows once a persistent cache is populated.\n   */\n  primeRuns?: number\n}\n\n// custom environments are measured too: the run fails if they lack setupVM\nconst ENVIRONMENTS_WITHOUT_VM_UPSIDE = new Set(['node', 'edge-runtime'])\n\n/**\n * Builds the list of configurations worth measuring for this config. The\n * candidates are pruned by what the config already uses - there is no point\n * in measuring `threads` when the config already runs in `threads`.","diff":"@@ -47,11 +47,12 @@ export interface DoctorCandidate {\n    * only shows once a persistent cache is populated.\n    */\n   primeRuns?: number\n }\n \n-const DOM_ENVIRONMENTS = new Set(['jsdom', 'happy-dom'])\n+// custom environments are measured too: the run fails if they lack setupVM\n+const ENVIRONMENTS_WITHOUT_VM_UPSIDE = new Set(['node', 'edge-runtime'])\n \n /**\n  * Builds the list of configurations worth measuring for this config. The\n  * candidates are pruned by what the config already uses - there is no point\n  * in measuring `threads` when the config already runs in `threads`.","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix(doctor): measure vm pools for custom environments (#11212)","issue_url":"https://github.com/vitest-dev/vitest/issues/11192","pr_url":"https://github.com/vitest-dev/vitest/pull/11212"},"needs_manual_review":true}
{"id":"vitest-17e2b22-1","repo":"vitest-dev/vitest","license":"MIT","commit":"17e2b22dd018a9098a597d835bf45d1a7a3e73c8","parent":"73614654a46f617c357920eecda439654a561f92","file":"packages/snapshot/src/port/state.ts","language":"typescript","hunk_header":"@@ -148,11 +148,14 @@ export default class SnapshotState {","before":"  markSnapshotsAsCheckedForTest(testName: string): void {\n    this._uncheckedKeys.forEach((uncheckedKey) => {\n      // skip snapshots with following keys\n      //   testName n\n      //   testName > xxx n (this is for toMatchSnapshot(\"xxx\") API)\n      if (/ \\d+$| > /.test(uncheckedKey.slice(testName.length))) {\n        this._uncheckedKeys.delete(uncheckedKey)\n      }\n    })\n  }\n","after":"  markSnapshotsAsCheckedForTest(testName: string): void {\n    this._uncheckedKeys.forEach((uncheckedKey) => {\n      // skip snapshots with following keys\n      //   testName n\n      //   testName > xxx n (this is for toMatchSnapshot(\"xxx\") API)\n      if (\n        uncheckedKey.startsWith(testName)\n        && /^ \\d+$|^ > /.test(uncheckedKey.slice(testName.length))\n      ) {\n        this._uncheckedKeys.delete(uncheckedKey)\n      }\n    })\n  }\n","diff":"@@ -148,11 +148,14 @@ export default class SnapshotState {\n   markSnapshotsAsCheckedForTest(testName: string): void {\n     this._uncheckedKeys.forEach((uncheckedKey) => {\n       // skip snapshots with following keys\n       //   testName n\n       //   testName > xxx n (this is for toMatchSnapshot(\"xxx\") API)\n-      if (/ \\d+$| > /.test(uncheckedKey.slice(testName.length))) {\n+      if (\n+        uncheckedKey.startsWith(testName)\n+        && /^ \\d+$|^ > /.test(uncheckedKey.slice(testName.length))\n+      ) {\n         this._uncheckedKeys.delete(uncheckedKey)\n       }\n     })\n   }\n ","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix(snapshot): report obsolete keys next to skipped tests (fix #11157) (#11158)","issue_url":"https://github.com/vitest-dev/vitest/issues/11157","pr_url":"https://github.com/vitest-dev/vitest/pull/11158"},"needs_manual_review":true}
{"id":"vitest-7361465-1","repo":"vitest-dev/vitest","license":"MIT","commit":"73614654a46f617c357920eecda439654a561f92","parent":"972e24bab5fb96843de72447eb65585628b8dbde","file":"packages/vitest/src/node/cache/fsModuleCache.ts","language":"typescript","hunk_header":"@@ -316,10 +316,28 @@ export class FileSystemModuleCache {","before":"      return JSON.parse(content)\n    }\n    catch {}\n  }\n\n  // before vitest starts running tests, we check that the lockfile wasn't updated\n  // if it was, we nuke the previous cache in case a custom plugin was updated\n  // or a new version of vite/vitest is installed\n  // for the same reason we also cache config file content, but that won't catch changes made in external plugins\n  public async ensureCacheIntegrity(): Promise<void> {","after":"      return JSON.parse(content)\n    }\n    catch {}\n  }\n\n  private async writeMetadata(lockfileHash: string): Promise<void> {\n    try {\n      if (!existsSync(this.rootCache)) {\n        mkdirSync(this.rootCache, { recursive: true })\n      }\n      await writeFile(\n        this.metadataFilePath,\n        JSON.stringify({ lockfileHash }, null, 2),\n        'utf-8',\n      )\n    }\n    catch (error) {\n      // Recording the metadata is best-effort and losing the file shouldn't\n      // abort the entire execution\n      debugFs?.(`failed to write fs cache metadata: ${error}`)\n    }\n  }\n\n  // before vitest starts running tests, we check that the lockfile wasn't updated\n  // if it was, we nuke the previous cache in case a custom plugin was updated\n  // or a new version of vite/vitest is installed\n  // for the same reason we also cache config file content, but that won't catch changes made in external plugins\n  public async ensureCacheIntegrity(): Promise<void> {","diff":"@@ -316,10 +316,28 @@ export class FileSystemModuleCache {\n       return JSON.parse(content)\n     }\n     catch {}\n   }\n \n+  private async writeMetadata(lockfileHash: string): Promise<void> {\n+    try {\n+      if (!existsSync(this.rootCache)) {\n+        mkdirSync(this.rootCache, { recursive: true })\n+      }\n+      await writeFile(\n+        this.metadataFilePath,\n+        JSON.stringify({ lockfileHash }, null, 2),\n+        'utf-8',\n+      )\n+    }\n+    catch (error) {\n+      // Recording the metadata is best-effort and losing the file shouldn't\n+      // abort the entire execution\n+      debugFs?.(`failed to write fs cache metadata: ${error}`)\n+    }\n+  }\n+\n   // before vitest starts running tests, we check that the lockfile wasn't updated\n   // if it was, we nuke the previous cache in case a custom plugin was updated\n   // or a new version of vite/vitest is installed\n   // for the same reason we also cache config file content, but that won't catch changes made in external plugins\n   public async ensureCacheIntegrity(): Promise<void> {","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix: keep metadata file when clearing the cache (#11199)","issue_url":"https://github.com/vitest-dev/vitest/issues/11193","pr_url":"https://github.com/vitest-dev/vitest/pull/11199"},"needs_manual_review":true}
{"id":"vitest-972e24b-1","repo":"vitest-dev/vitest","license":"MIT","commit":"972e24bab5fb96843de72447eb65585628b8dbde","parent":"5dc4b5c925e8e3ccc3e2ddf0ff31cb4c1f4447ca","file":"packages/vitest/src/node/plugins/testConfig.ts","language":"typescript","hunk_header":"@@ -147,17 +147,17 @@ export function TestConfigPlugin(","before":"            delete config.define['import.meta.vitest']\n          }\n          // We inject the defines at runtime in non-browser tests,\n          // but keep the original behaviour in the browser mode\n          const resolvedTestConfig = testConfig as ResolvedConfig\n          if (sharedServer) {\n            resolvedTestConfig.defines = sharedServer.defines\n            resolvedTestConfig._scriptDefines = sharedServer.scriptDefines\n          }\n          else if (isBrowserEnabled) {\n            resolvedTestConfig.defines = config.define || {}\n          }\n          else {\n            const { defines, scriptDefines } = deleteDefineConfig(config)\n            resolvedTestConfig.defines = defines\n            resolvedTestConfig._scriptDefines = scriptDefines\n          }","after":"            delete config.define['import.meta.vitest']\n          }\n          // We inject the defines at runtime in non-browser tests,\n          // but keep the original behaviour in the browser mode\n          const resolvedTestConfig = testConfig as ResolvedConfig\n          if (isBrowserEnabled) {\n            resolvedTestConfig.defines = {}\n          }\n          else if (sharedServer) {\n            resolvedTestConfig.defines = sharedServer.defines\n            resolvedTestConfig._scriptDefines = sharedServer.scriptDefines\n          }\n          else {\n            const { defines, scriptDefines } = deleteDefineConfig(config)\n            resolvedTestConfig.defines = defines\n            resolvedTestConfig._scriptDefines = scriptDefines\n          }","diff":"@@ -147,17 +147,17 @@ export function TestConfigPlugin(\n             delete config.define['import.meta.vitest']\n           }\n           // We inject the defines at runtime in non-browser tests,\n           // but keep the original behaviour in the browser mode\n           const resolvedTestConfig = testConfig as ResolvedConfig\n-          if (sharedServer) {\n+          if (isBrowserEnabled) {\n+            resolvedTestConfig.defines = {}\n+          }\n+          else if (sharedServer) {\n             resolvedTestConfig.defines = sharedServer.defines\n             resolvedTestConfig._scriptDefines = sharedServer.scriptDefines\n           }\n-          else if (isBrowserEnabled) {\n-            resolvedTestConfig.defines = config.define || {}\n-          }\n           else {\n             const { defines, scriptDefines } = deleteDefineConfig(config)\n             resolvedTestConfig.defines = defines\n             resolvedTestConfig._scriptDefines = scriptDefines\n           }","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix(browser): avoid double quotes in `config.define` (#11198)","issue_url":"https://github.com/vitest-dev/vitest/issues/11164","pr_url":"https://github.com/vitest-dev/vitest/pull/11198"},"needs_manual_review":true}
{"id":"vitest-b426c19-1","repo":"vitest-dev/vitest","license":"MIT","commit":"b426c1976d32310c271d053b814bafb9a99bcd89","parent":"99fc5259175cf09671a0ed75ec476f06507a2605","file":"packages/vitest/src/runtime/moduleRunner/cachedResolver.ts","language":"typescript","hunk_header":"@@ -7,10 +7,18 @@ import { distDir } from '../../paths'","before":"const bareVitestRegexp = /^@?vitest(?:\\/|$)/\nconst normalizedDistDir = normalize(distDir)\nconst relativeIds: Record<string, string> = {}\nconst externalizeMap = new Map<string, string>()\n\n// all Vitest imports always need to be externalized\nexport function getCachedVitestImport(\n  id: string,\n  state: () => WorkerGlobalState,\n): null | { externalize: string; type: 'module' } {","after":"const bareVitestRegexp = /^@?vitest(?:\\/|$)/\nconst normalizedDistDir = normalize(distDir)\nconst relativeIds: Record<string, string> = {}\nconst externalizeMap = new Map<string, string>()\n\nfunction getRelativeDistDir(root: string): string {\n  const normalizedRoot = normalize(root).replace(/\\/+$/, '') || '/'\n  const rootPrefix = normalizedRoot === '/' ? normalizedRoot : `${normalizedRoot}/`\n  return normalizedDistDir.startsWith(rootPrefix)\n    ? normalizedDistDir.slice(normalizedRoot.length)\n    : ''\n}\n\n// all Vitest imports always need to be externalized\nexport function getCachedVitestImport(\n  id: string,\n  state: () => WorkerGlobalState,\n): null | { externalize: string; type: 'module' } {","diff":"@@ -7,10 +7,18 @@ import { distDir } from '../../paths'\n const bareVitestRegexp = /^@?vitest(?:\\/|$)/\n const normalizedDistDir = normalize(distDir)\n const relativeIds: Record<string, string> = {}\n const externalizeMap = new Map<string, string>()\n \n+function getRelativeDistDir(root: string): string {\n+  const normalizedRoot = normalize(root).replace(/\\/+$/, '') || '/'\n+  const rootPrefix = normalizedRoot === '/' ? normalizedRoot : `${normalizedRoot}/`\n+  return normalizedDistDir.startsWith(rootPrefix)\n+    ? normalizedDistDir.slice(normalizedRoot.length)\n+    : ''\n+}\n+\n // all Vitest imports always need to be externalized\n export function getCachedVitestImport(\n   id: string,\n   state: () => WorkerGlobalState,\n ): null | { externalize: string; type: 'module' } {","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix: prevent false Vitest import resolution (#11196)","issue_url":"https://github.com/vitest-dev/vitest/issues/11194","pr_url":"https://github.com/vitest-dev/vitest/pull/11196"},"needs_manual_review":true}
{"id":"vitest-99fc525-1","repo":"vitest-dev/vitest","license":"MIT","commit":"99fc5259175cf09671a0ed75ec476f06507a2605","parent":"09bc9ad6b3dfce1eb647d775376cdadd7c492694","file":"packages/spy/src/index.ts","language":"typescript","hunk_header":"@@ -651,11 +651,11 @@ function reparentMockPrototype(","before":"  }\n  // an implementation without a usable prototype (reset mock, arrow or bound\n  // function) reverts the chain to `Object.prototype`, the parent every mock\n  // is created with\n  const parent = (implementation as Constructable | undefined)?.prototype ?? Object.prototype\n  if (Object.getPrototypeOf(mockPrototype) !== parent) {\n    Object.setPrototypeOf(mockPrototype, parent)\n  }\n}\n\nfunction registerCalls(args: unknown[], state: MockContext, prototypeState?: MockContext) {","after":"  }\n  // an implementation without a usable prototype (reset mock, arrow or bound\n  // function) reverts the chain to `Object.prototype`, the parent every mock\n  // is created with\n  const parent = (implementation as Constructable | undefined)?.prototype ?? Object.prototype\n  if (mockPrototype !== parent && Object.getPrototypeOf(mockPrototype) !== parent) {\n    Object.setPrototypeOf(mockPrototype, parent)\n  }\n}\n\nfunction registerCalls(args: unknown[], state: MockContext, prototypeState?: MockContext) {","diff":"@@ -651,11 +651,11 @@ function reparentMockPrototype(\n   }\n   // an implementation without a usable prototype (reset mock, arrow or bound\n   // function) reverts the chain to `Object.prototype`, the parent every mock\n   // is created with\n   const parent = (implementation as Constructable | undefined)?.prototype ?? Object.prototype\n-  if (Object.getPrototypeOf(mockPrototype) !== parent) {\n+  if (mockPrototype !== parent && Object.getPrototypeOf(mockPrototype) !== parent) {\n     Object.setPrototypeOf(mockPrototype, parent)\n   }\n }\n \n function registerCalls(args: unknown[], state: MockContext, prototypeState?: MockContext) {","label":{"defect":true,"category":"bugfix","touches_public_api":true,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix: avoid recursive prototype in automocking (#11195)","issue_url":"https://github.com/vitest-dev/vitest/issues/11168","pr_url":"https://github.com/vitest-dev/vitest/pull/11195"},"needs_manual_review":true}
{"id":"vitest-7c81815-1","repo":"vitest-dev/vitest","license":"MIT","commit":"7c818153add03b0bca54453a54e76961dd5be18d","parent":"455466c168a9d5cfecd98824860fbdc236f1f9ba","file":"packages/vitest/src/node/ast-collect.ts","language":"typescript","hunk_header":"@@ -10,10 +10,11 @@ import { parseAst } from 'vite'","before":"import { validateTags } from '../runtime/runner/utils/tags'\nimport { createIndexLocationsMap } from '../utils/base'\nimport { createDebugger } from '../utils/debugger'\nimport { calculateSuiteHash, createFileTask as createFileTaskOriginal, createTaskName } from '../utils/tasks'\nimport { detectCodeBlock } from '../utils/test-helpers'\n\ninterface ParsedFile extends File {\n  start: number\n  end: number\n}","after":"import { validateTags } from '../runtime/runner/utils/tags'\nimport { createIndexLocationsMap } from '../utils/base'\nimport { createDebugger } from '../utils/debugger'\nimport { calculateSuiteHash, createFileTask as createFileTaskOriginal, createTaskName } from '../utils/tasks'\nimport { detectCodeBlock } from '../utils/test-helpers'\nimport { toRollupError } from './environments/fetchModule'\n\ninterface ParsedFile extends File {\n  start: number\n  end: number\n}","diff":"@@ -10,10 +10,11 @@ import { parseAst } from 'vite'\n import { validateTags } from '../runtime/runner/utils/tags'\n import { createIndexLocationsMap } from '../utils/base'\n import { createDebugger } from '../utils/debugger'\n import { calculateSuiteHash, createFileTask as createFileTaskOriginal, createTaskName } from '../utils/tasks'\n import { detectCodeBlock } from '../utils/test-helpers'\n+import { toRollupError } from './environments/fetchModule'\n \n interface ParsedFile extends File {\n   start: number\n   end: number\n }","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix: keep parse error details in static collection (fix #11150) (#11151)","issue_url":"https://github.com/vitest-dev/vitest/issues/11150","pr_url":"https://github.com/vitest-dev/vitest/pull/11151"},"needs_manual_review":true}
{"id":"vitest-455466c-1","repo":"vitest-dev/vitest","license":"MIT","commit":"455466c168a9d5cfecd98824860fbdc236f1f9ba","parent":"6108b819785bceff03edb2cc7b60e17779f21191","file":"packages/vitest/src/public/browser.ts","language":"typescript","hunk_header":"@@ -46,10 +46,26 @@ export interface BrowserCommands {","before":"\nexport interface CDPSession {\n  // methods are defined by the provider type augmentation\n}\n\n/**\n * @internal\n */\nexport const __INTERNAL: {\n  _asLocator: (lang: 'javascript', selector: string) => string","after":"\nexport interface CDPSession {\n  // methods are defined by the provider type augmentation\n}\n\nexport type BrowserTraceEntryKind = 'action' | 'expect' | 'mark' | 'lifecycle'\n\nexport interface MarkOptions {\n  /**\n   * Optional stack string used to resolve marker location.\n   * Useful for wrapper libraries that need to forward the end-user callsite.\n   */\n  stack?: string\n\n  /**\n   * Optional marker kind that's used to categorize the marker in the trace viewer.\n   * @default 'mark'\n   */\n  kind?: BrowserTraceEntryKind\n}\n\n/**\n * @internal\n */\nexport const __INTERNAL: {\n  _asLocator: (lang: 'javascript', selector: string) => string","diff":"@@ -46,10 +46,26 @@ export interface BrowserCommands {\n \n export interface CDPSession {\n   // methods are defined by the provider type augmentation\n }\n \n+export type BrowserTraceEntryKind = 'action' | 'expect' | 'mark' | 'lifecycle'\n+\n+export interface MarkOptions {\n+  /**\n+   * Optional stack string used to resolve marker location.\n+   * Useful for wrapper libraries that need to forward the end-user callsite.\n+   */\n+  stack?: string\n+\n+  /**\n+   * Optional marker kind that's used to categorize the marker in the trace viewer.\n+   * @default 'mark'\n+   */\n+  kind?: BrowserTraceEntryKind\n+}\n+\n /**\n  * @internal\n  */\n export const __INTERNAL: {\n   _asLocator: (lang: 'javascript', selector: string) => string","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix(types): make public declarations self-contained (#11141)","issue_url":"https://github.com/vitest-dev/vitest/issues/11140","pr_url":"https://github.com/vitest-dev/vitest/pull/11141"},"needs_manual_review":true}
{"id":"vitest-6108b81-1","repo":"vitest-dev/vitest","license":"MIT","commit":"6108b819785bceff03edb2cc7b60e17779f21191","parent":"9e1166959e14bd32298d8a0e85352431c769ec7a","file":"packages/vitest/src/node/core.ts","language":"typescript","hunk_header":"@@ -799,10 +799,13 @@ export class Vitest {","before":"\n      if (options.staticParse !== false) {\n        const testModules = await this.parseSpecifications(files, {\n          concurrency: options.staticParseConcurrency,\n        })\n        return { testModules, unhandledErrors: [] }\n      }\n\n      return this.collectTests(files)\n    })","after":"\n      if (options.staticParse !== false) {\n        const testModules = await this.parseSpecifications(files, {\n          concurrency: options.staticParseConcurrency,\n        })\n        if (hasFailed(testModules.map(testModule => testModule.task))) {\n          process.exitCode = 1\n        }\n        return { testModules, unhandledErrors: [] }\n      }\n\n      return this.collectTests(files)\n    })","diff":"@@ -799,10 +799,13 @@ export class Vitest {\n \n       if (options.staticParse !== false) {\n         const testModules = await this.parseSpecifications(files, {\n           concurrency: options.staticParseConcurrency,\n         })\n+        if (hasFailed(testModules.map(testModule => testModule.task))) {\n+          process.exitCode = 1\n+        }\n         return { testModules, unhandledErrors: [] }\n       }\n \n       return this.collectTests(files)\n     })","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix: exit 1 when vitest list fails collection (fix #11145) (#11146)","issue_url":"https://github.com/vitest-dev/vitest/issues/11145","pr_url":"https://github.com/vitest-dev/vitest/pull/11146"},"needs_manual_review":true}
{"id":"vitest-f08ce4b-1","repo":"vitest-dev/vitest","license":"MIT","commit":"f08ce4b7144542af128dcb884150c42074223653","parent":"c4473e48ee046938a9ab6784fe2735257c2c0f72","file":"packages/vitest/src/runtime/moduleRunner/bareModuleMocker.ts","language":"typescript","hunk_header":"@@ -150,39 +150,42 @@ export class BareModuleMocker implements TestModuleMocker {","before":"  public async resolveMocks(): Promise<void> {\n    if (!BareModuleMocker.pendingIds.length) {\n      return\n    }\n\n    const resolveMock = async (mock: PendingSuiteMock) => {\n      const { id, url, external } = await this.resolveId(\n        mock.id,\n        mock.importer,\n      )\n      if (mock.action === 'unmock') {\n        this.unmockPath(id)\n      }\n      if (mock.action === 'mock') {\n        this.mockPath(\n          mock.id,\n          id,\n          url,\n          external,\n          mock.type,\n          mock.factory,\n        )\n      }\n    }\n\n    // group consecutive mocks of the same action type together,\n    // resolve in parallel inside each group, but run groups sequentially\n    // to preserve mock/unmock ordering\n    const groups = groupByConsecutiveAction(BareModuleMocker.pendingIds)\n    for (const group of groups) {\n      await Promise.all(group.map(resolveMock))\n    }\n\n    BareModuleMocker.pendingIds = []\n  }\n\n  // public method to avoid circular dependency\n  public getMockContext(): ModuleMockContext {\n    return this.mockContext","after":"  public async resolveMocks(): Promise<void> {\n    if (!BareModuleMocker.pendingIds.length) {\n      return\n    }\n\n    const pendingIds = BareModuleMocker.pendingIds\n    BareModuleMocker.pendingIds = []\n\n    const resolveMock = async (mock: PendingSuiteMock) => {\n      const resolved = await this.resolveId(mock.id, mock.importer)\n      return { mock, ...resolved }\n    }\n\n    // group consecutive mocks of the same action type together,\n    // resolve in parallel inside each group, but run groups sequentially\n    // to preserve mock/unmock ordering\n    const groups = groupByConsecutiveAction(pendingIds)\n    for (const group of groups) {\n      // apply in queue order: two mocks of the same path can resolve out of\n      // order, and the last queued one must win\n      const resolvedGroup = await Promise.all(group.map(resolveMock))\n      for (const { mock, id, url, external } of resolvedGroup) {\n        if (mock.action === 'unmock') {\n          this.unmockPath(id)\n        }\n        if (mock.action === 'mock') {\n          this.mockPath(\n            mock.id,\n            id,\n            url,\n            external,\n            mock.type,\n            mock.factory,\n          )\n        }\n      }\n    }\n  }\n\n  // public method to avoid circular dependency\n  public getMockContext(): ModuleMockContext {\n    return this.mockContext","diff":"@@ -150,39 +150,42 @@ export class BareModuleMocker implements TestModuleMocker {\n   public async resolveMocks(): Promise<void> {\n     if (!BareModuleMocker.pendingIds.length) {\n       return\n     }\n \n+    const pendingIds = BareModuleMocker.pendingIds\n+    BareModuleMocker.pendingIds = []\n+\n     const resolveMock = async (mock: PendingSuiteMock) => {\n-      const { id, url, external } = await this.resolveId(\n-        mock.id,\n-        mock.importer,\n-      )\n-      if (mock.action === 'unmock') {\n-        this.unmockPath(id)\n-      }\n-      if (mock.action === 'mock') {\n-        this.mockPath(\n-          mock.id,\n-          id,\n-          url,\n-          external,\n-          mock.type,\n-          mock.factory,\n-        )\n-      }\n+      const resolved = await this.resolveId(mock.id, mock.importer)\n+      return { mock, ...resolved }\n     }\n \n     // group consecutive mocks of the same action type together,\n     // resolve in parallel inside each group, but run groups sequentially\n     // to preserve mock/unmock ordering\n-    const groups = groupByConsecutiveAction(BareModuleMocker.pendingIds)\n+    const groups = groupByConsecutiveAction(pendingIds)\n     for (const group of groups) {\n-      await Promise.all(group.map(resolveMock))\n+      // apply in queue order: two mocks of the same path can resolve out of\n+      // order, and the last queued one must win\n+      const resolvedGroup = await Promise.all(group.map(resolveMock))\n+      for (const { mock, id, url, external } of resolvedGroup) {\n+        if (mock.action === 'unmock') {\n+          this.unmockPath(id)\n+        }\n+        if (mock.action === 'mock') {\n+          this.mockPath(\n+            mock.id,\n+            id,\n+            url,\n+            external,\n+            mock.type,\n+            mock.factory,\n+          )\n+        }\n+      }\n     }\n-\n-    BareModuleMocker.pendingIds = []\n   }\n \n   // public method to avoid circular dependency\n   public getMockContext(): ModuleMockContext {\n     return this.mockContext","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix: apply queued mocks from doMock() in queue order (fixes #10706) (#11127)","issue_url":"https://github.com/vitest-dev/vitest/issues/10706","pr_url":"https://github.com/vitest-dev/vitest/pull/11127"},"needs_manual_review":true}
{"id":"vitest-f58a209-1","repo":"vitest-dev/vitest","license":"MIT","commit":"f58a209779ffd3007006481d0c9da0099d5226e6","parent":"d1c3ecc932dcb4cd50420c54aa1f7fa3051deac7","file":"packages/vitest/src/node/types/benchmark.ts","language":"typescript","hunk_header":"@@ -8,11 +8,12 @@ export interface BenchmarkUserOptions {","before":"   */\n  include?: string[]\n\n  /**\n   * Exclude globs for benchmark test files\n   * @default []\n   */\n  exclude?: string[]\n\n  /**\n   * Include globs for in-source benchmark test files","after":"   */\n  include?: string[]\n\n  /**\n   * Exclude globs for benchmark test files\n   *\n   * @default ['**\\/node_modules/**', '**\\/.git/**']\n   */\n  exclude?: string[]\n\n  /**\n   * Include globs for in-source benchmark test files","diff":"@@ -8,11 +8,12 @@ export interface BenchmarkUserOptions {\n    */\n   include?: string[]\n \n   /**\n    * Exclude globs for benchmark test files\n-   * @default []\n+   *\n+   * @default ['**\\/node_modules/**', '**\\/.git/**']\n    */\n   exclude?: string[]\n \n   /**\n    * Include globs for in-source benchmark test files","label":{"defect":false,"category":"docs","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"docs: correct benchmark.exclude and watch defaults (#11268)","issue_url":null,"pr_url":"https://github.com/vitest-dev/vitest/pull/11268"},"needs_manual_review":true}
{"id":"vitest-2d05360-1","repo":"vitest-dev/vitest","license":"MIT","commit":"2d053600efa0a646150e755bcc3745c5d1fded43","parent":"0a7122daaf6de7728f99359af879abba02c41616","file":"test/ui/playwright.config.ts","language":"typescript","hunk_header":"@@ -3,12 +3,11 @@ import { defineConfig, devices } from '@playwright/test'","before":"export default defineConfig({\n  testDir: './test',\n  projects: [\n    {\n      name: 'chromium',\n      // increase viewport height so virtual scroller renders all explorer items\n      use: { ...devices['Desktop Chrome'], viewport: { width: 800, height: 1300 } },\n    },\n  ],\n  use: {\n    trace: process.env.CI ? 'on-first-retry' : undefined,\n  },","after":"export default defineConfig({\n  testDir: './test',\n  projects: [\n    {\n      name: 'chromium',\n      use: { ...devices['Desktop Chrome'] },\n    },\n  ],\n  use: {\n    trace: process.env.CI ? 'on-first-retry' : undefined,\n  },","diff":"@@ -3,12 +3,11 @@ import { defineConfig, devices } from '@playwright/test'\n export default defineConfig({\n   testDir: './test',\n   projects: [\n     {\n       name: 'chromium',\n-      // increase viewport height so virtual scroller renders all explorer items\n-      use: { ...devices['Desktop Chrome'], viewport: { width: 800, height: 1300 } },\n+      use: { ...devices['Desktop Chrome'] },\n     },\n   ],\n   use: {\n     trace: process.env.CI ? 'on-first-retry' : undefined,\n   },","label":{"defect":false,"category":"test","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"test(ui): improve explorer filter assertions (#11124)","issue_url":null,"pr_url":"https://github.com/vitest-dev/vitest/pull/11124"},"needs_manual_review":true}
{"id":"vitest-ccd6d05-1","repo":"vitest-dev/vitest","license":"MIT","commit":"ccd6d057b6e4ca29a5e6b753a2b8e16b0afd8cda","parent":"7f75f902eb918e8ccfc176779ebfcf17f7bce992","file":"packages/vitest/src/node/types/config.ts","language":"typescript","hunk_header":"@@ -1101,11 +1101,11 @@ export interface TypecheckConfig {","before":"   */\n  include: string[]\n  /**\n   * Pattern for files that should not be treated as test files\n   *\n   * @default ['**\\/node_modules/**', '**\\/dist/**', '**\\/cypress/**', '**\\/.{idea,git,cache,output,temp}/**', '**\\/{karma,rollup,webpack,vite,vitest,jest,ava,babel,nyc,cypress,tsup,build,eslint,prettier}.config.*']\n   */\n  exclude: string[]\n  /**\n   * Check JS files that have `@ts-check` comment.\n   * If you have it enabled in tsconfig, this will not overwrite it.","after":"   */\n  include: string[]\n  /**\n   * Pattern for files that should not be treated as test files\n   *\n   * @default ['**\\/node_modules/**', '**\\/.git/**']\n   */\n  exclude: string[]\n  /**\n   * Check JS files that have `@ts-check` comment.\n   * If you have it enabled in tsconfig, this will not overwrite it.","diff":"@@ -1101,11 +1101,11 @@ export interface TypecheckConfig {\n    */\n   include: string[]\n   /**\n    * Pattern for files that should not be treated as test files\n    *\n-   * @default ['**\\/node_modules/**', '**\\/dist/**', '**\\/cypress/**', '**\\/.{idea,git,cache,output,temp}/**', '**\\/{karma,rollup,webpack,vite,vitest,jest,ava,babel,nyc,cypress,tsup,build,eslint,prettier}.config.*']\n+   * @default ['**\\/node_modules/**', '**\\/.git/**']\n    */\n   exclude: string[]\n   /**\n    * Check JS files that have `@ts-check` comment.\n    * If you have it enabled in tsconfig, this will not overwrite it.","label":{"defect":false,"category":"docs","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"docs: fix typecheck exclude default in documentation (#11223)","issue_url":null,"pr_url":"https://github.com/vitest-dev/vitest/pull/11223"},"needs_manual_review":true}
{"id":"vitest-4f8ef02-1","repo":"vitest-dev/vitest","license":"MIT","commit":"4f8ef0216f637ed2d3e66684982d930f20892347","parent":"91ab1588c72c9c5f7c638368a541cd20e0e8a934","file":"docs/.vitepress/config.ts","language":"typescript","hunk_header":"@@ -169,14 +169,14 @@ export default ({ mode }: { mode: string }) => {","before":"          },\n          {\n            title: 'Versions',\n            items: [\n              { text: 'Unreleased Docs', link: 'https://main.vitest.dev/' },\n              { text: 'Vitest v3 Docs', link: 'https://v3.vitest.dev/' },\n              { text: 'Vitest v2 Docs', link: 'https://v2.vitest.dev/' },\n              { text: 'Vitest v1 Docs', link: 'https://v1.vitest.dev/' },\n              { text: 'Vitest v0 Docs', link: 'https://v0.vitest.dev/' },\n            ],\n          },\n          /* {\n            title: 'Legal',\n            items: [","after":"          },\n          {\n            title: 'Versions',\n            items: [\n              { text: 'Unreleased Docs', link: 'https://main.vitest.dev/' },\n              { text: 'Vitest v4 Docs', link: 'https://v4.vitest.dev/' },\n              { text: 'Vitest v3 Docs', link: 'https://v3.vitest.dev/' },\n              { text: 'Vitest v2 Docs', link: 'https://v2.vitest.dev/' },\n              { text: 'Vitest v1 Docs', link: 'https://v1.vitest.dev/' },\n            ],\n          },\n          /* {\n            title: 'Legal',\n            items: [","diff":"@@ -169,14 +169,14 @@ export default ({ mode }: { mode: string }) => {\n           },\n           {\n             title: 'Versions',\n             items: [\n               { text: 'Unreleased Docs', link: 'https://main.vitest.dev/' },\n+              { text: 'Vitest v4 Docs', link: 'https://v4.vitest.dev/' },\n               { text: 'Vitest v3 Docs', link: 'https://v3.vitest.dev/' },\n               { text: 'Vitest v2 Docs', link: 'https://v2.vitest.dev/' },\n               { text: 'Vitest v1 Docs', link: 'https://v1.vitest.dev/' },\n-              { text: 'Vitest v0 Docs', link: 'https://v0.vitest.dev/' },\n             ],\n           },\n           /* {\n             title: 'Legal',\n             items: [","label":{"defect":false,"category":"docs","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"docs: remove v0 link, update vitest API usage (#11211)","issue_url":null,"pr_url":"https://github.com/vitest-dev/vitest/pull/11211"},"needs_manual_review":true}
{"id":"vitest-0767036-1","repo":"vitest-dev/vitest","license":"MIT","commit":"07670364b45e61f2c542d332a232fe81e4f88d82","parent":"23dda738ccacb11682426426cd7b876afd107621","file":"packages/ui/vite.config.ts","language":"typescript","hunk_header":"@@ -1,11 +1,11 @@","before":"import type { Plugin } from 'vite'\nimport fs from 'node:fs'\nimport path from 'node:path'\nimport Vue from '@vitejs/plugin-vue'\nimport { resolve } from 'pathe'\nimport { presetAttributify, presetIcons, presetWind3, transformerDirectives } from 'unocss'\nimport Unocss from 'unocss/vite'\nimport { defineConfig } from 'vite'\nimport { resolveApiToken } from '../vitest/src/node/config/apiToken'\n\nexport default defineConfig({","after":"import type { Plugin } from 'vite'\nimport fs from 'node:fs'\nimport path from 'node:path'\nimport Vue from '@vitejs/plugin-vue'\nimport { resolve } from 'pathe'\nimport { presetIcons, presetWind3, transformerDirectives } from 'unocss'\nimport Unocss from 'unocss/vite'\nimport { defineConfig } from 'vite'\nimport { resolveApiToken } from '../vitest/src/node/config/apiToken'\n\nexport default defineConfig({","diff":"@@ -1,11 +1,11 @@\n import type { Plugin } from 'vite'\n import fs from 'node:fs'\n import path from 'node:path'\n import Vue from '@vitejs/plugin-vue'\n import { resolve } from 'pathe'\n-import { presetAttributify, presetIcons, presetWind3, transformerDirectives } from 'unocss'\n+import { presetIcons, presetWind3, transformerDirectives } from 'unocss'\n import Unocss from 'unocss/vite'\n import { defineConfig } from 'vite'\n import { resolveApiToken } from '../vitest/src/node/config/apiToken'\n \n export default defineConfig({","label":{"defect":false,"category":"refactor","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"refactor(ui): consolidate unocss style to use class (#11206)","issue_url":null,"pr_url":"https://github.com/vitest-dev/vitest/pull/11206"},"needs_manual_review":true}
{"id":"vitest-376dc3b-1","repo":"vitest-dev/vitest","license":"MIT","commit":"376dc3bc1c7ac099a7634b55c2e8f90cd6ec02a9","parent":"41c9f6516af58e6ca825d62cac63060b0c0a4e3e","file":"packages/ui/client/composables/navigation.ts","language":"typescript","hunk_header":"@@ -114,11 +114,11 @@ export function showDashboard(show: boolean) {","before":"    currentModule.value = undefined\n    activeFileId.value = ''\n  }\n}\n\nexport function navigateTo({ file, line, view, test, column }: Omit<Params, 'traceAttempt' | 'traceStep'>) {\n  activeFileId.value = file\n  lineNumber.value = line\n  columnNumber.value = column\n  viewMode.value = view\n  selectedTest.value = test","after":"    currentModule.value = undefined\n    activeFileId.value = ''\n  }\n}\n\nexport function navigateTo({ file, line, view, test, column }: Omit<Params, 'layout' | 'traceAttempt' | 'traceStep'>) {\n  activeFileId.value = file\n  lineNumber.value = line\n  columnNumber.value = column\n  viewMode.value = view\n  selectedTest.value = test","diff":"@@ -114,11 +114,11 @@ export function showDashboard(show: boolean) {\n     currentModule.value = undefined\n     activeFileId.value = ''\n   }\n }\n \n-export function navigateTo({ file, line, view, test, column }: Omit<Params, 'traceAttempt' | 'traceStep'>) {\n+export function navigateTo({ file, line, view, test, column }: Omit<Params, 'layout' | 'traceAttempt' | 'traceStep'>) {\n   activeFileId.value = file\n   lineNumber.value = line\n   columnNumber.value = column\n   viewMode.value = view\n   selectedTest.value = test","label":{"defect":false,"category":"feature","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"feat(ui): add focused trace view layout mode (#11190)","issue_url":null,"pr_url":"https://github.com/vitest-dev/vitest/pull/11190"},"needs_manual_review":true}
{"id":"vitest-5dc4b5c-1","repo":"vitest-dev/vitest","license":"MIT","commit":"5dc4b5c925e8e3ccc3e2ddf0ff31cb4c1f4447ca","parent":"b426c1976d32310c271d053b814bafb9a99bcd89","file":"packages/ui/client/composables/trace-view.ts","language":"typescript","hunk_header":"@@ -176,17 +176,15 @@ export function getTraceEntryClass(entry: BrowserTraceEntry) {","before":"    return 'text-amber-500'\n  }\n  return 'text-gray-400 dark:text-gray-500'\n}\n\nexport function openTrace(trace: BrowserTraceData, test: RunnerTestCase) {\n  detailsPosition.value = 'bottom'\n  setActiveTrace({\n    test,\n    attemptKey: getTraceAttemptKey(trace),\n    selectedStepIndex: 0,\n  })\n}\n\nfunction setActiveTrace(selection: TraceSelection) {\n  activeTraceView.value = selection\n  selectedTraceAttempt.value = selection.attemptKey ?? null","after":"    return 'text-amber-500'\n  }\n  return 'text-gray-400 dark:text-gray-500'\n}\n\nexport function openTrace(test: RunnerTestCase) {\n  detailsPosition.value = 'bottom'\n  if (activeTraceView.value?.test !== test) {\n    setActiveTrace({ test, selectedStepIndex: 0 })\n  }\n}\n\nfunction setActiveTrace(selection: TraceSelection) {\n  activeTraceView.value = selection\n  selectedTraceAttempt.value = selection.attemptKey ?? null","diff":"@@ -176,17 +176,15 @@ export function getTraceEntryClass(entry: BrowserTraceEntry) {\n     return 'text-amber-500'\n   }\n   return 'text-gray-400 dark:text-gray-500'\n }\n \n-export function openTrace(trace: BrowserTraceData, test: RunnerTestCase) {\n+export function openTrace(test: RunnerTestCase) {\n   detailsPosition.value = 'bottom'\n-  setActiveTrace({\n-    test,\n-    attemptKey: getTraceAttemptKey(trace),\n-    selectedStepIndex: 0,\n-  })\n+  if (activeTraceView.value?.test !== test) {\n+    setActiveTrace({ test, selectedStepIndex: 0 })\n+  }\n }\n \n function setActiveTrace(selection: TraceSelection) {\n   activeTraceView.value = selection\n   selectedTraceAttempt.value = selection.attemptKey ?? null","label":{"defect":false,"category":"feature","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"feat(ui): move trace attempts selector to viewer header (#11189)","issue_url":null,"pr_url":"https://github.com/vitest-dev/vitest/pull/11189"},"needs_manual_review":true}
{"id":"vitest-d7bf1a7-1","repo":"vitest-dev/vitest","license":"MIT","commit":"d7bf1a7444e15db23c9bbb921e968296b0e3cdf6","parent":"d46a7472266d5bb43595ea51dcdd64ab0f560f12","file":"docs/.vitepress/config.ts","language":"typescript","hunk_header":"@@ -1099,24 +1099,24 @@ export default ({ mode }: { mode: string }) => {","before":"          // upgrades and porting from other test runners (Jest, Mocha).\n          // Sits near the bottom because it's not daily-use and would push\n          // active-use guides further from the user's first scroll.\n          {\n            text: 'Migration',\n            link: '/guide/migration',\n            collapsed: false,\n            items: [\n              {\n                text: 'Migrating to Vitest 5.0',\n                link: '/guide/migration#vitest-5',\n              },\n              {\n                text: 'Migrating from Jest',\n                link: '/guide/migration#jest',\n              },\n              {\n                text: 'Migrating from Mocha + Chai + Sinon',\n                link: '/guide/migration#mocha-chai-sinon',\n              },\n            ],\n          },\n          {\n            items: [","after":"          // upgrades and porting from other test runners (Jest, Mocha).\n          // Sits near the bottom because it's not daily-use and would push\n          // active-use guides further from the user's first scroll.\n          {\n            text: 'Migration',\n            link: '/guide/migration/',\n            collapsed: false,\n            items: [\n              {\n                text: 'Migrating to Vitest 5.0',\n                link: '/guide/migration/',\n              },\n              {\n                text: 'Migrating from Jest',\n                link: '/guide/migration/jest',\n              },\n              {\n                text: 'Migrating from Mocha + Chai + Sinon',\n                link: '/guide/migration/mocha',\n              },\n            ],\n          },\n          {\n            items: [","diff":"@@ -1099,24 +1099,24 @@ export default ({ mode }: { mode: string }) => {\n           // upgrades and porting from other test runners (Jest, Mocha).\n           // Sits near the bottom because it's not daily-use and would push\n           // active-use guides further from the user's first scroll.\n           {\n             text: 'Migration',\n-            link: '/guide/migration',\n+            link: '/guide/migration/',\n             collapsed: false,\n             items: [\n               {\n                 text: 'Migrating to Vitest 5.0',\n-                link: '/guide/migration#vitest-5',\n+                link: '/guide/migration/',\n               },\n               {\n                 text: 'Migrating from Jest',\n-                link: '/guide/migration#jest',\n+                link: '/guide/migration/jest',\n               },\n               {\n                 text: 'Migrating from Mocha + Chai + Sinon',\n-                link: '/guide/migration#mocha-chai-sinon',\n+                link: '/guide/migration/mocha',\n               },\n             ],\n           },\n           {\n             items: [","label":{"defect":false,"category":"docs","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"docs: split migration into multiple pages, add a button to copy the prompt (#11129)","issue_url":null,"pr_url":"https://github.com/vitest-dev/vitest/pull/11129"},"needs_manual_review":true}
{"id":"vitest-e5e68a3-1","repo":"vitest-dev/vitest","license":"MIT","commit":"e5e68a308b7c64f4a021fab53042d5469a4d73d1","parent":"2451e34c404aba1038f5e749c85bb268b2f4525e","file":"test/unit/vite.config.ts","language":"typescript","hunk_header":"@@ -38,11 +38,11 @@ export default defineConfig({","before":"    !!rolldownVersion\n    && (import('@rolldown/plugin-babel').then(({ default: babel }) =>\n      babel({\n        presets: [\n          {\n            preset: ({\n              plugins: [['@babel/plugin-proposal-decorators', { version: '2023-11' }]],\n            }),\n            rolldown: {\n              filter: {\n                id: ['**/esnext-decorator.test.ts'],","after":"    !!rolldownVersion\n    && (import('@rolldown/plugin-babel').then(({ default: babel }) =>\n      babel({\n        presets: [\n          {\n            preset: () => ({\n              plugins: [['@babel/plugin-proposal-decorators', { version: '2023-11' }]],\n            }),\n            rolldown: {\n              filter: {\n                id: ['**/esnext-decorator.test.ts'],","diff":"@@ -38,11 +38,11 @@ export default defineConfig({\n     !!rolldownVersion\n     && (import('@rolldown/plugin-babel').then(({ default: babel }) =>\n       babel({\n         presets: [\n           {\n-            preset: ({\n+            preset: () => ({\n               plugins: [['@babel/plugin-proposal-decorators', { version: '2023-11' }]],\n             }),\n             rolldown: {\n               filter: {\n                 id: ['**/esnext-decorator.test.ts'],","label":{"defect":false,"category":"deps","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"chore(deps): update babel monorepo to v8 (#11105)","issue_url":null,"pr_url":"https://github.com/vitest-dev/vitest/pull/11105"},"needs_manual_review":true}
{"id":"vitest-51e9494-1","repo":"vitest-dev/vitest","license":"MIT","commit":"51e9494166d8d0108a621fce80c41b6dba760bae","parent":"2122ffdfb42d86c9a2f1238100d6a031312cae32","file":"packages/vitest/src/node/cli/cli-api.ts","language":"typescript","hunk_header":"@@ -28,16 +28,16 @@ export interface CliOptions extends UserConfig {","before":"   * Output collected test files only\n   */\n  filesOnly?: boolean\n  /**\n   * Parse files statically instead of running them to collect tests\n   * @experimental\n   */\n  staticParse?: boolean\n  /**\n   * How many tests to process at the same time\n   * @experimental\n   */\n  staticParseConcurrency?: number\n\n  /**\n   * Override vite config's configLoader from CLI.","after":"   * Output collected test files only\n   */\n  filesOnly?: boolean\n  /**\n   * Parse files statically instead of running them to collect tests\n   * @default true\n   */\n  staticParse?: boolean\n  /**\n   * How many tests to process at the same time\n   * @default os.availableParallelism()\n   */\n  staticParseConcurrency?: number\n\n  /**\n   * Override vite config's configLoader from CLI.","diff":"@@ -28,16 +28,16 @@ export interface CliOptions extends UserConfig {\n    * Output collected test files only\n    */\n   filesOnly?: boolean\n   /**\n    * Parse files statically instead of running them to collect tests\n-   * @experimental\n+   * @default true\n    */\n   staticParse?: boolean\n   /**\n    * How many tests to process at the same time\n-   * @experimental\n+   * @default os.availableParallelism()\n    */\n   staticParseConcurrency?: number\n \n   /**\n    * Override vite config's configLoader from CLI.","label":{"defect":false,"category":"feature","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"feat!: parse files statically in vitest list by default (#11088)","issue_url":null,"pr_url":"https://github.com/vitest-dev/vitest/pull/11088"},"needs_manual_review":true}
{"id":"vitest-58f02ae-1","repo":"vitest-dev/vitest","license":"MIT","commit":"58f02ae3663435854902ca8c2de049e75c4610f9","parent":"dc10f5f8fb69b026afcf56f8a7a9dd36bd964d73","file":"packages/ui/client/composables/attachments.ts","language":"typescript","hunk_header":"@@ -36,5 +36,57 @@ export function internalOrExternalUrl(attachment: TestAttachment): string {","before":"    return potentialUrl\n  }\n\n  return getAttachmentUrl(attachment)\n}","after":"    return potentialUrl\n  }\n\n  return getAttachmentUrl(attachment)\n}\n\nexport async function openPlaywrightTrace(attachment: TestAttachment): Promise<void> {\n  const popup = window.open('', '_blank')\n  if (!popup) {\n    // eslint-disable-next-line no-alert\n    window.alert('Unable to open Playwright Trace Viewer. Please allow pop-ups and try again.')\n    return\n  }\n\n  popup.document.write('<!doctype html><title>Opening Playwright Trace</title><body>Opening Playwright trace...</body>')\n  popup.document.close()\n  popup.focus()\n\n  try {\n    const response = await fetch(getAttachmentUrl(attachment))\n    if (!response.ok) {\n      throw new Error(`Failed to load Playwright trace: ${response.statusText}`)\n    }\n    const trace = await response.blob()\n    if (popup.closed) {\n      return\n    }\n\n    // Playwright signals when the trace viewer is ready to receive messages: https://github.com/microsoft/playwright/pull/42451\n    const ready = new Promise<void>((resolve, reject) => {\n      const timeout = setTimeout(() => {\n        window.removeEventListener('message', onMessage)\n        reject(new Error('Timed out waiting for Playwright Trace Viewer'))\n      }, 10_000)\n      function onMessage(event: MessageEvent) {\n        if (event.origin !== 'https://trace.playwright.dev' || event.source !== popup || event.data?.method !== 'ready') {\n          return\n        }\n        clearTimeout(timeout)\n        window.removeEventListener('message', onMessage)\n        resolve()\n      }\n      window.addEventListener('message', onMessage)\n    })\n    popup.location.href = 'https://trace.playwright.dev/next/'\n    await ready\n    if (!popup.closed) {\n      popup.postMessage({ method: 'load', params: { trace } }, 'https://trace.playwright.dev')\n    }\n  }\n  catch {\n    if (!popup.closed) {\n      const errorPage = new Blob(['<!doctype html><title>Failed to Open Playwright Trace</title><body>Failed to load Playwright trace attachment.</body>'], { type: 'text/html' })\n      popup.location.href = URL.createObjectURL(errorPage)\n    }\n  }\n}","diff":"@@ -36,5 +36,57 @@ export function internalOrExternalUrl(attachment: TestAttachment): string {\n     return potentialUrl\n   }\n \n   return getAttachmentUrl(attachment)\n }\n+\n+export async function openPlaywrightTrace(attachment: TestAttachment): Promise<void> {\n+  const popup = window.open('', '_blank')\n+  if (!popup) {\n+    // eslint-disable-next-line no-alert\n+    window.alert('Unable to open Playwright Trace Viewer. Please allow pop-ups and try again.')\n+    return\n+  }\n+\n+  popup.document.write('<!doctype html><title>Opening Playwright Trace</title><body>Opening Playwright trace...</body>')\n+  popup.document.close()\n+  popup.focus()\n+\n+  try {\n+    const response = await fetch(getAttachmentUrl(attachment))\n+    if (!response.ok) {\n+      throw new Error(`Failed to load Playwright trace: ${response.statusText}`)\n+    }\n+    const trace = await response.blob()\n+    if (popup.closed) {\n+      return\n+    }\n+\n+    // Playwright signals when the trace viewer is ready to receive messages: https://github.com/microsoft/playwright/pull/42451\n+    const ready = new Promise<void>((resolve, reject) => {\n+      const timeout = setTimeout(() => {\n+        window.removeEventListener('message', onMessage)\n+        reject(new Error('Timed out waiting for Playwright Trace Viewer'))\n+      }, 10_000)\n+      function onMessage(event: MessageEvent) {\n+        if (event.origin !== 'https://trace.playwright.dev' || event.source !== popup || event.data?.method !== 'ready') {\n+          return\n+        }\n+        clearTimeout(timeout)\n+        window.removeEventListener('message', onMessage)\n+        resolve()\n+      }\n+      window.addEventListener('message', onMessage)\n+    })\n+    popup.location.href = 'https://trace.playwright.dev/next/'\n+    await ready\n+    if (!popup.closed) {\n+      popup.postMessage({ method: 'load', params: { trace } }, 'https://trace.playwright.dev')\n+    }\n+  }\n+  catch {\n+    if (!popup.closed) {\n+      const errorPage = new Blob(['<!doctype html><title>Failed to Open Playwright Trace</title><body>Failed to load Playwright trace attachment.</body>'], { type: 'text/html' })\n+      popup.location.href = URL.createObjectURL(errorPage)\n+    }\n+  }\n+}","label":{"defect":false,"category":"feature","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"feat(ui): add link to open playwright trace (#11059)","issue_url":null,"pr_url":"https://github.com/vitest-dev/vitest/pull/11059"},"needs_manual_review":true}
{"id":"vitest-d4fe198-1","repo":"vitest-dev/vitest","license":"MIT","commit":"d4fe1986fe334d22940039e815ccdbcc907baa6a","parent":"3f2ddc21a83f3ebf16be50a1b31982ee9060266f","file":"packages/vitest/src/node/cli/cli-api.ts","language":"typescript","hunk_header":"@@ -149,11 +149,11 @@ export async function startVitest(","before":"  try {\n    if (ctx.config.listTags) {\n      await ctx.listTags()\n    }\n    else if (ctx.config.clearCache) {\n      await ctx.experimental_clearCache()\n    }\n    else if (ctx.config.mergeReports) {\n      await ctx.mergeReports()\n    }\n    else if (ctx.config.standalone) {","after":"  try {\n    if (ctx.config.listTags) {\n      await ctx.listTags()\n    }\n    else if (ctx.config.clearCache) {\n      await ctx.clearCache()\n    }\n    else if (ctx.config.mergeReports) {\n      await ctx.mergeReports()\n    }\n    else if (ctx.config.standalone) {","diff":"@@ -149,11 +149,11 @@ export async function startVitest(\n   try {\n     if (ctx.config.listTags) {\n       await ctx.listTags()\n     }\n     else if (ctx.config.clearCache) {\n-      await ctx.experimental_clearCache()\n+      await ctx.clearCache()\n     }\n     else if (ctx.config.mergeReports) {\n       await ctx.mergeReports()\n     }\n     else if (ctx.config.standalone) {","label":{"defect":false,"category":"feature","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"feat: promote clearCache out of experimental (#11086)","issue_url":null,"pr_url":"https://github.com/vitest-dev/vitest/pull/11086"},"needs_manual_review":true}
{"id":"vitest-5b278a4-1","repo":"vitest-dev/vitest","license":"MIT","commit":"5b278a4dd6e826271498b4553bf2010904786542","parent":"e08c45c3921ab1c517021836bc73a13f1310c538","file":"test/e2e/test/detect-async-leaks.test.ts","language":"typescript","hunk_header":"@@ -210,39 +210,43 @@ test('fetch', async () => {","before":"\n    \"\n  `)\n})\n\ntest('fs handle', async () => {\n  const { stderr } = await runInlineTests({\n    'packages/example/test/example.test.ts': `\n      import { readFile } from 'node:fs'\n\n      test('leaking fs handle', () => {\n        readFile(import.meta.filename, () => {});\n      })\n    `,\n    'packages/example/test/example-2.test.ts': `\n      import { readFile } from 'node:fs'\n\n      test('not a leak', async () => {\n        await new Promise(resolve => readFile(import.meta.filename, () => { resolve() }));\n      })\n    `,\n  })\n\n  // This might be racy. Sometimes readFile fires two FSREQCALLBACK's, sometimes just one.\n  expect(stderr).toContain(`\\\nFSREQCALLBACK leaking in packages/example/test/example.test.ts\n  3|\n  4|       test('leaking fs handle', () => {\n  5|         readFile(import.meta.filename, () => {});\n   |         ^\n  6|       })\n  7|\n ❯ packages/example/test/example.test.ts:5:9\n`)\n})\n\ntest('http server', async () => {\n  const { stdout, stderr } = await runInlineTests({\n    'packages/example/test/example.test.ts': `","after":"\n    \"\n  `)\n})\n\ntest('fs watcher', async () => {\n  const { stderr } = await runInlineTests({\n    'packages/example/test/example.test.ts': `\n      import { watch } from 'node:fs'\n\n      test('leaking fs watcher', () => {\n        watch(import.meta.filename, () => {});\n      })\n    `,\n    'packages/example/test/example-2.test.ts': `\n      import { watch } from 'node:fs'\n\n      test('not a leak', () => {\n        watch(import.meta.filename, () => {}).close();\n      })\n    `,\n  })\n\n  expect(stderr).toMatchInlineSnapshot(`\n    \"\n    ⎯⎯⎯⎯⎯⎯⎯ Async Leaks 1 ⎯⎯⎯⎯⎯⎯⎯⎯\n\n    FSEVENTWRAP leaking in packages/example/test/example.test.ts\n      3|\n      4|       test('leaking fs watcher', () => {\n      5|         watch(import.meta.filename, () => {});\n       |         ^\n      6|       })\n      7|\n     ❯ packages/example/test/example.test.ts:5:9\n\n    \"\n  `)\n})\n\ntest('http server', async () => {\n  const { stdout, stderr } = await runInlineTests({\n    'packages/example/test/example.test.ts': `","diff":"@@ -210,39 +210,43 @@ test('fetch', async () => {\n \n     \"\n   `)\n })\n \n-test('fs handle', async () => {\n+test('fs watcher', async () => {\n   const { stderr } = await runInlineTests({\n     'packages/example/test/example.test.ts': `\n-      import { readFile } from 'node:fs'\n+      import { watch } from 'node:fs'\n \n-      test('leaking fs handle', () => {\n-        readFile(import.meta.filename, () => {});\n+      test('leaking fs watcher', () => {\n+        watch(import.meta.filename, () => {});\n       })\n     `,\n     'packages/example/test/example-2.test.ts': `\n-      import { readFile } from 'node:fs'\n+      import { watch } from 'node:fs'\n \n-      test('not a leak', async () => {\n-        await new Promise(resolve => readFile(import.meta.filename, () => { resolve() }));\n+      test('not a leak', () => {\n+        watch(import.meta.filename, () => {}).close();\n       })\n     `,\n   })\n \n-  // This might be racy. Sometimes readFile fires two FSREQCALLBACK's, sometimes just one.\n-  expect(stderr).toContain(`\\\n-FSREQCALLBACK leaking in packages/example/test/example.test.ts\n-  3|\n-  4|       test('leaking fs handle', () => {\n-  5|         readFile(import.meta.filename, () => {});\n-   |         ^\n-  6|       })\n-  7|\n- ❯ packages/example/test/example.test.ts:5:9\n-`)\n+  expect(stderr).toMatchInlineSnapshot(`\n+    \"\n+    ⎯⎯⎯⎯⎯⎯⎯ Async Leaks 1 ⎯⎯⎯⎯⎯⎯⎯⎯\n+\n+    FSEVENTWRAP leaking in packages/example/test/example.test.ts\n+      3|\n+      4|       test('leaking fs watcher', () => {\n+      5|         watch(import.meta.filename, () => {});\n+       |         ^\n+      6|       })\n+      7|\n+     ❯ packages/example/test/example.test.ts:5:9\n+\n+    \"\n+  `)\n })\n \n test('http server', async () => {\n   const { stdout, stderr } = await runInlineTests({\n     'packages/example/test/example.test.ts': `","label":{"defect":false,"category":"test","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"test: use fs.watch for the async leak fixture (#11099)","issue_url":null,"pr_url":"https://github.com/vitest-dev/vitest/pull/11099"},"needs_manual_review":true}
{"id":"vitest-e08c45c-1","repo":"vitest-dev/vitest","license":"MIT","commit":"e08c45c3921ab1c517021836bc73a13f1310c538","parent":"7db80dc27e5948010c00160ed0b86570baad6ce8","file":"packages/expect/src/jest-expect.ts","language":"typescript","hunk_header":"@@ -606,18 +606,17 @@ export const JestChaiExpect: ChaiPlugin = (chai, utils) => {","before":"    const spy = getSpy(this)\n    const spyName = spy.getMockName()\n    const pass = spy.mock.calls.some(callArg => equalsArgumentArray(callArg, args))\n    const isNot = utils.flag(this, 'negate') as boolean\n\n    const msg = utils.getMessage(this, [\n      pass,\n      `expected \"${spyName}\" to be called with arguments: #{exp}`,\n      `expected \"${spyName}\" to not be called with arguments: #{exp}`,\n      args,\n    ])\n\n    if ((pass && isNot) || (!pass && !isNot)) {\n      throw new AssertionError(formatCalls(spy, msg, args))\n    }\n  })\n  def('toHaveBeenCalledExactlyOnceWith', function (...args) {\n    const spy = getSpy(this)","after":"    const spy = getSpy(this)\n    const spyName = spy.getMockName()\n    const pass = spy.mock.calls.some(callArg => equalsArgumentArray(callArg, args))\n    const isNot = utils.flag(this, 'negate') as boolean\n\n    if ((pass && isNot) || (!pass && !isNot)) {\n      const msg = utils.getMessage(this, [\n        pass,\n        `expected \"${spyName}\" to be called with arguments: #{exp}`,\n        `expected \"${spyName}\" to not be called with arguments: #{exp}`,\n        args,\n      ])\n      throw new AssertionError(formatCalls(spy, msg, args))\n    }\n  })\n  def('toHaveBeenCalledExactlyOnceWith', function (...args) {\n    const spy = getSpy(this)","diff":"@@ -606,18 +606,17 @@ export const JestChaiExpect: ChaiPlugin = (chai, utils) => {\n     const spy = getSpy(this)\n     const spyName = spy.getMockName()\n     const pass = spy.mock.calls.some(callArg => equalsArgumentArray(callArg, args))\n     const isNot = utils.flag(this, 'negate') as boolean\n \n-    const msg = utils.getMessage(this, [\n-      pass,\n-      `expected \"${spyName}\" to be called with arguments: #{exp}`,\n-      `expected \"${spyName}\" to not be called with arguments: #{exp}`,\n-      args,\n-    ])\n-\n     if ((pass && isNot) || (!pass && !isNot)) {\n+      const msg = utils.getMessage(this, [\n+        pass,\n+        `expected \"${spyName}\" to be called with arguments: #{exp}`,\n+        `expected \"${spyName}\" to not be called with arguments: #{exp}`,\n+        args,\n+      ])\n       throw new AssertionError(formatCalls(spy, msg, args))\n     }\n   })\n   def('toHaveBeenCalledExactlyOnceWith', function (...args) {\n     const spy = getSpy(this)","label":{"defect":false,"category":"refactor","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"perf(expect): lazily format spy matcher failures (#11090)","issue_url":null,"pr_url":"https://github.com/vitest-dev/vitest/pull/11090"},"needs_manual_review":true}
{"id":"hono-edd138e-1","repo":"honojs/hono","license":"MIT","commit":"edd138ee3049749190de3dcd7e32d7bcbf224e41","parent":"9b4e9c2864c30ea9275cf426e2d2716bd9f05195","file":"src/request.test.ts","language":"typescript","hunk_header":"@@ -375,10 +375,62 @@ describe('Body methods with caching', () => {","before":"    expect(async () => await req.arrayBuffer()).not.toThrow()\n    expect(async () => await req.bytes()).not.toThrow()\n    expect(async () => await req.blob()).not.toThrow()\n  })\n\n  describe('req.parseBody()', async () => {\n    it('should parse form data', async () => {\n      const data = new FormData()\n      data.append('foo', 'bar')\n      const req = new HonoRequest(","after":"    expect(async () => await req.arrayBuffer()).not.toThrow()\n    expect(async () => await req.bytes()).not.toThrow()\n    expect(async () => await req.blob()).not.toThrow()\n  })\n\n  describe('formData() after another representation has been cached', () => {\n    const urlencoded = 'application/x-www-form-urlencoded'\n    const body = 'foo=bar&baz=qux'\n\n    for (const first of ['text', 'arrayBuffer', 'bytes', 'blob'] as const) {\n      test(`req.formData() after req.${first}()`, async () => {\n        const req = new HonoRequest(\n          new Request('http://localhost', {\n            method: 'POST',\n            headers: { 'Content-Type': urlencoded },\n            body,\n          })\n        )\n        await req[first]()\n        const formData = await req.formData()\n        expect(formData.get('foo')).toBe('bar')\n        expect(formData.get('baz')).toBe('qux')\n      })\n    }\n\n    test('req.formData() after req.text() for multipart/form-data', async () => {\n      const boundary = '----hono-test-boundary'\n      const multipart =\n        `--${boundary}\\r\\n` +\n        'Content-Disposition: form-data; name=\"foo\"\\r\\n\\r\\n' +\n        'bar\\r\\n' +\n        `--${boundary}--\\r\\n`\n      const req = new HonoRequest(\n        new Request('http://localhost', {\n          method: 'POST',\n          headers: { 'Content-Type': `multipart/form-data; boundary=${boundary}` },\n          body: multipart,\n        })\n      )\n      await req.text()\n      expect((await req.formData()).get('foo')).toBe('bar')\n    })\n\n    test('the cached representation is still returned unchanged', async () => {\n      const req = new HonoRequest(\n        new Request('http://localhost', {\n          method: 'POST',\n          headers: { 'Content-Type': urlencoded },\n          body,\n        })\n      )\n      expect(await req.text()).toBe(body)\n      expect(await req.text()).toBe(body)\n      expect(await req.arrayBuffer()).toEqual(new TextEncoder().encode(body).buffer)\n    })\n  })\n\n  describe('req.parseBody()', async () => {\n    it('should parse form data', async () => {\n      const data = new FormData()\n      data.append('foo', 'bar')\n      const req = new HonoRequest(","diff":"@@ -375,10 +375,62 @@ describe('Body methods with caching', () => {\n     expect(async () => await req.arrayBuffer()).not.toThrow()\n     expect(async () => await req.bytes()).not.toThrow()\n     expect(async () => await req.blob()).not.toThrow()\n   })\n \n+  describe('formData() after another representation has been cached', () => {\n+    const urlencoded = 'application/x-www-form-urlencoded'\n+    const body = 'foo=bar&baz=qux'\n+\n+    for (const first of ['text', 'arrayBuffer', 'bytes', 'blob'] as const) {\n+      test(`req.formData() after req.${first}()`, async () => {\n+        const req = new HonoRequest(\n+          new Request('http://localhost', {\n+            method: 'POST',\n+            headers: { 'Content-Type': urlencoded },\n+            body,\n+          })\n+        )\n+        await req[first]()\n+        const formData = await req.formData()\n+        expect(formData.get('foo')).toBe('bar')\n+        expect(formData.get('baz')).toBe('qux')\n+      })\n+    }\n+\n+    test('req.formData() after req.text() for multipart/form-data', async () => {\n+      const boundary = '----hono-test-boundary'\n+      const multipart =\n+        `--${boundary}\\r\\n` +\n+        'Content-Disposition: form-data; name=\"foo\"\\r\\n\\r\\n' +\n+        'bar\\r\\n' +\n+        `--${boundary}--\\r\\n`\n+      const req = new HonoRequest(\n+        new Request('http://localhost', {\n+          method: 'POST',\n+          headers: { 'Content-Type': `multipart/form-data; boundary=${boundary}` },\n+          body: multipart,\n+        })\n+      )\n+      await req.text()\n+      expect((await req.formData()).get('foo')).toBe('bar')\n+    })\n+\n+    test('the cached representation is still returned unchanged', async () => {\n+      const req = new HonoRequest(\n+        new Request('http://localhost', {\n+          method: 'POST',\n+          headers: { 'Content-Type': urlencoded },\n+          body,\n+        })\n+      )\n+      expect(await req.text()).toBe(body)\n+      expect(await req.text()).toBe(body)\n+      expect(await req.arrayBuffer()).toEqual(new TextEncoder().encode(body).buffer)\n+    })\n+  })\n+\n   describe('req.parseBody()', async () => {\n     it('should parse form data', async () => {\n       const data = new FormData()\n       data.append('foo', 'bar')\n       const req = new HonoRequest(","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix(request): keep the request media type when reusing a cached body (#5366)","issue_url":"https://github.com/honojs/hono/issues/5365","pr_url":"https://github.com/honojs/hono/pull/5366"},"needs_manual_review":true}
{"id":"hono-f147de5-1","repo":"honojs/hono","license":"MIT","commit":"f147de5ea1eeadd4b4851ab075999673e140a303","parent":"90e1b948467961718afd3ae34af9dee582b42248","file":"src/helper/accepts/accepts.test.ts","language":"typescript","hunk_header":"@@ -188,10 +188,58 @@ describe('accepts', () => {","before":"      match,\n    }\n    const result = accepts(c, options)\n    expect(result).toBe('application/xml')\n  })\n})\n\ndescribe('Usage', () => {\n  test('decide compression by Accept-Encoding header', async () => {\n    const app = new Hono()","after":"      match,\n    }\n    const result = accepts(c, options)\n    expect(result).toBe('application/xml')\n  })\n\n  test('should not match entries explicitly rejected with q=0', () => {\n    const c = {\n      req: {\n        header: () => 'application/json;q=0',\n      },\n      // eslint-disable-next-line @typescript-eslint/no-explicit-any\n    } as any\n    const options: acceptsConfig = {\n      header: 'Accept',\n      supports: ['application/json'],\n      default: 'text/html',\n    }\n    const result = accepts(c, options)\n    expect(result).toBe('text/html')\n  })\n\n  test('should skip q=0 entries and match the remaining ones', () => {\n    const c = {\n      req: {\n        header: () => 'text/html;q=0.5, application/json;q=0',\n      },\n      // eslint-disable-next-line @typescript-eslint/no-explicit-any\n    } as any\n    const options: acceptsConfig = {\n      header: 'Accept',\n      supports: ['application/json'],\n      default: 'application/octet-stream',\n    }\n    const result = accepts(c, options)\n    expect(result).toBe('application/octet-stream')\n  })\n\n  test('should not match a subtype wildcard the client rejected with q=0', () => {\n    const c = {\n      req: {\n        header: () => 'application/*;q=0',\n      },\n      // eslint-disable-next-line @typescript-eslint/no-explicit-any\n    } as any\n    const options: acceptsConfig = {\n      header: 'Accept',\n      supports: ['application/json'],\n      default: 'text/html',\n    }\n    const result = accepts(c, options)\n    expect(result).toBe('text/html')\n  })\n})\n\ndescribe('Usage', () => {\n  test('decide compression by Accept-Encoding header', async () => {\n    const app = new Hono()","diff":"@@ -188,10 +188,58 @@ describe('accepts', () => {\n       match,\n     }\n     const result = accepts(c, options)\n     expect(result).toBe('application/xml')\n   })\n+\n+  test('should not match entries explicitly rejected with q=0', () => {\n+    const c = {\n+      req: {\n+        header: () => 'application/json;q=0',\n+      },\n+      // eslint-disable-next-line @typescript-eslint/no-explicit-any\n+    } as any\n+    const options: acceptsConfig = {\n+      header: 'Accept',\n+      supports: ['application/json'],\n+      default: 'text/html',\n+    }\n+    const result = accepts(c, options)\n+    expect(result).toBe('text/html')\n+  })\n+\n+  test('should skip q=0 entries and match the remaining ones', () => {\n+    const c = {\n+      req: {\n+        header: () => 'text/html;q=0.5, application/json;q=0',\n+      },\n+      // eslint-disable-next-line @typescript-eslint/no-explicit-any\n+    } as any\n+    const options: acceptsConfig = {\n+      header: 'Accept',\n+      supports: ['application/json'],\n+      default: 'application/octet-stream',\n+    }\n+    const result = accepts(c, options)\n+    expect(result).toBe('application/octet-stream')\n+  })\n+\n+  test('should not match a subtype wildcard the client rejected with q=0', () => {\n+    const c = {\n+      req: {\n+        header: () => 'application/*;q=0',\n+      },\n+      // eslint-disable-next-line @typescript-eslint/no-explicit-any\n+    } as any\n+    const options: acceptsConfig = {\n+      header: 'Accept',\n+      supports: ['application/json'],\n+      default: 'text/html',\n+    }\n+    const result = accepts(c, options)\n+    expect(result).toBe('text/html')\n+  })\n })\n \n describe('Usage', () => {\n   test('decide compression by Accept-Encoding header', async () => {\n     const app = new Hono()","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix(accepts, language): skip accept entries with quality 0 when matching (#5311)","issue_url":"https://github.com/honojs/hono/issues/5310","pr_url":"https://github.com/honojs/hono/pull/5311"},"needs_manual_review":true}
{"id":"hono-90e1b94-1","repo":"honojs/hono","license":"MIT","commit":"90e1b948467961718afd3ae34af9dee582b42248","parent":"7792f5dc14acaa92056c1bf402241567ff9c7396","file":"src/adapter/aws-lambda/handler.ts","language":"typescript","hunk_header":"@@ -121,22 +122,25 @@ const getRequestContext = (","before":"  | ALBRequestContext\n  | LatticeRequestContextV2 => {\n  return event.requestContext\n}\n\nconst streamToNodeStream = async (\n  reader: ReadableStreamDefaultReader<Uint8Array>,\n  writer: NodeJS.WritableStream\n): Promise<void> => {\n  let readResult = await reader.read()\n  while (!readResult.done) {\n    writer.write(readResult.value)\n    readResult = await reader.read()\n  }\n  writer.end()\n}\n\nexport const streamHandle = <\n  E extends Env = Env,\n  S extends Schema = {},\n  BasePath extends string = '/',\n>(","after":"  | ALBRequestContext\n  | LatticeRequestContextV2 => {\n  return event.requestContext\n}\n\nasync function* readWebStream(\n  reader: ReadableStreamDefaultReader<Uint8Array>\n): AsyncGenerator<Uint8Array> {\n  let readResult = await reader.read()\n  while (!readResult.done) {\n    yield readResult.value\n    readResult = await reader.read()\n  }\n}\n\nconst streamToNodeStream = (\n  reader: ReadableStreamDefaultReader<Uint8Array>,\n  writer: NodeJS.WritableStream\n): Promise<void> => pipeline(readWebStream(reader), writer)\n\nexport const streamHandle = <\n  E extends Env = Env,\n  S extends Schema = {},\n  BasePath extends string = '/',\n>(","diff":"@@ -121,22 +122,25 @@ const getRequestContext = (\n   | ALBRequestContext\n   | LatticeRequestContextV2 => {\n   return event.requestContext\n }\n \n-const streamToNodeStream = async (\n-  reader: ReadableStreamDefaultReader<Uint8Array>,\n-  writer: NodeJS.WritableStream\n-): Promise<void> => {\n+async function* readWebStream(\n+  reader: ReadableStreamDefaultReader<Uint8Array>\n+): AsyncGenerator<Uint8Array> {\n   let readResult = await reader.read()\n   while (!readResult.done) {\n-    writer.write(readResult.value)\n+    yield readResult.value\n     readResult = await reader.read()\n   }\n-  writer.end()\n }\n \n+const streamToNodeStream = (\n+  reader: ReadableStreamDefaultReader<Uint8Array>,\n+  writer: NodeJS.WritableStream\n+): Promise<void> => pipeline(readWebStream(reader), writer)\n+\n export const streamHandle = <\n   E extends Env = Env,\n   S extends Schema = {},\n   BasePath extends string = '/',\n >(","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix(aws-lambda): respect backpressure when streaming the response body (#5351)","issue_url":"https://github.com/honojs/hono/issues/5333","pr_url":"https://github.com/honojs/hono/pull/5351"},"needs_manual_review":true}
{"id":"hono-e2740d5-1","repo":"honojs/hono","license":"MIT","commit":"e2740d5a1bd0b4254e517e3af8b60789284bc7bd","parent":"499c35ebda35777fd35a7dd1906dd4f2687da61e","file":"src/context.test.ts","language":"typescript","hunk_header":"@@ -142,10 +142,17 @@ describe('Context', () => {","before":"    const res = c.body('Hi')\n    const foo = res.headers.get('X-Foo')\n    expect(foo).toBe('Bar, Buzz')\n  })\n\n  it('c.set() and c.get()', async () => {\n    expect(c.get('foo')).toBe(undefined)\n    c.set('foo', 'bar')\n    expect(c.get('foo')).toBe('bar')\n    expect(c.get('foo2')).toBe(undefined)","after":"    const res = c.body('Hi')\n    const foo = res.headers.get('X-Foo')\n    expect(foo).toBe('Bar, Buzz')\n  })\n\n  it('c.set() and c.get() with symbol key', () => {\n    const sym = Symbol('key')\n    expect(c.get(sym)).toBe(undefined)\n    c.set(sym, 'value')\n    expect(c.get(sym)).toBe('value')\n  })\n\n  it('c.set() and c.get()', async () => {\n    expect(c.get('foo')).toBe(undefined)\n    c.set('foo', 'bar')\n    expect(c.get('foo')).toBe('bar')\n    expect(c.get('foo2')).toBe(undefined)","diff":"@@ -142,10 +142,17 @@ describe('Context', () => {\n     const res = c.body('Hi')\n     const foo = res.headers.get('X-Foo')\n     expect(foo).toBe('Bar, Buzz')\n   })\n \n+  it('c.set() and c.get() with symbol key', () => {\n+    const sym = Symbol('key')\n+    expect(c.get(sym)).toBe(undefined)\n+    c.set(sym, 'value')\n+    expect(c.get(sym)).toBe('value')\n+  })\n+\n   it('c.set() and c.get()', async () => {\n     expect(c.get('foo')).toBe(undefined)\n     c.set('foo', 'bar')\n     expect(c.get('foo')).toBe('bar')\n     expect(c.get('foo2')).toBe(undefined)","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix(types): allow symbol keys in Context<any> get and set fallbacks (#5300)","issue_url":"https://github.com/honojs/hono/issues/5298","pr_url":"https://github.com/honojs/hono/pull/5300"},"needs_manual_review":true}
{"id":"hono-a10592f-1","repo":"honojs/hono","license":"MIT","commit":"a10592f3e1864cbe277619633db953450a6b1e4b","parent":"48e360fcaa107c7706fcf8adccb93505876791dc","file":"src/router/common.case.test.ts","language":"typescript","hunk_header":"@@ -395,10 +395,20 @@ export const runTest = ({","before":"\n      it('GET /asset', async () => {\n        const res = match('GET', '/asset')\n        expect(res.length).toBe(0)\n      })\n    })\n\n    describe('Trailing wildcard', () => {\n      beforeEach(() => {\n        router.add('GET', '/path/*', 'path')","after":"\n      it('GET /asset', async () => {\n        const res = match('GET', '/asset')\n        expect(res.length).toBe(0)\n      })\n\n      it.each([\n        ['/assets*', '/assets/app.js'],\n        ['/assets/app.js', '/assets*'],\n      ])('associates %s before %s', async (first, second) => {\n        router.add('POST', first, first)\n        router.add('POST', second, second)\n        const res = match('POST', '/assets/app.js')\n        expect(res.map(({ handler }) => handler)).toEqual([first, second])\n      })\n    })\n\n    describe('Trailing wildcard', () => {\n      beforeEach(() => {\n        router.add('GET', '/path/*', 'path')","diff":"@@ -395,10 +395,20 @@ export const runTest = ({\n \n       it('GET /asset', async () => {\n         const res = match('GET', '/asset')\n         expect(res.length).toBe(0)\n       })\n+\n+      it.each([\n+        ['/assets*', '/assets/app.js'],\n+        ['/assets/app.js', '/assets*'],\n+      ])('associates %s before %s', async (first, second) => {\n+        router.add('POST', first, first)\n+        router.add('POST', second, second)\n+        const res = match('POST', '/assets/app.js')\n+        expect(res.map(({ handler }) => handler)).toEqual([first, second])\n+      })\n     })\n \n     describe('Trailing wildcard', () => {\n       beforeEach(() => {\n         router.add('GET', '/path/*', 'path')","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix(reg-exp-router): associate wildcard middleware with matching routes (#5266)","issue_url":"https://github.com/honojs/hono/issues/5258","pr_url":"https://github.com/honojs/hono/pull/5266"},"needs_manual_review":true}
{"id":"hono-48e360f-1","repo":"honojs/hono","license":"MIT","commit":"48e360fcaa107c7706fcf8adccb93505876791dc","parent":"7967760748503b50de7c315fe3aa76666a8e6cab","file":"src/jsx/dom/render.ts","language":"typescript","hunk_header":"@@ -185,10 +185,11 @@ const applyProps = (","before":"          }\n        }\n      } else if (key === 'dangerouslySetInnerHTML' && value) {\n        container.innerHTML = value.__html\n      } else if (key === 'ref') {\n        let cleanup\n        if (typeof value === 'function') {\n          cleanup = value(container) || (() => value(null))\n        } else if (value && 'current' in value) {\n          value.current = container","after":"          }\n        }\n      } else if (key === 'dangerouslySetInnerHTML' && value) {\n        container.innerHTML = value.__html\n      } else if (key === 'ref') {\n        refCleanupMap.get(container)?.()\n        let cleanup\n        if (typeof value === 'function') {\n          cleanup = value(container) || (() => value(null))\n        } else if (value && 'current' in value) {\n          value.current = container","diff":"@@ -185,10 +185,11 @@ const applyProps = (\n           }\n         }\n       } else if (key === 'dangerouslySetInnerHTML' && value) {\n         container.innerHTML = value.__html\n       } else if (key === 'ref') {\n+        refCleanupMap.get(container)?.()\n         let cleanup\n         if (typeof value === 'function') {\n           cleanup = value(container) || (() => value(null))\n         } else if (value && 'current' in value) {\n           value.current = container","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix(jsx/dom): execute previous ref cleanup when ref prop changes on re-render (#5264)","issue_url":"https://github.com/honojs/hono/issues/5262","pr_url":"https://github.com/honojs/hono/pull/5264"},"needs_manual_review":true}
{"id":"hono-63bbcf5-1","repo":"honojs/hono","license":"MIT","commit":"63bbcf508c74d9fe7a3e20f4dfe7ffacc566dcce","parent":"ef0739d5f6e83242ba2b64c1365c9f96738933a1","file":"src/hono.test.ts","language":"typescript","hunk_header":"@@ -471,10 +471,24 @@ describe('Routing', () => {","before":"    res = await app.request('http://localhost/add-path-after-route-call', { method: 'GET' })\n    expect(res.status).toBe(200)\n    expect(await res.text()).toBe('get /add-path-after-route-call')\n  })\n\n  it('Nested route - subApp with basePath', async () => {\n    const app = new Hono()\n    const book = new Hono().basePath('/book')\n    book.get('/', (c) => c.text('get /book'))\n    app.route('/api', book)","after":"    res = await app.request('http://localhost/add-path-after-route-call', { method: 'GET' })\n    expect(res.status).toBe(200)\n    expect(await res.text()).toBe('get /add-path-after-route-call')\n  })\n\n  it('Should match a suffix wildcard after falling back to TrieRouter', async () => {\n    const app = new Hono()\n    const sub = new Hono()\n    sub.post('/items', (c) => c.text('items'))\n    sub.post('/:slug', (c) => c.text('slug'))\n    app.route('/api', sub)\n    app.get('/assets*', (c) => c.text('asset'))\n\n    const res = await app.request('http://localhost/assets/app.js')\n    expect(res.status).toBe(200)\n    expect(await res.text()).toBe('asset')\n    expect(app.router.name).toBe('SmartRouter + TrieRouter')\n  })\n\n  it('Nested route - subApp with basePath', async () => {\n    const app = new Hono()\n    const book = new Hono().basePath('/book')\n    book.get('/', (c) => c.text('get /book'))\n    app.route('/api', book)","diff":"@@ -471,10 +471,24 @@ describe('Routing', () => {\n     res = await app.request('http://localhost/add-path-after-route-call', { method: 'GET' })\n     expect(res.status).toBe(200)\n     expect(await res.text()).toBe('get /add-path-after-route-call')\n   })\n \n+  it('Should match a suffix wildcard after falling back to TrieRouter', async () => {\n+    const app = new Hono()\n+    const sub = new Hono()\n+    sub.post('/items', (c) => c.text('items'))\n+    sub.post('/:slug', (c) => c.text('slug'))\n+    app.route('/api', sub)\n+    app.get('/assets*', (c) => c.text('asset'))\n+\n+    const res = await app.request('http://localhost/assets/app.js')\n+    expect(res.status).toBe(200)\n+    expect(await res.text()).toBe('asset')\n+    expect(app.router.name).toBe('SmartRouter + TrieRouter')\n+  })\n+\n   it('Nested route - subApp with basePath', async () => {\n     const app = new Hono()\n     const book = new Hono().basePath('/book')\n     book.get('/', (c) => c.text('get /book'))\n     app.route('/api', book)","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix(trie-router): match suffix wildcard routes (#5236)","issue_url":"https://github.com/honojs/hono/issues/5219","pr_url":"https://github.com/honojs/hono/pull/5236"},"needs_manual_review":true}
{"id":"hono-f6aa913-1","repo":"honojs/hono","license":"MIT","commit":"f6aa913c3f8915d0695b77ca3f78f6b8ecb248a0","parent":"cd31bc196eb234bcaaae18ecf889784cf30004fd","file":"src/middleware/etag/index.test.ts","language":"typescript","hunk_header":"@@ -271,33 +271,49 @@ describe('Etag Middleware', () => {","before":"      },\n    })\n    expect(res.status).toBe(304)\n  })\n\n  it('Should not return 304 for `If-None-Match: *` on unsafe methods or error responses', async () => {\n    const app = new Hono()\n    app.use('/etag/*', etag())\n    app.put('/etag/put', (c) => c.text('created', 201))\n    app.get('/etag/not-found', (c) => c.text('not found', 404))\n\n    // `*` does not apply to unsafe methods:\n    let res = await app.request('http://localhost/etag/put', {\n      method: 'PUT',\n      headers: {\n        'If-None-Match': '*',\n      },\n    })\n    expect(res.status).toBe(201)\n    expect(await res.text()).toBe('created')\n\n    // `*` does not apply when there is no current representation:\n    res = await app.request('http://localhost/etag/not-found', {\n      headers: {\n        'If-None-Match': '*',\n      },\n    })\n    expect(res.status).toBe(404)\n  })\n\n  it('Should not return duplicate etag header values', async () => {\n    const app = new Hono()\n    app.use('/etag/*', etag())","after":"      },\n    })\n    expect(res.status).toBe(304)\n  })\n\n  it('Should not return 304 on unsafe methods or error responses', async () => {\n    const app = new Hono()\n    app.use('/etag/*', etag())\n    app.put('/etag/put', (c) => c.text('created', 201, { ETag: '\"etag-123\"' }))\n    app.get('/etag/not-found', (c) => c.text('not found', 404, { ETag: '\"etag-123\"' }))\n\n    // Unsafe methods:\n    let res = await app.request('http://localhost/etag/put', {\n      method: 'PUT',\n      headers: {\n        'If-None-Match': '*',\n      },\n    })\n    expect(res.status).toBe(201)\n    expect(await res.text()).toBe('created')\n\n    res = await app.request('http://localhost/etag/put', {\n      method: 'PUT',\n      headers: {\n        'If-None-Match': '\"etag-123\"',\n      },\n    })\n    expect(res.status).toBe(201)\n    expect(await res.text()).toBe('created')\n\n    // Error responses:\n    res = await app.request('http://localhost/etag/not-found', {\n      headers: {\n        'If-None-Match': '*',\n      },\n    })\n    expect(res.status).toBe(404)\n\n    res = await app.request('http://localhost/etag/not-found', {\n      headers: {\n        'If-None-Match': '\"etag-123\"',\n      },\n    })\n    expect(res.status).toBe(404)\n  })\n\n  it('Should not return duplicate etag header values', async () => {\n    const app = new Hono()\n    app.use('/etag/*', etag())","diff":"@@ -271,33 +271,49 @@ describe('Etag Middleware', () => {\n       },\n     })\n     expect(res.status).toBe(304)\n   })\n \n-  it('Should not return 304 for `If-None-Match: *` on unsafe methods or error responses', async () => {\n+  it('Should not return 304 on unsafe methods or error responses', async () => {\n     const app = new Hono()\n     app.use('/etag/*', etag())\n-    app.put('/etag/put', (c) => c.text('created', 201))\n-    app.get('/etag/not-found', (c) => c.text('not found', 404))\n+    app.put('/etag/put', (c) => c.text('created', 201, { ETag: '\"etag-123\"' }))\n+    app.get('/etag/not-found', (c) => c.text('not found', 404, { ETag: '\"etag-123\"' }))\n \n-    // `*` does not apply to unsafe methods:\n+    // Unsafe methods:\n     let res = await app.request('http://localhost/etag/put', {\n       method: 'PUT',\n       headers: {\n         'If-None-Match': '*',\n       },\n     })\n     expect(res.status).toBe(201)\n     expect(await res.text()).toBe('created')\n \n-    // `*` does not apply when there is no current representation:\n+    res = await app.request('http://localhost/etag/put', {\n+      method: 'PUT',\n+      headers: {\n+        'If-None-Match': '\"etag-123\"',\n+      },\n+    })\n+    expect(res.status).toBe(201)\n+    expect(await res.text()).toBe('created')\n+\n+    // Error responses:\n     res = await app.request('http://localhost/etag/not-found', {\n       headers: {\n         'If-None-Match': '*',\n       },\n     })\n     expect(res.status).toBe(404)\n+\n+    res = await app.request('http://localhost/etag/not-found', {\n+      headers: {\n+        'If-None-Match': '\"etag-123\"',\n+      },\n+    })\n+    expect(res.status).toBe(404)\n   })\n \n   it('Should not return duplicate etag header values', async () => {\n     const app = new Hono()\n     app.use('/etag/*', etag())","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix(etag): skip unsafe methods or error responses on non-* case (#5196)","issue_url":"https://github.com/honojs/hono/issues/5191","pr_url":"https://github.com/honojs/hono/pull/5196"},"needs_manual_review":true}
{"id":"hono-cd31bc1-1","repo":"honojs/hono","license":"MIT","commit":"cd31bc196eb234bcaaae18ecf889784cf30004fd","parent":"569b4191a291ef7a4116881e521c3a0179407332","file":"src/utils/stream.test.ts","language":"typescript","hunk_header":"@@ -55,10 +55,48 @@ describe('StreamingApi', () => {","before":"    const reader = api.responseReadable.getReader()\n    expect((await reader.read()).value).toEqual(new TextEncoder().encode('foo'))\n    expect((await reader.read()).value).toEqual(new TextEncoder().encode('bar'))\n  })\n\n  it('close()', async () => {\n    const { readable, writable } = new TransformStream()\n    const api = new StreamingApi(writable, readable)\n    const reader = api.responseReadable.getReader()\n    await api.close()","after":"    const reader = api.responseReadable.getReader()\n    expect((await reader.read()).value).toEqual(new TextEncoder().encode('foo'))\n    expect((await reader.read()).value).toEqual(new TextEncoder().encode('bar'))\n  })\n\n  it('pipe() re-acquires writer lock when pipeTo() throws', async () => {\n    const { readable, writable } = new TransformStream()\n    const api = new StreamingApi(writable, readable)\n    const reader = api.responseReadable.getReader()\n\n    const erroringReadable = new ReadableStream({\n      start(controller) {\n        controller.error(new Error('upstream error'))\n      },\n    })\n\n    await expect(api.pipe(erroringReadable)).rejects.toThrow('upstream error')\n    api.write('after pipe')\n    expect((await reader.read()).value).toEqual(new TextEncoder().encode('after pipe'))\n  })\n\n  it('pipe() can be called again after a previous pipe() throws', async () => {\n    const { readable, writable } = new TransformStream()\n    const api = new StreamingApi(writable, readable)\n    const reader = api.responseReadable.getReader()\n\n    const erroringReadable = new ReadableStream({\n      start(controller) {\n        controller.error(new Error('upstream error'))\n      },\n    })\n    await expect(api.pipe(erroringReadable)).rejects.toThrow('upstream error')\n\n    const src = new ReadableStream({\n      start(controller) {\n        controller.enqueue(new TextEncoder().encode('second pipe'))\n        controller.close()\n      },\n    })\n    await api.pipe(src)\n    expect((await reader.read()).value).toEqual(new TextEncoder().encode('second pipe'))\n  })\n\n  it('close()', async () => {\n    const { readable, writable } = new TransformStream()\n    const api = new StreamingApi(writable, readable)\n    const reader = api.responseReadable.getReader()\n    await api.close()","diff":"@@ -55,10 +55,48 @@ describe('StreamingApi', () => {\n     const reader = api.responseReadable.getReader()\n     expect((await reader.read()).value).toEqual(new TextEncoder().encode('foo'))\n     expect((await reader.read()).value).toEqual(new TextEncoder().encode('bar'))\n   })\n \n+  it('pipe() re-acquires writer lock when pipeTo() throws', async () => {\n+    const { readable, writable } = new TransformStream()\n+    const api = new StreamingApi(writable, readable)\n+    const reader = api.responseReadable.getReader()\n+\n+    const erroringReadable = new ReadableStream({\n+      start(controller) {\n+        controller.error(new Error('upstream error'))\n+      },\n+    })\n+\n+    await expect(api.pipe(erroringReadable)).rejects.toThrow('upstream error')\n+    api.write('after pipe')\n+    expect((await reader.read()).value).toEqual(new TextEncoder().encode('after pipe'))\n+  })\n+\n+  it('pipe() can be called again after a previous pipe() throws', async () => {\n+    const { readable, writable } = new TransformStream()\n+    const api = new StreamingApi(writable, readable)\n+    const reader = api.responseReadable.getReader()\n+\n+    const erroringReadable = new ReadableStream({\n+      start(controller) {\n+        controller.error(new Error('upstream error'))\n+      },\n+    })\n+    await expect(api.pipe(erroringReadable)).rejects.toThrow('upstream error')\n+\n+    const src = new ReadableStream({\n+      start(controller) {\n+        controller.enqueue(new TextEncoder().encode('second pipe'))\n+        controller.close()\n+      },\n+    })\n+    await api.pipe(src)\n+    expect((await reader.read()).value).toEqual(new TextEncoder().encode('second pipe'))\n+  })\n+\n   it('close()', async () => {\n     const { readable, writable } = new TransformStream()\n     const api = new StreamingApi(writable, readable)\n     const reader = api.responseReadable.getReader()\n     await api.close()","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix(utils/stream): re-acquire writer lock when pipe() throws (#4988)","issue_url":"https://github.com/honojs/hono/issues/4981","pr_url":"https://github.com/honojs/hono/pull/4988"},"needs_manual_review":true}
{"id":"hono-224d2f5-1","repo":"honojs/hono","license":"MIT","commit":"224d2f5cbf2b4bc2ebb7482d0592149a8d9f0574","parent":"df653cea0450657977887d56a5188368387456c8","file":"src/jsx/hooks/index.ts","language":"typescript","hunk_header":"@@ -406,20 +406,28 @@ export const useSyncExternalStore = <T>(","before":"      throw new Error('getServerSnapshot is required for server side rendering')\n    }\n    return getServerSnapshot()\n  }\n\n  const [serverSnapshotIsUsed] = useState<boolean>(!!(buildData[0][4] && getServerSnapshot))\n  const [state, setState] = useState(() =>\n    serverSnapshotIsUsed ? (getServerSnapshot as () => T)() : getSnapshot()\n  )\n  useEffect(() => {\n    if (serverSnapshotIsUsed) {\n      setState(getSnapshot())\n    }\n    return subscribe(() => {\n      setState(getSnapshot())\n    })\n  }, [])\n\n  return state\n}","after":"      throw new Error('getServerSnapshot is required for server side rendering')\n    }\n    return getServerSnapshot()\n  }\n\n  const snapshot =\n    buildData[0][4] && getServerSnapshot ? (getServerSnapshot as () => T)() : getSnapshot()\n  const [, setVersion] = useState(0)\n  const latestSnapshot = useRef<[T, () => T]>([snapshot, getSnapshot])\n  latestSnapshot.current = [snapshot, getSnapshot]\n  const unsubscribeRef = useRef<() => void>(null)\n\n  // Swap subscriptions at effect flush: a returned cleanup would run synchronously\n  // during render when `subscribe` changes, leaving a window with no subscription.\n  useEffect(() => {\n    const update = () => setVersion((version) => version + 1)\n    unsubscribeRef.current?.()\n    unsubscribeRef.current = subscribe(update)\n    const [snapshot, getSnapshot] = latestSnapshot.current!\n    if (!Object.is(snapshot, getSnapshot())) {\n      update()\n    }\n  }, [subscribe])\n\n  useEffect(() => () => unsubscribeRef.current?.(), [])\n\n  return snapshot\n}","diff":"@@ -406,20 +406,28 @@ export const useSyncExternalStore = <T>(\n       throw new Error('getServerSnapshot is required for server side rendering')\n     }\n     return getServerSnapshot()\n   }\n \n-  const [serverSnapshotIsUsed] = useState<boolean>(!!(buildData[0][4] && getServerSnapshot))\n-  const [state, setState] = useState(() =>\n-    serverSnapshotIsUsed ? (getServerSnapshot as () => T)() : getSnapshot()\n-  )\n+  const snapshot =\n+    buildData[0][4] && getServerSnapshot ? (getServerSnapshot as () => T)() : getSnapshot()\n+  const [, setVersion] = useState(0)\n+  const latestSnapshot = useRef<[T, () => T]>([snapshot, getSnapshot])\n+  latestSnapshot.current = [snapshot, getSnapshot]\n+  const unsubscribeRef = useRef<() => void>(null)\n+\n+  // Swap subscriptions at effect flush: a returned cleanup would run synchronously\n+  // during render when `subscribe` changes, leaving a window with no subscription.\n   useEffect(() => {\n-    if (serverSnapshotIsUsed) {\n-      setState(getSnapshot())\n+    const update = () => setVersion((version) => version + 1)\n+    unsubscribeRef.current?.()\n+    unsubscribeRef.current = subscribe(update)\n+    const [snapshot, getSnapshot] = latestSnapshot.current!\n+    if (!Object.is(snapshot, getSnapshot())) {\n+      update()\n     }\n-    return subscribe(() => {\n-      setState(getSnapshot())\n-    })\n-  }, [])\n+  }, [subscribe])\n+\n+  useEffect(() => () => unsubscribeRef.current?.(), [])\n \n-  return state\n+  return snapshot\n }","label":{"defect":true,"category":"bugfix","touches_public_api":true,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix(jsx): handle useSyncExternalStore subscription and snapshot changes (#5166)","issue_url":"https://github.com/honojs/hono/issues/5162","pr_url":"https://github.com/honojs/hono/pull/5166"},"needs_manual_review":true}
{"id":"hono-09cf01c-1","repo":"honojs/hono","license":"MIT","commit":"09cf01c0a85d4e73439ff5fa26bb475d493a545c","parent":"26d8e42bfff7eb78fc323a01e1d02d63b9dbcc72","file":"src/utils/cookie.test.ts","language":"typescript","hunk_header":"@@ -78,10 +78,36 @@ describe('Parse cookie', () => {","before":"    expect(cookie['tasty cookie']).toBeUndefined()\n    expect(cookie['best_cookie\\\\']).toBeUndefined()\n    expect(cookie['']).toBeUndefined()\n  })\n\n  it('Should ignore invalid cookie values', () => {\n    const cookieString = 'yummy_cookie=choco\\\\nchip; tasty_cookie=strawberry; best_cookie=\"sugar'\n    const cookie: Cookie = parse(cookieString)\n    expect(cookie['yummy_cookie']).toBeUndefined()\n    expect(cookie['tasty_cookie']).toBe('strawberry')","after":"    expect(cookie['tasty cookie']).toBeUndefined()\n    expect(cookie['best_cookie\\\\']).toBeUndefined()\n    expect(cookie['']).toBeUndefined()\n  })\n\n  it('Should parse cookies whose names are not RFC 6265 tokens', () => {\n    const cookieString =\n      'paraglide:lang=en; CognitoIdentityServiceProvider.abc.user@example.com.idToken=token; round(brackets)=ok; square[brackets]=ok; curly{brackets}=ok'\n    const cookie: Cookie = parse(cookieString)\n    expect(cookie['paraglide:lang']).toBe('en')\n    expect(cookie['CognitoIdentityServiceProvider.abc.user@example.com.idToken']).toBe('token')\n    expect(cookie['round(brackets)']).toBe('ok')\n    expect(cookie['square[brackets]']).toBe('ok')\n    expect(cookie['curly{brackets}']).toBe('ok')\n  })\n\n  it('Should parse one cookie specified by a name that is not an RFC 6265 token', () => {\n    const cookieString = 'paraglide:lang=en; test=ok'\n    const cookie: Cookie = parse(cookieString, 'paraglide:lang')\n    expect(cookie['paraglide:lang']).toBe('en')\n    expect(cookie['test']).toBeUndefined()\n  })\n\n  it('Should ignore cookie names containing non-ASCII or control characters', () => {\n    const cookieString = 'yummy_cookie=choco; クッキー=strawberry; bad\\x01name=sugar'\n    const cookie: Cookie = parse(cookieString)\n    expect(cookie['yummy_cookie']).toBe('choco')\n    expect(cookie['クッキー']).toBeUndefined()\n    expect(cookie['bad\\x01name']).toBeUndefined()\n  })\n\n  it('Should ignore invalid cookie values', () => {\n    const cookieString = 'yummy_cookie=choco\\\\nchip; tasty_cookie=strawberry; best_cookie=\"sugar'\n    const cookie: Cookie = parse(cookieString)\n    expect(cookie['yummy_cookie']).toBeUndefined()\n    expect(cookie['tasty_cookie']).toBe('strawberry')","diff":"@@ -78,10 +78,36 @@ describe('Parse cookie', () => {\n     expect(cookie['tasty cookie']).toBeUndefined()\n     expect(cookie['best_cookie\\\\']).toBeUndefined()\n     expect(cookie['']).toBeUndefined()\n   })\n \n+  it('Should parse cookies whose names are not RFC 6265 tokens', () => {\n+    const cookieString =\n+      'paraglide:lang=en; CognitoIdentityServiceProvider.abc.user@example.com.idToken=token; round(brackets)=ok; square[brackets]=ok; curly{brackets}=ok'\n+    const cookie: Cookie = parse(cookieString)\n+    expect(cookie['paraglide:lang']).toBe('en')\n+    expect(cookie['CognitoIdentityServiceProvider.abc.user@example.com.idToken']).toBe('token')\n+    expect(cookie['round(brackets)']).toBe('ok')\n+    expect(cookie['square[brackets]']).toBe('ok')\n+    expect(cookie['curly{brackets}']).toBe('ok')\n+  })\n+\n+  it('Should parse one cookie specified by a name that is not an RFC 6265 token', () => {\n+    const cookieString = 'paraglide:lang=en; test=ok'\n+    const cookie: Cookie = parse(cookieString, 'paraglide:lang')\n+    expect(cookie['paraglide:lang']).toBe('en')\n+    expect(cookie['test']).toBeUndefined()\n+  })\n+\n+  it('Should ignore cookie names containing non-ASCII or control characters', () => {\n+    const cookieString = 'yummy_cookie=choco; クッキー=strawberry; bad\\x01name=sugar'\n+    const cookie: Cookie = parse(cookieString)\n+    expect(cookie['yummy_cookie']).toBe('choco')\n+    expect(cookie['クッキー']).toBeUndefined()\n+    expect(cookie['bad\\x01name']).toBeUndefined()\n+  })\n+\n   it('Should ignore invalid cookie values', () => {\n     const cookieString = 'yummy_cookie=choco\\\\nchip; tasty_cookie=strawberry; best_cookie=\"sugar'\n     const cookie: Cookie = parse(cookieString)\n     expect(cookie['yummy_cookie']).toBeUndefined()\n     expect(cookie['tasty_cookie']).toBe('strawberry')","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix(cookie): relax name validation when parsing Cookie header (#5164)","issue_url":"https://github.com/honojs/hono/issues/3189","pr_url":"https://github.com/honojs/hono/pull/5164"},"needs_manual_review":true}
{"id":"hono-e36f57d-1","repo":"honojs/hono","license":"MIT","commit":"e36f57dac0615c1bdd03ff1975b6ca8d3dc5917f","parent":"bf8608cb77f91c3768311d9977f807ec2f3fd6b9","file":"src/adapter/aws-lambda/handler.test.ts","language":"typescript","hunk_header":"@@ -1,15 +1,16 @@","before":"import { setCookie } from '../../helper/cookie'\nimport { Hono } from '../../hono'\nimport { bodyLimit } from '../../middleware/body-limit'\nimport type { LambdaEvent, LatticeProxyEventV2 } from './handler'\nimport {\n  getProcessor,\n  handle,\n  isContentEncodingBinary,\n  defaultIsContentTypeBinary,\n} from './handler'\n\n// Base event objects to reduce duplication\nconst baseV1Event: LambdaEvent = {\n  version: '1.0',\n  resource: '/my/path',","after":"import { setCookie } from '../../helper/cookie'\nimport { Hono } from '../../hono'\nimport { bodyLimit } from '../../middleware/body-limit'\nimport type { APIGatewayProxyEventV2, LambdaEvent, LatticeProxyEventV2 } from './handler'\nimport {\n  getProcessor,\n  handle,\n  isContentEncodingBinary,\n  defaultIsContentTypeBinary,\n} from './handler'\nimport type { ApiGatewayRequestContextV2 } from './types'\n\n// Base event objects to reduce duplication\nconst baseV1Event: LambdaEvent = {\n  version: '1.0',\n  resource: '/my/path',","diff":"@@ -1,15 +1,16 @@\n import { setCookie } from '../../helper/cookie'\n import { Hono } from '../../hono'\n import { bodyLimit } from '../../middleware/body-limit'\n-import type { LambdaEvent, LatticeProxyEventV2 } from './handler'\n+import type { APIGatewayProxyEventV2, LambdaEvent, LatticeProxyEventV2 } from './handler'\n import {\n   getProcessor,\n   handle,\n   isContentEncodingBinary,\n   defaultIsContentTypeBinary,\n } from './handler'\n+import type { ApiGatewayRequestContextV2 } from './types'\n \n // Base event objects to reduce duplication\n const baseV1Event: LambdaEvent = {\n   version: '1.0',\n   resource: '/my/path',","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix(aws-lambda): add jwt and lambda authorizer types for API Gateway v2 (#5142)","issue_url":"https://github.com/honojs/hono/issues/3281","pr_url":"https://github.com/honojs/hono/pull/5142"},"needs_manual_review":true}
{"id":"hono-80959d4-1","repo":"honojs/hono","license":"MIT","commit":"80959d47d56ac18c2985336ad311917dc56497c5","parent":"21b44b6db6118fc77fabb49832fc4cadd0ef8ec8","file":"src/request.test.ts","language":"typescript","hunk_header":"@@ -379,10 +379,26 @@ describe('Body methods with caching', () => {","before":"      expect(async () => await req.arrayBuffer()).not.toThrow()\n      expect(async () => await req.bytes()).not.toThrow()\n      expect(async () => await req.blob()).not.toThrow()\n    })\n\n    describe('should not break body methods after parseBody() with non-form content-type', () => {\n      const createReq = () =>\n        new HonoRequest(\n          new Request('http://localhost', {\n            method: 'POST',","after":"      expect(async () => await req.arrayBuffer()).not.toThrow()\n      expect(async () => await req.bytes()).not.toThrow()\n      expect(async () => await req.blob()).not.toThrow()\n    })\n\n    it('should parse form data even if formData() is called before parseBody()', async () => {\n      const data = new FormData()\n      data.append('foo', 'bar')\n      data.append('file', new File(['hello'], 't.txt', { type: 'text/plain' }))\n      const req = new HonoRequest(\n        new Request('http://localhost', {\n          method: 'POST',\n          body: data,\n        })\n      )\n      await req.formData()\n      const body = await req.parseBody()\n      expect(body['foo']).toBe('bar')\n      expect(body['file']).toBeInstanceOf(File)\n    })\n\n    describe('should not break body methods after parseBody() with non-form content-type', () => {\n      const createReq = () =>\n        new HonoRequest(\n          new Request('http://localhost', {\n            method: 'POST',","diff":"@@ -379,10 +379,26 @@ describe('Body methods with caching', () => {\n       expect(async () => await req.arrayBuffer()).not.toThrow()\n       expect(async () => await req.bytes()).not.toThrow()\n       expect(async () => await req.blob()).not.toThrow()\n     })\n \n+    it('should parse form data even if formData() is called before parseBody()', async () => {\n+      const data = new FormData()\n+      data.append('foo', 'bar')\n+      data.append('file', new File(['hello'], 't.txt', { type: 'text/plain' }))\n+      const req = new HonoRequest(\n+        new Request('http://localhost', {\n+          method: 'POST',\n+          body: data,\n+        })\n+      )\n+      await req.formData()\n+      const body = await req.parseBody()\n+      expect(body['foo']).toBe('bar')\n+      expect(body['file']).toBeInstanceOf(File)\n+    })\n+\n     describe('should not break body methods after parseBody() with non-form content-type', () => {\n       const createReq = () =>\n         new HonoRequest(\n           new Request('http://localhost', {\n             method: 'POST',","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix(utils/body): reuse cached formData in `parseBody()` (#5131)","issue_url":"https://github.com/honojs/hono/issues/5129","pr_url":"https://github.com/honojs/hono/pull/5131"},"needs_manual_review":true}
{"id":"hono-2126289-1","repo":"honojs/hono","license":"MIT","commit":"2126289c15b0f87fb928645c4ed92d0f81d21cd4","parent":"0fc7ffc94988d0dce19477d3e1cce29f3c2da1cb","file":"src/middleware/etag/index.test.ts","language":"typescript","hunk_header":"@@ -214,10 +214,43 @@ describe('Etag Middleware', () => {","before":"      headers: {\n        'If-None-Match': `\"mismatch 1\", ${etagHeaderValue}, \"mismatch 2\"`,\n      },\n    })\n    expect(res.status).toBe(304)\n  })\n\n  it('Should not return duplicate etag header values', async () => {\n    const app = new Hono()\n    app.use('/etag/*', etag())","after":"      headers: {\n        'If-None-Match': `\"mismatch 1\", ${etagHeaderValue}, \"mismatch 2\"`,\n      },\n    })\n    expect(res.status).toBe(304)\n\n    // conditional GET with `*` wildcard:\n    res = await app.request('http://localhost/etag/ghi', {\n      headers: {\n        'If-None-Match': '*',\n      },\n    })\n    expect(res.status).toBe(304)\n  })\n\n  it('Should not return 304 for `If-None-Match: *` on unsafe methods or error responses', async () => {\n    const app = new Hono()\n    app.use('/etag/*', etag())\n    app.put('/etag/put', (c) => c.text('created', 201))\n    app.get('/etag/not-found', (c) => c.text('not found', 404))\n\n    // `*` does not apply to unsafe methods:\n    let res = await app.request('http://localhost/etag/put', {\n      method: 'PUT',\n      headers: {\n        'If-None-Match': '*',\n      },\n    })\n    expect(res.status).toBe(201)\n    expect(await res.text()).toBe('created')\n\n    // `*` does not apply when there is no current representation:\n    res = await app.request('http://localhost/etag/not-found', {\n      headers: {\n        'If-None-Match': '*',\n      },\n    })\n    expect(res.status).toBe(404)\n  })\n\n  it('Should not return duplicate etag header values', async () => {\n    const app = new Hono()\n    app.use('/etag/*', etag())","diff":"@@ -214,10 +214,43 @@ describe('Etag Middleware', () => {\n       headers: {\n         'If-None-Match': `\"mismatch 1\", ${etagHeaderValue}, \"mismatch 2\"`,\n       },\n     })\n     expect(res.status).toBe(304)\n+\n+    // conditional GET with `*` wildcard:\n+    res = await app.request('http://localhost/etag/ghi', {\n+      headers: {\n+        'If-None-Match': '*',\n+      },\n+    })\n+    expect(res.status).toBe(304)\n+  })\n+\n+  it('Should not return 304 for `If-None-Match: *` on unsafe methods or error responses', async () => {\n+    const app = new Hono()\n+    app.use('/etag/*', etag())\n+    app.put('/etag/put', (c) => c.text('created', 201))\n+    app.get('/etag/not-found', (c) => c.text('not found', 404))\n+\n+    // `*` does not apply to unsafe methods:\n+    let res = await app.request('http://localhost/etag/put', {\n+      method: 'PUT',\n+      headers: {\n+        'If-None-Match': '*',\n+      },\n+    })\n+    expect(res.status).toBe(201)\n+    expect(await res.text()).toBe('created')\n+\n+    // `*` does not apply when there is no current representation:\n+    res = await app.request('http://localhost/etag/not-found', {\n+      headers: {\n+        'If-None-Match': '*',\n+      },\n+    })\n+    expect(res.status).toBe(404)\n   })\n \n   it('Should not return duplicate etag header values', async () => {\n     const app = new Hono()\n     app.use('/etag/*', etag())","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix(etag): treat If-None-Match: `*` as a match (#5084)","issue_url":"https://github.com/honojs/hono/issues/5083","pr_url":"https://github.com/honojs/hono/pull/5084"},"needs_manual_review":true}
{"id":"hono-e8c8c21-1","repo":"honojs/hono","license":"MIT","commit":"e8c8c21244f87da80aaa5ac5ab9c92f3faca2489","parent":"8755b17fbcfdee76511eeb460e18e94e6c9a8d30","file":"src/jsx/dom/render.ts","language":"typescript","hunk_header":"@@ -572,27 +572,40 @@ export const build = (context: Context, node: NodeObject, children?: Child[]): v","before":"          ) {\n            oldChild = candidate as NodeObject\n            oldChildrenByKey.delete(key)\n          }\n        } else if (oldVChildren && oldVChildren.length) {\n          const i = oldVChildren.findIndex(\n            isNodeString(child)\n              ? (c) => isNodeString(c)\n              : child.key !== undefined\n                ? (c) => c.key === (child as Node).key && c.tag === (child as Node).tag\n                : (c) => c.tag === (child as Node).tag\n          )\n\n          scanBudget -= i === -1 ? oldVChildren.length : i\n          if (i !== -1) {\n            oldChild = oldVChildren[i] as NodeObject\n            oldVChildren.splice(i, 1)\n          }\n          // Try indexing once, after searches exceed a linear budget.\n          if (scanBudget < 0) {\n            scanBudget = Infinity\n            oldChildrenByKey = indexChildrenByKey(oldVChildren)\n          }\n        }\n\n        if (oldChild) {\n          if (isNodeString(child)) {","after":"          ) {\n            oldChild = candidate as NodeObject\n            oldChildrenByKey.delete(key)\n          }\n        } else if (oldVChildren && oldVChildren.length) {\n          const first = oldVChildren[0]\n          const isMatchFirst = isNodeString(child)\n            ? isNodeString(first)\n            : !isNodeString(first) &&\n              (child.key !== undefined\n                ? (first as Node).key === (child as Node).key &&\n                  (first as Node).tag === (child as Node).tag\n                : (first as Node).tag === (child as Node).tag)\n\n          if (isMatchFirst) {\n            oldChild = oldVChildren.shift() as NodeObject\n          } else {\n            const i = oldVChildren.findIndex(\n              isNodeString(child)\n                ? (c) => isNodeString(c)\n                : child.key !== undefined\n                  ? (c) => c.key === (child as Node).key && c.tag === (child as Node).tag\n                  : (c) => c.tag === (child as Node).tag\n            )\n\n            scanBudget -= i === -1 ? oldVChildren.length : i\n            if (i !== -1) {\n              oldChild = oldVChildren[i] as NodeObject\n              oldVChildren.splice(i, 1)\n            }\n            // Try indexing once, after searches exceed a linear budget.\n            if (scanBudget < 0) {\n              scanBudget = Infinity\n              oldChildrenByKey = indexChildrenByKey(oldVChildren)\n            }\n          }\n        }\n\n        if (oldChild) {\n          if (isNodeString(child)) {","diff":"@@ -572,27 +572,40 @@ export const build = (context: Context, node: NodeObject, children?: Child[]): v\n           ) {\n             oldChild = candidate as NodeObject\n             oldChildrenByKey.delete(key)\n           }\n         } else if (oldVChildren && oldVChildren.length) {\n-          const i = oldVChildren.findIndex(\n-            isNodeString(child)\n-              ? (c) => isNodeString(c)\n-              : child.key !== undefined\n-                ? (c) => c.key === (child as Node).key && c.tag === (child as Node).tag\n-                : (c) => c.tag === (child as Node).tag\n-          )\n-\n-          scanBudget -= i === -1 ? oldVChildren.length : i\n-          if (i !== -1) {\n-            oldChild = oldVChildren[i] as NodeObject\n-            oldVChildren.splice(i, 1)\n-          }\n-          // Try indexing once, after searches exceed a linear budget.\n-          if (scanBudget < 0) {\n-            scanBudget = Infinity\n-            oldChildrenByKey = indexChildrenByKey(oldVChildren)\n+          const first = oldVChildren[0]\n+          const isMatchFirst = isNodeString(child)\n+            ? isNodeString(first)\n+            : !isNodeString(first) &&\n+              (child.key !== undefined\n+                ? (first as Node).key === (child as Node).key &&\n+                  (first as Node).tag === (child as Node).tag\n+                : (first as Node).tag === (child as Node).tag)\n+\n+          if (isMatchFirst) {\n+            oldChild = oldVChildren.shift() as NodeObject\n+          } else {\n+            const i = oldVChildren.findIndex(\n+              isNodeString(child)\n+                ? (c) => isNodeString(c)\n+                : child.key !== undefined\n+                  ? (c) => c.key === (child as Node).key && c.tag === (child as Node).tag\n+                  : (c) => c.tag === (child as Node).tag\n+            )\n+\n+            scanBudget -= i === -1 ? oldVChildren.length : i\n+            if (i !== -1) {\n+              oldChild = oldVChildren[i] as NodeObject\n+              oldVChildren.splice(i, 1)\n+            }\n+            // Try indexing once, after searches exceed a linear budget.\n+            if (scanBudget < 0) {\n+              scanBudget = Infinity\n+              oldChildrenByKey = indexChildrenByKey(oldVChildren)\n+            }\n           }\n         }\n \n         if (oldChild) {\n           if (isNodeString(child)) {","label":{"defect":false,"category":"refactor","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"perf(jsx/dom): optimize matching-head child lookup during reconciliation (#5329)","issue_url":null,"pr_url":"https://github.com/honojs/hono/pull/5329"},"needs_manual_review":true}
{"id":"hono-8755b17-1","repo":"honojs/hono","license":"MIT","commit":"8755b17fbcfdee76511eeb460e18e94e6c9a8d30","parent":"edd138ee3049749190de3dcd7e32d7bcbf224e41","file":"src/middleware/combine/index.ts","language":"typescript","hunk_header":"@@ -121,11 +121,13 @@ export const every = (...middleware: (MiddlewareHandler | Condition)[]): Middlew","before":" *\n * @param condition - A string or Condition function.\n * If there are multiple targets to match any of them, they can be passed as an array.\n * If a string is passed, it will be treated as a path pattern to match.\n * If a Condition function is passed, it will be evaluated against the request context.\n * @param middleware - A composed middleware\n *\n * @example\n * ```ts\n * import { except } from 'hono/combine'\n * import { bearerAuth } from 'hono/bearer-auth'","after":" *\n * @param condition - A string or Condition function.\n * If there are multiple targets to match any of them, they can be passed as an array.\n * If a string is passed, it will be treated as a path pattern to match.\n * If a Condition function is passed, it will be evaluated against the request context.\n * @param middleware - Middleware to run when the condition is not met.\n * Multiple middleware can be passed, and they are applied in the order they are passed.\n * @returns A composed middleware.\n *\n * @example\n * ```ts\n * import { except } from 'hono/combine'\n * import { bearerAuth } from 'hono/bearer-auth'","diff":"@@ -121,11 +121,13 @@ export const every = (...middleware: (MiddlewareHandler | Condition)[]): Middlew\n  *\n  * @param condition - A string or Condition function.\n  * If there are multiple targets to match any of them, they can be passed as an array.\n  * If a string is passed, it will be treated as a path pattern to match.\n  * If a Condition function is passed, it will be evaluated against the request context.\n- * @param middleware - A composed middleware\n+ * @param middleware - Middleware to run when the condition is not met.\n+ * Multiple middleware can be passed, and they are applied in the order they are passed.\n+ * @returns A composed middleware.\n  *\n  * @example\n  * ```ts\n  * import { except } from 'hono/combine'\n  * import { bearerAuth } from 'hono/bearer-auth'","label":{"defect":false,"category":"docs","touches_public_api":true,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"docs(combine): fix except() JSDoc param and add missing @returns (#5346)","issue_url":null,"pr_url":"https://github.com/honojs/hono/pull/5346"},"needs_manual_review":true}
{"id":"hono-e7b38ee-1","repo":"honojs/hono","license":"MIT","commit":"e7b38ee42bfa41f20194ad20fb949b625346be62","parent":"eebdf7be39abf0a872671835ccce0c4f03ea497a","file":"src/context.ts","language":"typescript","hunk_header":"@@ -769,11 +769,11 @@ export class Context<","before":"    status?: T\n  ): Response & TypedResponse<undefined, T, 'redirect'> => {\n    const locationString = String(location)\n    this.header(\n      'Location',\n      // Multibyes should be encoded\n      // eslint-disable-next-line no-control-regex\n      !/[^\\x00-\\xFF]/.test(locationString) ? locationString : encodeURI(locationString)\n    )\n    return this.newResponse(null, status ?? 302) as any\n  }","after":"    status?: T\n  ): Response & TypedResponse<undefined, T, 'redirect'> => {\n    const locationString = String(location)\n    this.header(\n      'Location',\n      // Multibytes should be encoded\n      // eslint-disable-next-line no-control-regex\n      !/[^\\x00-\\xFF]/.test(locationString) ? locationString : encodeURI(locationString)\n    )\n    return this.newResponse(null, status ?? 302) as any\n  }","diff":"@@ -769,11 +769,11 @@ export class Context<\n     status?: T\n   ): Response & TypedResponse<undefined, T, 'redirect'> => {\n     const locationString = String(location)\n     this.header(\n       'Location',\n-      // Multibyes should be encoded\n+      // Multibytes should be encoded\n       // eslint-disable-next-line no-control-regex\n       !/[^\\x00-\\xFF]/.test(locationString) ? locationString : encodeURI(locationString)\n     )\n     return this.newResponse(null, status ?? 302) as any\n   }","label":{"defect":false,"category":"docs","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"docs: fix typos in code comments and link third-party middleware section (#5343)","issue_url":null,"pr_url":"https://github.com/honojs/hono/pull/5343"},"needs_manual_review":true}
{"id":"hono-4b44abe-1","repo":"honojs/hono","license":"MIT","commit":"4b44abe1b5c7755aac8555d7943611555183450b","parent":"a10592f3e1864cbe277619633db953450a6b1e4b","file":"src/router/reg-exp-router/node.ts","language":"typescript","hunk_header":"@@ -44,11 +46,11 @@ function compareKey(a: string, b: string): number {","before":"\nexport class Node {\n  // handler index of a dynamic path, or -1 for a static path terminal\n  #index?: number\n  #varIndex?: number\n  #children: Record<string, Node> = Object.create(null)\n\n  insert(\n    tokens: readonly string[],\n    index: number,\n    paramMap: ParamAssocArray,","after":"\nexport class Node {\n  // handler index of a dynamic path, or -1 for a static path terminal\n  #index?: number\n  #varIndex?: number\n  #children: Record<string, Node> = createNullObject()\n\n  insert(\n    tokens: readonly string[],\n    index: number,\n    paramMap: ParamAssocArray,","diff":"@@ -44,11 +46,11 @@ function compareKey(a: string, b: string): number {\n \n export class Node {\n   // handler index of a dynamic path, or -1 for a static path terminal\n   #index?: number\n   #varIndex?: number\n-  #children: Record<string, Node> = Object.create(null)\n+  #children: Record<string, Node> = createNullObject()\n \n   insert(\n     tokens: readonly string[],\n     index: number,\n     paramMap: ParamAssocArray,","label":{"defect":false,"category":"refactor","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"perf(router): share null object creation (#5267)","issue_url":null,"pr_url":"https://github.com/honojs/hono/pull/5267"},"needs_manual_review":true}
{"id":"hono-5ad469a-1","repo":"honojs/hono","license":"MIT","commit":"5ad469a888f3944ae3e5d993621612d799f1e96c","parent":"c91ec9b60df18c76a3feb47533ce097d02f7fe6c","file":"src/middleware/pretty-json/index.test.ts","language":"typescript","hunk_header":"@@ -71,6 +71,52 @@ describe('JSON pretty by Middleware', () => {","before":"    const resWithQuery = await (await app.request('http://localhost/?pretty')).text()\n    expect(resWithQuery).toBe(`{\n  \"message\": \"Hono!\"\n}`)\n  })\n})","after":"    const resWithQuery = await (await app.request('http://localhost/?pretty')).text()\n    expect(resWithQuery).toBe(`{\n  \"message\": \"Hono!\"\n}`)\n  })\n\n  it('Should return pretty JSON output for structured JSON content-types (+json)', async () => {\n    const app = new Hono()\n    app.use('*', prettyJSON({ force: true }))\n    app.get('/problem', (c) => {\n      return c.newResponse(JSON.stringify({ type: 'about:blank', title: 'Bad Request' }), 400, {\n        'Content-Type': 'application/problem+json',\n      })\n    })\n    app.get('/jsonapi', (c) => {\n      return c.newResponse(JSON.stringify({ data: { id: '1', type: 'articles' } }), 200, {\n        'Content-Type': 'application/vnd.api+json',\n      })\n    })\n\n    const problemRes = await app.request('http://localhost/problem')\n    expect(await problemRes.text()).toBe(`{\n  \"type\": \"about:blank\",\n  \"title\": \"Bad Request\"\n}`)\n\n    const jsonApiRes = await app.request('http://localhost/jsonapi')\n    expect(await jsonApiRes.text()).toBe(`{\n  \"data\": {\n    \"id\": \"1\",\n    \"type\": \"articles\"\n  }\n}`)\n  })\n\n  it('Should not touch responses with non-JSON content-types', async () => {\n    const app = new Hono()\n    app.use('*', prettyJSON({ force: true }))\n    app.get('/text', (c) => c.text('{\"message\":\"Hono!\"}'))\n    app.get('/json-seq', (c) => {\n      return c.body('{\"message\":\"Hono!\"}', 200, {\n        'Content-Type': 'application/json-seq',\n      })\n    })\n\n    const textRes = await app.request('http://localhost/text')\n    expect(await textRes.text()).toBe('{\"message\":\"Hono!\"}')\n\n    const jsonSeqRes = await app.request('http://localhost/json-seq')\n    expect(await jsonSeqRes.text()).toBe('{\"message\":\"Hono!\"}')\n  })\n})","diff":"@@ -71,6 +71,52 @@ describe('JSON pretty by Middleware', () => {\n     const resWithQuery = await (await app.request('http://localhost/?pretty')).text()\n     expect(resWithQuery).toBe(`{\n   \"message\": \"Hono!\"\n }`)\n   })\n+\n+  it('Should return pretty JSON output for structured JSON content-types (+json)', async () => {\n+    const app = new Hono()\n+    app.use('*', prettyJSON({ force: true }))\n+    app.get('/problem', (c) => {\n+      return c.newResponse(JSON.stringify({ type: 'about:blank', title: 'Bad Request' }), 400, {\n+        'Content-Type': 'application/problem+json',\n+      })\n+    })\n+    app.get('/jsonapi', (c) => {\n+      return c.newResponse(JSON.stringify({ data: { id: '1', type: 'articles' } }), 200, {\n+        'Content-Type': 'application/vnd.api+json',\n+      })\n+    })\n+\n+    const problemRes = await app.request('http://localhost/problem')\n+    expect(await problemRes.text()).toBe(`{\n+  \"type\": \"about:blank\",\n+  \"title\": \"Bad Request\"\n+}`)\n+\n+    const jsonApiRes = await app.request('http://localhost/jsonapi')\n+    expect(await jsonApiRes.text()).toBe(`{\n+  \"data\": {\n+    \"id\": \"1\",\n+    \"type\": \"articles\"\n+  }\n+}`)\n+  })\n+\n+  it('Should not touch responses with non-JSON content-types', async () => {\n+    const app = new Hono()\n+    app.use('*', prettyJSON({ force: true }))\n+    app.get('/text', (c) => c.text('{\"message\":\"Hono!\"}'))\n+    app.get('/json-seq', (c) => {\n+      return c.body('{\"message\":\"Hono!\"}', 200, {\n+        'Content-Type': 'application/json-seq',\n+      })\n+    })\n+\n+    const textRes = await app.request('http://localhost/text')\n+    expect(await textRes.text()).toBe('{\"message\":\"Hono!\"}')\n+\n+    const jsonSeqRes = await app.request('http://localhost/json-seq')\n+    expect(await jsonSeqRes.text()).toBe('{\"message\":\"Hono!\"}')\n+  })\n })","label":{"defect":false,"category":"feature","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"feat(pretty-json): support structured JSON content-types (+json) (#5226)","issue_url":null,"pr_url":"https://github.com/honojs/hono/pull/5226"},"needs_manual_review":true}
{"id":"hono-ef0739d-1","repo":"honojs/hono","license":"MIT","commit":"ef0739d5f6e83242ba2b64c1365c9f96738933a1","parent":"8bf03c377a39226b3dbcbcadf3b342532e1dd0a8","file":"src/context.ts","language":"typescript","hunk_header":"@@ -506,10 +506,14 @@ export class Context<","before":"   * app.get('/welcome', (c) => {\n   *   // Set headers\n   *   c.header('X-Message', 'Hello!')\n   *   c.header('Content-Type', 'text/plain')\n   *\n   *   return c.body('Thank you for coming')\n   * })\n   * ```\n   */\n  header: SetHeaders = (name, value, options): void => {","after":"   * app.get('/welcome', (c) => {\n   *   // Set headers\n   *   c.header('X-Message', 'Hello!')\n   *   c.header('Content-Type', 'text/plain')\n   *\n   *   // Append multiple headers using the append option (e.g. Vary)\n   *   c.header('Vary', 'Accept-Encoding', { append: true })\n   *   c.header('Vary', 'User-Agent', { append: true })\n   *\n   *   return c.body('Thank you for coming')\n   * })\n   * ```\n   */\n  header: SetHeaders = (name, value, options): void => {","diff":"@@ -506,10 +506,14 @@ export class Context<\n    * app.get('/welcome', (c) => {\n    *   // Set headers\n    *   c.header('X-Message', 'Hello!')\n    *   c.header('Content-Type', 'text/plain')\n    *\n+   *   // Append multiple headers using the append option (e.g. Vary)\n+   *   c.header('Vary', 'Accept-Encoding', { append: true })\n+   *   c.header('Vary', 'User-Agent', { append: true })\n+   *\n    *   return c.body('Thank you for coming')\n    * })\n    * ```\n    */\n   header: SetHeaders = (name, value, options): void => {","label":{"defect":false,"category":"docs","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"docs(context): add custom headers append option example to Context JSDoc (#5248)","issue_url":null,"pr_url":"https://github.com/honojs/hono/pull/5248"},"needs_manual_review":true}
{"id":"hono-6ea514d-1","repo":"honojs/hono","license":"MIT","commit":"6ea514d6ff09e84599ac35c0e885f62be2aa44a3","parent":"329b6f46865aad2fa1d0ac17fb53afc5a3216f1b","file":"src/middleware/secure-headers/index.test.ts","language":"typescript","hunk_header":"@@ -179,20 +179,23 @@ describe('Secure Headers Middleware', () => {","before":"          geolocation: ['*'],\n          usb: ['self', 'https://a.example.com', 'https://b.example.com'],\n          accelerometer: ['https://*.example.com'],\n          gyroscope: ['src'],\n          magnetometer: ['https://a.example.com', 'https://b.example.com'],\n        },\n      })\n    )\n\n    const res = await app.request('/test')\n    expect(res.headers.get('Permissions-Policy')).toEqual(\n      'fullscreen=(self), bluetooth=(), payment=(self \"example.com\"), sync-xhr=(), camera=(), microphone=*, ' +\n        'geolocation=*, usb=(self \"https://a.example.com\" \"https://b.example.com\"), ' +\n        'accelerometer=(\"https://*.example.com\"), gyroscope=(src), ' +\n        'magnetometer=(\"https://a.example.com\" \"https://b.example.com\")'\n    )\n  })\n\n  it('Remove X-Powered-By', async () => {\n    const app = new Hono()","after":"          geolocation: ['*'],\n          usb: ['self', 'https://a.example.com', 'https://b.example.com'],\n          accelerometer: ['https://*.example.com'],\n          gyroscope: ['src'],\n          magnetometer: ['https://a.example.com', 'https://b.example.com'],\n          mediasession: ['self'],\n          deferredFetch: ['none'],\n        },\n      })\n    )\n\n    const res = await app.request('/test')\n    expect(res.headers.get('Permissions-Policy')).toEqual(\n      'fullscreen=(self), bluetooth=(), payment=(self \"example.com\"), sync-xhr=(), camera=(), microphone=*, ' +\n        'geolocation=*, usb=(self \"https://a.example.com\" \"https://b.example.com\"), ' +\n        'accelerometer=(\"https://*.example.com\"), gyroscope=(src), ' +\n        'magnetometer=(\"https://a.example.com\" \"https://b.example.com\"), ' +\n        'mediasession=(self), deferred-fetch=()'\n    )\n  })\n\n  it('Remove X-Powered-By', async () => {\n    const app = new Hono()","diff":"@@ -179,20 +179,23 @@ describe('Secure Headers Middleware', () => {\n           geolocation: ['*'],\n           usb: ['self', 'https://a.example.com', 'https://b.example.com'],\n           accelerometer: ['https://*.example.com'],\n           gyroscope: ['src'],\n           magnetometer: ['https://a.example.com', 'https://b.example.com'],\n+          mediasession: ['self'],\n+          deferredFetch: ['none'],\n         },\n       })\n     )\n \n     const res = await app.request('/test')\n     expect(res.headers.get('Permissions-Policy')).toEqual(\n       'fullscreen=(self), bluetooth=(), payment=(self \"example.com\"), sync-xhr=(), camera=(), microphone=*, ' +\n         'geolocation=*, usb=(self \"https://a.example.com\" \"https://b.example.com\"), ' +\n         'accelerometer=(\"https://*.example.com\"), gyroscope=(src), ' +\n-        'magnetometer=(\"https://a.example.com\" \"https://b.example.com\")'\n+        'magnetometer=(\"https://a.example.com\" \"https://b.example.com\"), ' +\n+        'mediasession=(self), deferred-fetch=()'\n     )\n   })\n \n   it('Remove X-Powered-By', async () => {\n     const app = new Hono()","label":{"defect":false,"category":"feature","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"feat(secure-headers): add missing W3C Permissions-Policy directives (#5214)","issue_url":null,"pr_url":"https://github.com/honojs/hono/pull/5214"},"needs_manual_review":true}
{"id":"hono-a1e4ac7-1","repo":"honojs/hono","license":"MIT","commit":"a1e4ac7d46a3e627c143c4dd6dcf7bc59031bb9a","parent":"d982f637ebab758715f7e68ff35a7c4844450023","file":"src/middleware/cors/index.test.ts","language":"typescript","hunk_header":"@@ -380,10 +380,21 @@ describe('CORS by Middleware', () => {","before":"    const res2 = await app.request(req2)\n    expect(res2.headers.get('Access-Control-Allow-Origin')).toBe('*')\n    expect(res2.headers.get('Access-Control-Allow-Methods')).toBe('GET,HEAD')\n  })\n\n  it('Emits the wildcard, not the reflected origin, when credentials is true with wildcard origin', async () => {\n    const res = await app.request('http://localhost/api10/abc', {\n      headers: {\n        Origin: 'http://example.com',\n      },","after":"    const res2 = await app.request(req2)\n    expect(res2.headers.get('Access-Control-Allow-Origin')).toBe('*')\n    expect(res2.headers.get('Access-Control-Allow-Methods')).toBe('GET,HEAD')\n  })\n\n  it('Does not set allow methods when function returns an empty array', async () => {\n    for (const allowMethods of [() => [], async () => []]) {\n      const app = new Hono()\n      app.use(cors({ allowMethods }))\n\n      const res = await app.request('http://localhost/', { method: 'OPTIONS' })\n\n      expect(res.headers.get('Access-Control-Allow-Methods')).toBeNull()\n    }\n  })\n\n  it('Emits the wildcard, not the reflected origin, when credentials is true with wildcard origin', async () => {\n    const res = await app.request('http://localhost/api10/abc', {\n      headers: {\n        Origin: 'http://example.com',\n      },","diff":"@@ -380,10 +380,21 @@ describe('CORS by Middleware', () => {\n     const res2 = await app.request(req2)\n     expect(res2.headers.get('Access-Control-Allow-Origin')).toBe('*')\n     expect(res2.headers.get('Access-Control-Allow-Methods')).toBe('GET,HEAD')\n   })\n \n+  it('Does not set allow methods when function returns an empty array', async () => {\n+    for (const allowMethods of [() => [], async () => []]) {\n+      const app = new Hono()\n+      app.use(cors({ allowMethods }))\n+\n+      const res = await app.request('http://localhost/', { method: 'OPTIONS' })\n+\n+      expect(res.headers.get('Access-Control-Allow-Methods')).toBeNull()\n+    }\n+  })\n+\n   it('Emits the wildcard, not the reflected origin, when credentials is true with wildcard origin', async () => {\n     const res = await app.request('http://localhost/api10/abc', {\n       headers: {\n         Origin: 'http://example.com',\n       },","label":{"defect":false,"category":"refactor","touches_public_api":null,"touches_security":true,"source":"commit-heuristic"},"evidence":{"commit_message":"perf(cors): pre-join static array header options during initialization (#5210)","issue_url":null,"pr_url":"https://github.com/honojs/hono/pull/5210"},"needs_manual_review":true}
{"id":"hono-8a0b18f-1","repo":"honojs/hono","license":"MIT","commit":"8a0b18fd9b4d64dd2eb1d7f18e3536fc06cb54b2","parent":"3feb3551d46de1f633e82253f12cf1117316be93","file":"src/router/reg-exp-router/node.ts","language":"typescript","hunk_header":"@@ -23,13 +23,13 @@ function compareKey(a: string, b: string): number {","before":"  }\n  if (b.length === 1) {\n    return 1\n  }\n\n  // wildcard\n  if (a === ONLY_WILDCARD_REG_EXP_STR || a === TAIL_WILDCARD_REG_EXP_STR) {\n    return 1\n  } else if (b === ONLY_WILDCARD_REG_EXP_STR || b === TAIL_WILDCARD_REG_EXP_STR) {\n    return -1\n  }\n\n  // label","after":"  }\n  if (b.length === 1) {\n    return 1\n  }\n\n  // wildcard: the only wildcard (.*) precedes the tail wildcard\n  if (a === ONLY_WILDCARD_REG_EXP_STR || a === TAIL_WILDCARD_REG_EXP_STR) {\n    return b === TAIL_WILDCARD_REG_EXP_STR ? -1 : 1\n  } else if (b === ONLY_WILDCARD_REG_EXP_STR || b === TAIL_WILDCARD_REG_EXP_STR) {\n    return -1\n  }\n\n  // label","diff":"@@ -23,13 +23,13 @@ function compareKey(a: string, b: string): number {\n   }\n   if (b.length === 1) {\n     return 1\n   }\n \n-  // wildcard\n+  // wildcard: the only wildcard (.*) precedes the tail wildcard\n   if (a === ONLY_WILDCARD_REG_EXP_STR || a === TAIL_WILDCARD_REG_EXP_STR) {\n-    return 1\n+    return b === TAIL_WILDCARD_REG_EXP_STR ? -1 : 1\n   } else if (b === ONLY_WILDCARD_REG_EXP_STR || b === TAIL_WILDCARD_REG_EXP_STR) {\n     return -1\n   }\n \n   // label","label":{"defect":false,"category":"feature","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"feat(reg-exp-router): throw UnsupportedPathError during route registration (#5171)","issue_url":null,"pr_url":"https://github.com/honojs/hono/pull/5171"},"needs_manual_review":true}
{"id":"hono-5d911d2-1","repo":"honojs/hono","license":"MIT","commit":"5d911d2ab7bcb2adb2e974ddd5b17742fb5a0bca","parent":"30277aee0ddd96b643a4958d6102abe6e54d9a29","file":"src/utils/headers.ts","language":"typescript","hunk_header":"@@ -15,10 +15,11 @@ export type RequestHeader =","before":"  | 'Accept-Encoding'\n  | 'Accept-Features'\n  | 'Accept-Language'\n  | 'Accept-Patch'\n  | 'Accept-Post'\n  | 'Accept-Ranges'\n  | 'Accept-Signature'\n  | 'Access-Control'\n  | 'Access-Control-Allow-Credentials'\n  | 'Access-Control-Allow-Headers'","after":"  | 'Accept-Encoding'\n  | 'Accept-Features'\n  | 'Accept-Language'\n  | 'Accept-Patch'\n  | 'Accept-Post'\n  | 'Accept-Query'\n  | 'Accept-Ranges'\n  | 'Accept-Signature'\n  | 'Access-Control'\n  | 'Access-Control-Allow-Credentials'\n  | 'Access-Control-Allow-Headers'","diff":"@@ -15,10 +15,11 @@ export type RequestHeader =\n   | 'Accept-Encoding'\n   | 'Accept-Features'\n   | 'Accept-Language'\n   | 'Accept-Patch'\n   | 'Accept-Post'\n+  | 'Accept-Query'\n   | 'Accept-Ranges'\n   | 'Accept-Signature'\n   | 'Access-Control'\n   | 'Access-Control-Allow-Credentials'\n   | 'Access-Control-Allow-Headers'","label":{"defect":false,"category":"feature","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"feat(utils/headers): add HTTP fields newly registered with IANA (#5153)","issue_url":null,"pr_url":"https://github.com/honojs/hono/pull/5153"},"needs_manual_review":true}
{"id":"hono-30277ae-1","repo":"honojs/hono","license":"MIT","commit":"30277aee0ddd96b643a4958d6102abe6e54d9a29","parent":"1f707c56378a4497bc0a6a44fef5f5a8517fdf6b","file":"src/middleware/jwk/index.test.ts","language":"typescript","hunk_header":"@@ -1075,6 +1075,61 @@ describe('JWK', () => {","before":"    })\n\n    // Note: Test for \"no whitelist\" was removed because alg is now required.\n    // This is a breaking change that enforces explicit algorithm specification for security.\n  })\n})","after":"    })\n\n    // Note: Test for \"no whitelist\" was removed because alg is now required.\n    // This is a breaking change that enforces explicit algorithm specification for security.\n  })\n  describe('WWW-Authenticate realm', () => {\n    it('Should default to the request URL', async () => {\n      const app = new Hono()\n      app.use('/auth/*', jwk({ keys: verify_keys, alg: ['RS256'] }))\n      app.get('/auth/*', (c) => c.text('ok'))\n\n      const res = await app.request('http://localhost/auth/page?a=1')\n\n      expect(res.status).toBe(401)\n      expect(res.headers.get('WWW-Authenticate')).toBe(\n        'Bearer realm=\"http://localhost/auth/page?a=1\",error=\"invalid_request\",error_description=\"no authorization included in request\"'\n      )\n    })\n\n    it('Should use the configured realm', async () => {\n      const app = new Hono()\n      app.use('/auth/*', jwk({ keys: verify_keys, alg: ['RS256'], realm: 'my-api' }))\n      app.get('/auth/*', (c) => c.text('ok'))\n\n      const res = await app.request('http://localhost/auth/page?a=1')\n\n      expect(res.status).toBe(401)\n      expect(res.headers.get('WWW-Authenticate')).toBe(\n        'Bearer realm=\"my-api\",error=\"invalid_request\",error_description=\"no authorization included in request\"'\n      )\n    })\n\n    it('Should use the configured realm for an invalid token', async () => {\n      const app = new Hono()\n      app.use('/auth/*', jwk({ keys: verify_keys, alg: ['RS256'], realm: 'my-api' }))\n      app.get('/auth/*', (c) => c.text('ok'))\n\n      const req = new Request('http://localhost/auth/page')\n      req.headers.set('Authorization', 'Bearer invalid-token')\n      const res = await app.request(req)\n\n      expect(res.status).toBe(401)\n      expect(res.headers.get('WWW-Authenticate')).toBe(\n        'Bearer realm=\"my-api\",error=\"invalid_token\",error_description=\"token verification failure\"'\n      )\n    })\n\n    it('Should escape double quotes in the realm', async () => {\n      const app = new Hono()\n      app.use('/auth/*', jwk({ keys: verify_keys, alg: ['RS256'], realm: 'my \"quoted\" api' }))\n      app.get('/auth/*', (c) => c.text('ok'))\n\n      const res = await app.request('http://localhost/auth/page')\n\n      expect(res.status).toBe(401)\n      expect(res.headers.get('WWW-Authenticate')).toBe(\n        'Bearer realm=\"my \\\\\"quoted\\\\\" api\",error=\"invalid_request\",error_description=\"no authorization included in request\"'\n      )\n    })\n  })\n})","diff":"@@ -1075,6 +1075,61 @@ describe('JWK', () => {\n     })\n \n     // Note: Test for \"no whitelist\" was removed because alg is now required.\n     // This is a breaking change that enforces explicit algorithm specification for security.\n   })\n+  describe('WWW-Authenticate realm', () => {\n+    it('Should default to the request URL', async () => {\n+      const app = new Hono()\n+      app.use('/auth/*', jwk({ keys: verify_keys, alg: ['RS256'] }))\n+      app.get('/auth/*', (c) => c.text('ok'))\n+\n+      const res = await app.request('http://localhost/auth/page?a=1')\n+\n+      expect(res.status).toBe(401)\n+      expect(res.headers.get('WWW-Authenticate')).toBe(\n+        'Bearer realm=\"http://localhost/auth/page?a=1\",error=\"invalid_request\",error_description=\"no authorization included in request\"'\n+      )\n+    })\n+\n+    it('Should use the configured realm', async () => {\n+      const app = new Hono()\n+      app.use('/auth/*', jwk({ keys: verify_keys, alg: ['RS256'], realm: 'my-api' }))\n+      app.get('/auth/*', (c) => c.text('ok'))\n+\n+      const res = await app.request('http://localhost/auth/page?a=1')\n+\n+      expect(res.status).toBe(401)\n+      expect(res.headers.get('WWW-Authenticate')).toBe(\n+        'Bearer realm=\"my-api\",error=\"invalid_request\",error_description=\"no authorization included in request\"'\n+      )\n+    })\n+\n+    it('Should use the configured realm for an invalid token', async () => {\n+      const app = new Hono()\n+      app.use('/auth/*', jwk({ keys: verify_keys, alg: ['RS256'], realm: 'my-api' }))\n+      app.get('/auth/*', (c) => c.text('ok'))\n+\n+      const req = new Request('http://localhost/auth/page')\n+      req.headers.set('Authorization', 'Bearer invalid-token')\n+      const res = await app.request(req)\n+\n+      expect(res.status).toBe(401)\n+      expect(res.headers.get('WWW-Authenticate')).toBe(\n+        'Bearer realm=\"my-api\",error=\"invalid_token\",error_description=\"token verification failure\"'\n+      )\n+    })\n+\n+    it('Should escape double quotes in the realm', async () => {\n+      const app = new Hono()\n+      app.use('/auth/*', jwk({ keys: verify_keys, alg: ['RS256'], realm: 'my \"quoted\" api' }))\n+      app.get('/auth/*', (c) => c.text('ok'))\n+\n+      const res = await app.request('http://localhost/auth/page')\n+\n+      expect(res.status).toBe(401)\n+      expect(res.headers.get('WWW-Authenticate')).toBe(\n+        'Bearer realm=\"my \\\\\"quoted\\\\\" api\",error=\"invalid_request\",error_description=\"no authorization included in request\"'\n+      )\n+    })\n+  })\n })","label":{"defect":false,"category":"feature","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"feat(jwt,jwk): add a configurable WWW-Authenticate realm (#5141)","issue_url":null,"pr_url":"https://github.com/honojs/hono/pull/5141"},"needs_manual_review":true}
{"id":"hono-3bc96ba-1","repo":"honojs/hono","license":"MIT","commit":"3bc96ba915bf8d5b25430c955aec8f7df500ff21","parent":"75b8a4932a24a4533168600436e10f03055844bf","file":"src/middleware/cache/index.test.ts","language":"typescript","hunk_header":"@@ -99,11 +99,13 @@ describe('Customizing Caching Keys', () => {","before":"  })\n\n  it('Should use dynamically generated cache key', async () => {\n    await app.request('http://localhost/dynamic-cache-key/')\n    const cache = await caches.open('my-app-v1')\n    const response = await cache.match(dynamicCacheKey)\n    expect(response).not.toBeNull()\n  })\n\n  it('Should retrieve cached response with dynamic cache name and key', async () => {\n    await app.request('http://localhost/dynamic-cache/', undefined, undefined, ctx)","after":"  })\n\n  it('Should use dynamically generated cache key', async () => {\n    await app.request('http://localhost/dynamic-cache-key/')\n    const cache = await caches.open('my-app-v1')\n    const response = await cache.match(\n      'http://localhost/.hono/cache?__hono_cache_key=dynamic-cache-key&__hono_cache_method=GET'\n    )\n    expect(response).not.toBeNull()\n  })\n\n  it('Should retrieve cached response with dynamic cache name and key', async () => {\n    await app.request('http://localhost/dynamic-cache/', undefined, undefined, ctx)","diff":"@@ -99,11 +99,13 @@ describe('Customizing Caching Keys', () => {\n   })\n \n   it('Should use dynamically generated cache key', async () => {\n     await app.request('http://localhost/dynamic-cache-key/')\n     const cache = await caches.open('my-app-v1')\n-    const response = await cache.match(dynamicCacheKey)\n+    const response = await cache.match(\n+      'http://localhost/.hono/cache?__hono_cache_key=dynamic-cache-key&__hono_cache_method=GET'\n+    )\n     expect(response).not.toBeNull()\n   })\n \n   it('Should retrieve cached response with dynamic cache name and key', async () => {\n     await app.request('http://localhost/dynamic-cache/', undefined, undefined, ctx)","label":{"defect":false,"category":"feature","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"feat(cache): add first-class support for QUERY requests (#5119)","issue_url":null,"pr_url":"https://github.com/honojs/hono/pull/5154"},"needs_manual_review":true}
{"id":"hono-75b8a49-1","repo":"honojs/hono","license":"MIT","commit":"75b8a4932a24a4533168600436e10f03055844bf","parent":"2159deb4bdd087025e343326480bb803055c04b6","file":"src/middleware/cors/index.test.ts","language":"typescript","hunk_header":"@@ -143,17 +143,30 @@ describe('CORS by Middleware', () => {","before":"    expect(res.headers.get('Access-Control-Allow-Origin')).toBe('*')\n    expect(res.headers.get('Vary')).toBeNull()\n  })\n\n  it('Preflight default', async () => {\n    const req = new Request('https://localhost/api/abc', { method: 'OPTIONS' })\n    req.headers.append('Access-Control-Request-Headers', 'X-PINGOTHER, Content-Type')\n    const res = await app.request(req)\n\n    expect(res.status).toBe(204)\n    expect(res.statusText).toBe('No Content')\n    expect(res.headers.get('Access-Control-Allow-Methods')?.split(',')[0]).toBe('GET')\n    expect(res.headers.get('Access-Control-Allow-Headers')?.split(',')).toEqual([\n      'X-PINGOTHER',\n      'Content-Type',\n    ])\n  })","after":"    expect(res.headers.get('Access-Control-Allow-Origin')).toBe('*')\n    expect(res.headers.get('Vary')).toBeNull()\n  })\n\n  it('Preflight default', async () => {\n    const req = new Request('https://localhost/api/abc', {\n      method: 'OPTIONS',\n      headers: {\n        'Access-Control-Request-Method': 'QUERY',\n        'Access-Control-Request-Headers': 'X-PINGOTHER, Content-Type',\n      },\n    })\n    const res = await app.request(req)\n\n    expect(res.status).toBe(204)\n    expect(res.statusText).toBe('No Content')\n    expect(res.headers.get('Access-Control-Allow-Methods')?.split(',')).toEqual([\n      'GET',\n      'HEAD',\n      'PUT',\n      'POST',\n      'DELETE',\n      'PATCH',\n      'QUERY',\n    ])\n    expect(res.headers.get('Access-Control-Allow-Headers')?.split(',')).toEqual([\n      'X-PINGOTHER',\n      'Content-Type',\n    ])\n  })","diff":"@@ -143,17 +143,30 @@ describe('CORS by Middleware', () => {\n     expect(res.headers.get('Access-Control-Allow-Origin')).toBe('*')\n     expect(res.headers.get('Vary')).toBeNull()\n   })\n \n   it('Preflight default', async () => {\n-    const req = new Request('https://localhost/api/abc', { method: 'OPTIONS' })\n-    req.headers.append('Access-Control-Request-Headers', 'X-PINGOTHER, Content-Type')\n+    const req = new Request('https://localhost/api/abc', {\n+      method: 'OPTIONS',\n+      headers: {\n+        'Access-Control-Request-Method': 'QUERY',\n+        'Access-Control-Request-Headers': 'X-PINGOTHER, Content-Type',\n+      },\n+    })\n     const res = await app.request(req)\n \n     expect(res.status).toBe(204)\n     expect(res.statusText).toBe('No Content')\n-    expect(res.headers.get('Access-Control-Allow-Methods')?.split(',')[0]).toBe('GET')\n+    expect(res.headers.get('Access-Control-Allow-Methods')?.split(',')).toEqual([\n+      'GET',\n+      'HEAD',\n+      'PUT',\n+      'POST',\n+      'DELETE',\n+      'PATCH',\n+      'QUERY',\n+    ])\n     expect(res.headers.get('Access-Control-Allow-Headers')?.split(',')).toEqual([\n       'X-PINGOTHER',\n       'Content-Type',\n     ])\n   })","label":{"defect":false,"category":"feature","touches_public_api":null,"touches_security":true,"source":"commit-heuristic"},"evidence":{"commit_message":"feat(cors): allow QUERY by default as a first-class method (#5115)","issue_url":null,"pr_url":"https://github.com/honojs/hono/pull/5154"},"needs_manual_review":true}
{"id":"hono-2159deb-1","repo":"honojs/hono","license":"MIT","commit":"2159deb4bdd087025e343326480bb803055c04b6","parent":"6f101a7779bf6e79c3af0a09cd888428bbd35ef8","file":"src/middleware/etag/index.test.ts","language":"typescript","hunk_header":"@@ -224,10 +224,57 @@ describe('Etag Middleware', () => {","before":"      },\n    })\n    expect(res.status).toBe(304)\n  })\n\n  it('Should not return 304 for `If-None-Match: *` on unsafe methods or error responses', async () => {\n    const app = new Hono()\n    app.use('/etag/*', etag())\n    app.put('/etag/put', (c) => c.text('created', 201))\n    app.get('/etag/not-found', (c) => c.text('not found', 404))","after":"      },\n    })\n    expect(res.status).toBe(304)\n  })\n\n  it('Should handle conditional QUERY requests', async () => {\n    const app = new Hono()\n    app.use('/etag/*', etag())\n    app.on('QUERY', '/etag/query', (c) => c.text('Hono is great'))\n\n    // unconditional QUERY\n    let res = await app.request('http://localhost/etag/query', {\n      method: 'QUERY',\n      body: 'select *',\n    })\n    expect(res.status).toBe(200)\n    expect(res.headers.get('ETag')).not.toBeFalsy()\n    const etagHeaderValue = res.headers.get('ETag') || ''\n\n    // conditional QUERY with the wrong ETag:\n    res = await app.request('http://localhost/etag/query', {\n      method: 'QUERY',\n      body: 'select *',\n      headers: {\n        'If-None-Match': '\"not the right etag\"',\n      },\n    })\n    expect(res.status).toBe(200)\n\n    // conditional QUERY with matching ETag:\n    res = await app.request('http://localhost/etag/query', {\n      method: 'QUERY',\n      body: 'select *',\n      headers: {\n        'If-None-Match': etagHeaderValue,\n      },\n    })\n    expect(res.status).toBe(304)\n    expect(res.headers.get('ETag')).toBe(etagHeaderValue)\n    expect(await res.text()).toBe('')\n\n    // conditional QUERY with `*` wildcard:\n    res = await app.request('http://localhost/etag/query', {\n      method: 'QUERY',\n      body: 'select *',\n      headers: {\n        'If-None-Match': '*',\n      },\n    })\n    expect(res.status).toBe(304)\n  })\n\n  it('Should not return 304 for `If-None-Match: *` on unsafe methods or error responses', async () => {\n    const app = new Hono()\n    app.use('/etag/*', etag())\n    app.put('/etag/put', (c) => c.text('created', 201))\n    app.get('/etag/not-found', (c) => c.text('not found', 404))","diff":"@@ -224,10 +224,57 @@ describe('Etag Middleware', () => {\n       },\n     })\n     expect(res.status).toBe(304)\n   })\n \n+  it('Should handle conditional QUERY requests', async () => {\n+    const app = new Hono()\n+    app.use('/etag/*', etag())\n+    app.on('QUERY', '/etag/query', (c) => c.text('Hono is great'))\n+\n+    // unconditional QUERY\n+    let res = await app.request('http://localhost/etag/query', {\n+      method: 'QUERY',\n+      body: 'select *',\n+    })\n+    expect(res.status).toBe(200)\n+    expect(res.headers.get('ETag')).not.toBeFalsy()\n+    const etagHeaderValue = res.headers.get('ETag') || ''\n+\n+    // conditional QUERY with the wrong ETag:\n+    res = await app.request('http://localhost/etag/query', {\n+      method: 'QUERY',\n+      body: 'select *',\n+      headers: {\n+        'If-None-Match': '\"not the right etag\"',\n+      },\n+    })\n+    expect(res.status).toBe(200)\n+\n+    // conditional QUERY with matching ETag:\n+    res = await app.request('http://localhost/etag/query', {\n+      method: 'QUERY',\n+      body: 'select *',\n+      headers: {\n+        'If-None-Match': etagHeaderValue,\n+      },\n+    })\n+    expect(res.status).toBe(304)\n+    expect(res.headers.get('ETag')).toBe(etagHeaderValue)\n+    expect(await res.text()).toBe('')\n+\n+    // conditional QUERY with `*` wildcard:\n+    res = await app.request('http://localhost/etag/query', {\n+      method: 'QUERY',\n+      body: 'select *',\n+      headers: {\n+        'If-None-Match': '*',\n+      },\n+    })\n+    expect(res.status).toBe(304)\n+  })\n+\n   it('Should not return 304 for `If-None-Match: *` on unsafe methods or error responses', async () => {\n     const app = new Hono()\n     app.use('/etag/*', etag())\n     app.put('/etag/put', (c) => c.text('created', 201))\n     app.get('/etag/not-found', (c) => c.text('not found', 404))","label":{"defect":false,"category":"feature","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"feat(etag): support conditional requests for the QUERY method (#5111)","issue_url":null,"pr_url":"https://github.com/honojs/hono/pull/5154"},"needs_manual_review":true}
{"id":"trpc-8b649ad-1","repo":"trpc/trpc","license":"MIT","commit":"8b649ad874a8fe97c73962d4617912231b975736","parent":"fcc6bca0ece2762e393e6d8c8527bbaa17a878d2","file":"packages/server/src/unstable-core-do-not-import/stream/jsonl.test.ts","language":"typescript","hunk_header":"@@ -4,10 +4,11 @@ import SuperJSON from 'superjson';","before":"import { run } from '../utils';\nimport type { ConsumerOnError, ProducerOnError } from './jsonl';\nimport { jsonlStreamConsumer, jsonlStreamProducer } from './jsonl';\nimport { createDeferred } from './utils/createDeferred';\nimport { makeResource } from './utils/disposable';\n\ntest('encode/decode with superjson', async () => {\n  const abortController = new AbortController();\n  const data = {\n    0: Promise.resolve({","after":"import { run } from '../utils';\nimport type { ConsumerOnError, ProducerOnError } from './jsonl';\nimport { jsonlStreamConsumer, jsonlStreamProducer } from './jsonl';\nimport { createDeferred } from './utils/createDeferred';\nimport { makeResource } from './utils/disposable';\nimport { readableStreamFrom } from './utils/readableStreamFrom';\n\ntest('encode/decode with superjson', async () => {\n  const abortController = new AbortController();\n  const data = {\n    0: Promise.resolve({","diff":"@@ -4,10 +4,11 @@ import SuperJSON from 'superjson';\n import { run } from '../utils';\n import type { ConsumerOnError, ProducerOnError } from './jsonl';\n import { jsonlStreamConsumer, jsonlStreamProducer } from './jsonl';\n import { createDeferred } from './utils/createDeferred';\n import { makeResource } from './utils/disposable';\n+import { readableStreamFrom } from './utils/readableStreamFrom';\n \n test('encode/decode with superjson', async () => {\n   const abortController = new AbortController();\n   const data = {\n     0: Promise.resolve({","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix(server): Handle premature stream ending (#7604)","issue_url":"https://github.com/trpc/trpc/issues/7603","pr_url":"https://github.com/trpc/trpc/pull/7604"},"needs_manual_review":true}
{"id":"trpc-d1a9ec6-1","repo":"trpc/trpc","license":"MIT","commit":"d1a9ec68cea9740a5a46dcf98399d9a876f2313a","parent":"6a118b0be9bc9ba4a899c545edbcfdacfc504fad","file":"packages/client/src/links/httpBatchLink.ts","language":"typescript","hunk_header":"@@ -1,11 +1,11 @@","before":"import type { AnyRouter, ProcedureType } from '@trpc/server';\nimport { observable } from '@trpc/server/observable';\nimport { transformResult } from '@trpc/server/unstable-core-do-not-import';\nimport type { BatchLoader } from '../internals/dataLoader';\nimport { dataLoader } from '../internals/dataLoader';\nimport { allAbortSignals } from '../internals/signals';\nimport type { NonEmptyArray } from '../internals/types';\nimport { TRPCClientError } from '../TRPCClientError';\nimport type { HTTPBatchLinkOptions } from './HTTPBatchLinkOptions';\nimport type { HTTPResult } from './internals/httpUtils';\nimport {","after":"import type { AnyRouter, ProcedureType } from '@trpc/server';\nimport { observable } from '@trpc/server/observable';\nimport { transformResult } from '@trpc/server/unstable-core-do-not-import';\nimport type { BatchLoader } from '../internals/dataLoader';\nimport { dataLoader } from '../internals/dataLoader';\nimport { allAbortSignals, raceAbortSignals } from '../internals/signals';\nimport type { NonEmptyArray } from '../internals/types';\nimport { TRPCClientError } from '../TRPCClientError';\nimport type { HTTPBatchLinkOptions } from './HTTPBatchLinkOptions';\nimport type { HTTPResult } from './internals/httpUtils';\nimport {","diff":"@@ -1,11 +1,11 @@\n import type { AnyRouter, ProcedureType } from '@trpc/server';\n import { observable } from '@trpc/server/observable';\n import { transformResult } from '@trpc/server/unstable-core-do-not-import';\n import type { BatchLoader } from '../internals/dataLoader';\n import { dataLoader } from '../internals/dataLoader';\n-import { allAbortSignals } from '../internals/signals';\n+import { allAbortSignals, raceAbortSignals } from '../internals/signals';\n import type { NonEmptyArray } from '../internals/types';\n import { TRPCClientError } from '../TRPCClientError';\n import type { HTTPBatchLinkOptions } from './HTTPBatchLinkOptions';\n import type { HTTPResult } from './internals/httpUtils';\n import {","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix(client): abort httpLink/httpBatchLink requests on unsubscribe (#7469)","issue_url":"https://github.com/trpc/trpc/issues/7468","pr_url":"https://github.com/trpc/trpc/pull/7469"},"needs_manual_review":true}
{"id":"trpc-6408c54-1","repo":"trpc/trpc","license":"MIT","commit":"6408c54ba9bea7b18c39be3a7abd79c8841a2333","parent":"622bbbed38d87d8e04f62d15efceddb43de08fa4","file":"packages/server/src/adapters/ws.ts","language":"typescript","hunk_header":"@@ -404,10 +404,11 @@ export function getWSConnectionHandler<TRouter extends AnyRouter>(","before":"              type: 'stopped',\n            },\n          });\n          clientSubscriptions.delete(id);\n        }).catch((cause) => {\n          const error = getTRPCErrorFromUnknown(cause);\n          opts.onError?.({ error, path, type, ctx, req, input });\n          respond({\n            id,\n            jsonrpc,","after":"              type: 'stopped',\n            },\n          });\n          clientSubscriptions.delete(id);\n        }).catch((cause) => {\n          clientSubscriptions.delete(id);\n          const error = getTRPCErrorFromUnknown(cause);\n          opts.onError?.({ error, path, type, ctx, req, input });\n          respond({\n            id,\n            jsonrpc,","diff":"@@ -404,10 +404,11 @@ export function getWSConnectionHandler<TRouter extends AnyRouter>(\n               type: 'stopped',\n             },\n           });\n           clientSubscriptions.delete(id);\n         }).catch((cause) => {\n+          clientSubscriptions.delete(id);\n           const error = getTRPCErrorFromUnknown(cause);\n           opts.onError?.({ error, path, type, ctx, req, input });\n           respond({\n             id,\n             jsonrpc,","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix(server): delete stale clientSubscriptions entry on subscription task rejection (#7401)","issue_url":"https://github.com/trpc/trpc/issues/7400","pr_url":"https://github.com/trpc/trpc/pull/7401"},"needs_manual_review":true}
{"id":"trpc-ef684c8-1","repo":"trpc/trpc","license":"MIT","commit":"ef684c82910c4c18342088fbe64d222cb25721ed","parent":"d900ce181de48623d8b50cbcda5c800963eb7508","file":"packages/client/src/links/wsLink/wsClient/wsClient.ts","language":"typescript","hunk_header":"@@ -220,21 +220,28 @@ export class WsClient {","before":"            });\n          },\n        },\n      );\n\n      return () => {\n        abort();\n\n        if (type === 'subscription' && this.activeConnection.isOpen()) {\n          this.send({\n            id,\n            method: 'subscription.stop',\n          });\n        }\n\n        signal?.removeEventListener('abort', abort);\n      };\n    });\n  }\n\n  public get connection() {","after":"            });\n          },\n        },\n      );\n\n      const onAbort = () => observer.complete();\n      if (signal?.aborted) {\n        onAbort();\n      } else {\n        signal?.addEventListener('abort', onAbort, { once: true });\n      }\n\n      return () => {\n        abort();\n\n        if (type === 'subscription' && this.activeConnection.isOpen()) {\n          this.send({\n            id,\n            method: 'subscription.stop',\n          });\n        }\n\n        signal?.removeEventListener('abort', onAbort);\n      };\n    });\n  }\n\n  public get connection() {","diff":"@@ -220,21 +220,28 @@ export class WsClient {\n             });\n           },\n         },\n       );\n \n+      const onAbort = () => observer.complete();\n+      if (signal?.aborted) {\n+        onAbort();\n+      } else {\n+        signal?.addEventListener('abort', onAbort, { once: true });\n+      }\n+\n       return () => {\n         abort();\n \n         if (type === 'subscription' && this.activeConnection.isOpen()) {\n           this.send({\n             id,\n             method: 'subscription.stop',\n           });\n         }\n \n-        signal?.removeEventListener('abort', abort);\n+        signal?.removeEventListener('abort', onAbort);\n       };\n     });\n   }\n \n   public get connection() {","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix(client): wire the operation AbortSignal in the WebSocket link (#7434)","issue_url":"https://github.com/trpc/trpc/issues/7431","pr_url":"https://github.com/trpc/trpc/pull/7434"},"needs_manual_review":true}
{"id":"trpc-e35b998-1","repo":"trpc/trpc","license":"MIT","commit":"e35b9989bd474eec209fa2f9b36857a46c836940","parent":"2c61222290371fbc7f8645aa552c5b78b18afbe3","file":"packages/tanstack-react-query/src/internals/createOptionsProxy.ts","language":"typescript","hunk_header":"@@ -483,10 +483,11 @@ export function createTRPCOptionsProxy<","before":"\n      subscriptionOptions: () => {\n        return trpcSubscriptionOptions({\n          opts: arg2,\n          path,\n          queryKey: getQueryKeyInternal({\n            path,\n            input: arg1,\n            type: 'any',\n            prefix,","after":"\n      subscriptionOptions: () => {\n        return trpcSubscriptionOptions({\n          opts: arg2,\n          path,\n          input: arg1,\n          queryKey: getQueryKeyInternal({\n            path,\n            input: arg1,\n            type: 'any',\n            prefix,","diff":"@@ -483,10 +483,11 @@ export function createTRPCOptionsProxy<\n \n       subscriptionOptions: () => {\n         return trpcSubscriptionOptions({\n           opts: arg2,\n           path,\n+          input: arg1,\n           queryKey: getQueryKeyInternal({\n             path,\n             input: arg1,\n             type: 'any',\n             prefix,","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix(tanstack-react-query): subscriptionOptions(skipToken).enabled incorrectly returns true (#7464)","issue_url":"https://github.com/trpc/trpc/issues/7373","pr_url":"https://github.com/trpc/trpc/pull/7464"},"needs_manual_review":true}
{"id":"trpc-2c61222-1","repo":"trpc/trpc","license":"MIT","commit":"2c61222290371fbc7f8645aa552c5b78b18afbe3","parent":"3d21d2c1364139938f1d7924802af67b506ca544","file":"www/docusaurus.config.ts","language":"typescript","hunk_header":"@@ -396,12 +396,24 @@ export default {","before":"                  return false;\n                }\n                return true;\n              })\n              .map((docItem) => {\n                let link = `${versionSpec.routePath}/${docItem.id}`;\n                link = link.replace(new RegExp('([^:])//', 'g'), '$1/');\n                return `- [${docItem.title}](${link}): ${docItem.description ?? 'No description available'}`;\n              });\n\n            if (tocRecords.length > 0) {\n              const tocOutputFilename = `llms${versionSpec.fileSuffix}.txt`;","after":"                  return false;\n                }\n                return true;\n              })\n              .map((docItem) => {\n                // Prefer the doc's own permalink (which honours custom `slug`\n                // frontmatter) over the file-path-based id. Fall back to\n                // constructing from the id only when permalink is unavailable.\n                const docPath: string =\n                  (docItem.permalink as string | undefined) ??\n                  `${versionSpec.routePath}/${docItem.id}`;\n                // Make the URL absolute so llms.txt consumers don't need to\n                // know the site's base URL.\n                const link = docPath.startsWith('http')\n                  ? docPath\n                  : `${siteConfig.url}${docPath}`.replace(\n                      new RegExp('([^:])//', 'g'),\n                      '$1/',\n                    );\n                return `- [${docItem.title}](${link}): ${docItem.description ?? 'No description available'}`;\n              });\n\n            if (tocRecords.length > 0) {\n              const tocOutputFilename = `llms${versionSpec.fileSuffix}.txt`;","diff":"@@ -396,12 +396,24 @@ export default {\n                   return false;\n                 }\n                 return true;\n               })\n               .map((docItem) => {\n-                let link = `${versionSpec.routePath}/${docItem.id}`;\n-                link = link.replace(new RegExp('([^:])//', 'g'), '$1/');\n+                // Prefer the doc's own permalink (which honours custom `slug`\n+                // frontmatter) over the file-path-based id. Fall back to\n+                // constructing from the id only when permalink is unavailable.\n+                const docPath: string =\n+                  (docItem.permalink as string | undefined) ??\n+                  `${versionSpec.routePath}/${docItem.id}`;\n+                // Make the URL absolute so llms.txt consumers don't need to\n+                // know the site's base URL.\n+                const link = docPath.startsWith('http')\n+                  ? docPath\n+                  : `${siteConfig.url}${docPath}`.replace(\n+                      new RegExp('([^:])//', 'g'),\n+                      '$1/',\n+                    );\n                 return `- [${docItem.title}](${link}): ${docItem.description ?? 'No description available'}`;\n               });\n \n             if (tocRecords.length > 0) {\n               const tocOutputFilename = `llms${versionSpec.fileSuffix}.txt`;","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix(docs): fix 19 dead links in llms.txt — use permalink, absolute URLs, and H1 title (#7465)","issue_url":"https://github.com/trpc/trpc/issues/7462","pr_url":"https://github.com/trpc/trpc/pull/7465"},"needs_manual_review":true}
{"id":"trpc-3d21d2c-1","repo":"trpc/trpc","license":"MIT","commit":"3d21d2c1364139938f1d7924802af67b506ca544","parent":"19a0f58c648effba0cb6a8d4c49c43c035576ea5","file":"packages/server/src/unstable-core-do-not-import/rpc/parseTRPCMessage.ts","language":"typescript","hunk_header":"@@ -21,13 +21,12 @@ function assertIsProcedureType(obj: unknown): asserts obj is ProcedureType {","before":"function assertIsRequestId(\n  obj: unknown,\n): asserts obj is number | string | null {\n  if (\n    obj !== null &&\n    typeof obj === 'number' &&\n    isNaN(obj) &&\n    typeof obj !== 'string'\n  ) {\n    throw new Error('Invalid request id');\n  }\n}\n","after":"function assertIsRequestId(\n  obj: unknown,\n): asserts obj is number | string | null {\n  if (\n    obj !== null &&\n    typeof obj !== 'string' &&\n    (typeof obj !== 'number' || isNaN(obj))\n  ) {\n    throw new Error('Invalid request id');\n  }\n}\n","diff":"@@ -21,13 +21,12 @@ function assertIsProcedureType(obj: unknown): asserts obj is ProcedureType {\n function assertIsRequestId(\n   obj: unknown,\n ): asserts obj is number | string | null {\n   if (\n     obj !== null &&\n-    typeof obj === 'number' &&\n-    isNaN(obj) &&\n-    typeof obj !== 'string'\n+    typeof obj !== 'string' &&\n+    (typeof obj !== 'number' || isNaN(obj))\n   ) {\n     throw new Error('Invalid request id');\n   }\n }\n ","label":{"defect":true,"category":"bugfix","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix(server): correct assertIsRequestId boolean logic — non-NaN invalid types passed silently (#7477)","issue_url":"https://github.com/trpc/trpc/issues/7430","pr_url":"https://github.com/trpc/trpc/pull/7477"},"needs_manual_review":true}
{"id":"trpc-4f53152-1","repo":"trpc/trpc","license":"MIT","commit":"4f5315219f947589c9ef0f67b7aa551243312d61","parent":"6a70335e02fa1a8bc68e8d065b85687b0d7ffdea","file":"packages/server/src/@trpc/server/index.ts","language":"typescript","hunk_header":"@@ -77,10 +77,14 @@ export {","before":"\n  /**\n   * @internal\n   */\n  type UnsetMarker as TRPCUnsetMarker,\n} from '../../unstable-core-do-not-import';\n\nexport type {\n  /**\n   * @deprecated use `AnyTRPCProcedure` instead","after":"\n  /**\n   * @internal\n   */\n  type UnsetMarker as TRPCUnsetMarker,\n  /**\n   * @internal\n   */\n  type Unwrap as TRPCUnwrap,\n} from '../../unstable-core-do-not-import';\n\nexport type {\n  /**\n   * @deprecated use `AnyTRPCProcedure` instead","diff":"@@ -77,10 +77,14 @@ export {\n \n   /**\n    * @internal\n    */\n   type UnsetMarker as TRPCUnsetMarker,\n+  /**\n+   * @internal\n+   */\n+  type Unwrap as TRPCUnwrap,\n } from '../../unstable-core-do-not-import';\n \n export type {\n   /**\n    * @deprecated use `AnyTRPCProcedure` instead","label":{"defect":true,"category":"bugfix","touches_public_api":true,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"fix(server): keep middleware factory declarations portable (#7557)","issue_url":"https://github.com/trpc/trpc/issues/6889","pr_url":"https://github.com/trpc/trpc/pull/7557"},"needs_manual_review":true}
{"id":"trpc-23bd47e-1","repo":"trpc/trpc","license":"MIT","commit":"23bd47e8fc611da7b9cc3677ab620f3e72fe844d","parent":"bc67406b1a12d1ffe38e3eea5a9136ec00100e31","file":"packages/tests/server/outputParser.test.ts","language":"typescript","hunk_header":"@@ -1,9 +1,10 @@","before":"import { testServerAndClientResource } from '@trpc/client/__tests__/testClientResource';\nimport { initTRPC } from '@trpc/server';\nimport myzod from 'myzod';\nimport * as t from 'superstruct';\nimport * as v from 'valibot';\nimport * as yup from 'yup';\nimport { z } from 'zod';\n\ntest('zod', async () => {","after":"import { testServerAndClientResource } from '@trpc/client/__tests__/testClientResource';\nimport { initTRPC } from '@trpc/server';\nimport myzod from 'myzod';\nimport * as t from 'superstruct';\nimport * as S from 'sury';\nimport * as v from 'valibot';\nimport * as yup from 'yup';\nimport { z } from 'zod';\n\ntest('zod', async () => {","diff":"@@ -1,9 +1,10 @@\n import { testServerAndClientResource } from '@trpc/client/__tests__/testClientResource';\n import { initTRPC } from '@trpc/server';\n import myzod from 'myzod';\n import * as t from 'superstruct';\n+import * as S from 'sury';\n import * as v from 'valibot';\n import * as yup from 'yup';\n import { z } from 'zod';\n \n test('zod', async () => {","label":{"defect":false,"category":"docs","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"docs(www): add Sury to validator library integrations (#7466)","issue_url":null,"pr_url":"https://github.com/trpc/trpc/pull/7466"},"needs_manual_review":true}
{"id":"trpc-3e0e979-1","repo":"trpc/trpc","license":"MIT","commit":"3e0e9793eb7f8c4cfbe70a1dccb72f8d355e3c8b","parent":"f8f0c0e3979ecb980d38e9369fa8c98df95d212b","file":"packages/openapi/src/cli.ts","language":"typescript","hunk_header":"@@ -10,10 +10,11 @@","before":" * Options:\n *   --export, -e   Name of the exported router symbol  (default: AppRouter)\n *   --output, -o   Output file path                    (default: openapi.json)\n *   --title        OpenAPI info.title                  (default: tRPC API)\n *   --version      OpenAPI info.version                (default: 0.0.0)\n *   --help, -h     Show this help message\n */\nimport * as fs from 'node:fs';\nimport * as path from 'node:path';\nimport { parseArgs as nodeParseArgs } from 'node:util';","after":" * Options:\n *   --export, -e   Name of the exported router symbol  (default: AppRouter)\n *   --output, -o   Output file path                    (default: openapi.json)\n *   --title        OpenAPI info.title                  (default: tRPC API)\n *   --version      OpenAPI info.version                (default: 0.0.0)\n *   --server-url   OpenAPI servers[].url (include any tRPC prefix)\n *   --help, -h     Show this help message\n */\nimport * as fs from 'node:fs';\nimport * as path from 'node:path';\nimport { parseArgs as nodeParseArgs } from 'node:util';","diff":"@@ -10,10 +10,11 @@\n  * Options:\n  *   --export, -e   Name of the exported router symbol  (default: AppRouter)\n  *   --output, -o   Output file path                    (default: openapi.json)\n  *   --title        OpenAPI info.title                  (default: tRPC API)\n  *   --version      OpenAPI info.version                (default: 0.0.0)\n+ *   --server-url   OpenAPI servers[].url (include any tRPC prefix)\n  *   --help, -h     Show this help message\n  */\n import * as fs from 'node:fs';\n import * as path from 'node:path';\n import { parseArgs as nodeParseArgs } from 'node:util';","label":{"defect":false,"category":"feature","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"feat(openapi): Add server url support (#7411)","issue_url":null,"pr_url":"https://github.com/trpc/trpc/pull/7411"},"needs_manual_review":true}
{"id":"trpc-f8f0c0e-1","repo":"trpc/trpc","license":"MIT","commit":"f8f0c0e3979ecb980d38e9369fa8c98df95d212b","parent":"c7360d4eb3c89c336468809a293e5cda4b302d4b","file":"packages/server/src/unstable-core-do-not-import.ts","language":"typescript","hunk_header":"@@ -1,11 +1,11 @@","before":"/**\n * **DO NOT IMPORT FROM THIS FILE**\n *\n * This file is here to:\n * - make TypeScript happy and prevent _\"The inferred type of 'createContext' cannot be named without a reference to [...]\"_.\n * - the the glue between the official `@trpc/*`-packages\n *\n *\n * If you seem to need to import anything from here, please open an issue at https://github.com/trpc/trpc/issues\n */\nexport * from './unstable-core-do-not-import/clientish/inference';","after":"/**\n * **DO NOT IMPORT FROM THIS FILE**\n *\n * This file is here to:\n * - make TypeScript happy and prevent _\"The inferred type of 'createContext' cannot be named without a reference to [...]\"_.\n * - the glue between the official `@trpc/*`-packages\n *\n *\n * If you seem to need to import anything from here, please open an issue at https://github.com/trpc/trpc/issues\n */\nexport * from './unstable-core-do-not-import/clientish/inference';","diff":"@@ -1,11 +1,11 @@\n /**\n  * **DO NOT IMPORT FROM THIS FILE**\n  *\n  * This file is here to:\n  * - make TypeScript happy and prevent _\"The inferred type of 'createContext' cannot be named without a reference to [...]\"_.\n- * - the the glue between the official `@trpc/*`-packages\n+ * - the glue between the official `@trpc/*`-packages\n  *\n  *\n  * If you seem to need to import anything from here, please open an issue at https://github.com/trpc/trpc/issues\n  */\n export * from './unstable-core-do-not-import/clientish/inference';","label":{"defect":false,"category":"config","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"chore: fix duplicate \"the\" in unstable-core JSDoc comment (#7366)","issue_url":null,"pr_url":"https://github.com/trpc/trpc/pull/7366"},"needs_manual_review":true}
{"id":"trpc-60c89fc-1","repo":"trpc/trpc","license":"MIT","commit":"60c89fc316524904e03394b606c184b8e3abed03","parent":"37917335d97f4799dc9924ca78b16e299388bdd0","file":"packages/openapi/src/cli.ts","language":"typescript","hunk_header":"@@ -124,10 +124,11 @@ async function main(): Promise<void> {","before":"    console.error(`Error: ${message}`);\n    process.exit(1);\n  }\n\n  const outputPath = path.resolve(args.output);\n  fs.writeFileSync(outputPath, JSON.stringify(doc, null, 2) + '\\n', 'utf8');\n  console.log(`OpenAPI document written to: ${outputPath}`);\n}\n\nvoid main();","after":"    console.error(`Error: ${message}`);\n    process.exit(1);\n  }\n\n  const outputPath = path.resolve(args.output);\n  fs.mkdirSync(path.dirname(outputPath), { recursive: true });\n  fs.writeFileSync(outputPath, JSON.stringify(doc, null, 2) + '\\n', 'utf8');\n  console.log(`OpenAPI document written to: ${outputPath}`);\n}\n\nvoid main();","diff":"@@ -124,10 +124,11 @@ async function main(): Promise<void> {\n     console.error(`Error: ${message}`);\n     process.exit(1);\n   }\n \n   const outputPath = path.resolve(args.output);\n+  fs.mkdirSync(path.dirname(outputPath), { recursive: true });\n   fs.writeFileSync(outputPath, JSON.stringify(doc, null, 2) + '\\n', 'utf8');\n   console.log(`OpenAPI document written to: ${outputPath}`);\n }\n \n void main();","label":{"defect":false,"category":"deps","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"chore: upgrade pnpm to 10.33.1 (#7350)","issue_url":null,"pr_url":"https://github.com/trpc/trpc/pull/7350"},"needs_manual_review":true}
{"id":"trpc-3791733-1","repo":"trpc/trpc","license":"MIT","commit":"37917335d97f4799dc9924ca78b16e299388bdd0","parent":"63407c577124e7a2890a7599484d52a0cf025536","file":"www/src/components/sponsors/script.output.ts","language":"typescript","hunk_header":"@@ -3,43 +3,43 @@","before":"// prettier-ignore\nexport const topSponsors = [\n  {\n    \"name\": \"Greptile\",\n    \"imgSrc\": \"https://avatars.githubusercontent.com/u/140149887?v=4\",\n    \"weight\": 101,\n    \"login\": \"greptileai\",\n    \"link\": \"https://greptile.com/?ref=trpc&utm_source=github&utm_medium=referral&utm_campaign=trpc\",\n    \"createdAt\": 1743099912000\n  },\n  {\n    \"name\": \"CodeRabbit\",\n    \"imgSrc\": \"https://avatars.githubusercontent.com/u/132028505?v=4\",\n    \"weight\": 99,\n    \"login\": \"coderabbitai\",\n    \"link\": \"https://www.coderabbit.ai/?utm_source=github&utm_medium=referral&ref=trpc&utm_campaign=trpc\",\n    \"createdAt\": 1737645299000\n  },\n  {\n    \"name\": \"Ahoy Labs\",\n    \"imgSrc\": \"https://avatars.githubusercontent.com/u/95662801?v=4\",\n    \"weight\": 95,\n    \"login\": \"ahoylabs\",\n    \"link\": \"https://backyard.ai/?ref=trpc&utm_source=github&utm_medium=referral&utm_campaign=trpc\",\n    \"createdAt\": 1671157456000\n  },\n  {\n    \"name\": \"SerpApi\",\n    \"imgSrc\": \"https://github.com/serpapi.png\",\n    \"weight\": 88,\n    \"login\": \"SerpApi\",\n    \"link\": \"https://serpapi.com/?ref=trpc&utm_source=github&utm_medium=referral&utm_campaign=trpc\",\n    \"createdAt\": 1770768000000\n  },\n  {\n    \"name\": \"Unkey\",\n    \"imgSrc\": \"https://avatars.githubusercontent.com/u/138932600?v=4\",\n    \"weight\": 44,\n    \"login\": \"unkeyed\",\n    \"link\": \"https://unkey.com/?ref=trpc&utm_source=github&utm_medium=referral&utm_campaign=trpc\",\n    \"createdAt\": 1708012798000\n  }\n] as const;","after":"// prettier-ignore\nexport const topSponsors = [\n  {\n    \"name\": \"Greptile\",\n    \"imgSrc\": \"https://avatars.githubusercontent.com/u/140149887?v=4\",\n    \"weight\": 102,\n    \"login\": \"greptileai\",\n    \"link\": \"https://greptile.com/?ref=trpc&utm_source=github&utm_medium=referral&utm_campaign=trpc\",\n    \"createdAt\": 1743099912000\n  },\n  {\n    \"name\": \"CodeRabbit\",\n    \"imgSrc\": \"https://avatars.githubusercontent.com/u/132028505?v=4\",\n    \"weight\": 94,\n    \"login\": \"coderabbitai\",\n    \"link\": \"https://www.coderabbit.ai/?utm_source=github&utm_medium=referral&ref=trpc&utm_campaign=trpc\",\n    \"createdAt\": 1737645299000\n  },\n  {\n    \"name\": \"Ahoy Labs\",\n    \"imgSrc\": \"https://avatars.githubusercontent.com/u/95662801?v=4\",\n    \"weight\": 84,\n    \"login\": \"ahoylabs\",\n    \"link\": \"https://backyard.ai/?ref=trpc&utm_source=github&utm_medium=referral&utm_campaign=trpc\",\n    \"createdAt\": 1671157456000\n  },\n  {\n    \"name\": \"SerpApi\",\n    \"imgSrc\": \"https://github.com/serpapi.png\",\n    \"weight\": 78,\n    \"login\": \"SerpApi\",\n    \"link\": \"https://serpapi.com/?ref=trpc&utm_source=github&utm_medium=referral&utm_campaign=trpc\",\n    \"createdAt\": 1770768000000\n  },\n  {\n    \"name\": \"Unkey\",\n    \"imgSrc\": \"https://avatars.githubusercontent.com/u/138932600?v=4\",\n    \"weight\": 40,\n    \"login\": \"unkeyed\",\n    \"link\": \"https://unkey.com/?ref=trpc&utm_source=github&utm_medium=referral&utm_campaign=trpc\",\n    \"createdAt\": 1708012798000\n  }\n] as const;","diff":"@@ -3,43 +3,43 @@\n // prettier-ignore\n export const topSponsors = [\n   {\n     \"name\": \"Greptile\",\n     \"imgSrc\": \"https://avatars.githubusercontent.com/u/140149887?v=4\",\n-    \"weight\": 101,\n+    \"weight\": 102,\n     \"login\": \"greptileai\",\n     \"link\": \"https://greptile.com/?ref=trpc&utm_source=github&utm_medium=referral&utm_campaign=trpc\",\n     \"createdAt\": 1743099912000\n   },\n   {\n     \"name\": \"CodeRabbit\",\n     \"imgSrc\": \"https://avatars.githubusercontent.com/u/132028505?v=4\",\n-    \"weight\": 99,\n+    \"weight\": 94,\n     \"login\": \"coderabbitai\",\n     \"link\": \"https://www.coderabbit.ai/?utm_source=github&utm_medium=referral&ref=trpc&utm_campaign=trpc\",\n     \"createdAt\": 1737645299000\n   },\n   {\n     \"name\": \"Ahoy Labs\",\n     \"imgSrc\": \"https://avatars.githubusercontent.com/u/95662801?v=4\",\n-    \"weight\": 95,\n+    \"weight\": 84,\n     \"login\": \"ahoylabs\",\n     \"link\": \"https://backyard.ai/?ref=trpc&utm_source=github&utm_medium=referral&utm_campaign=trpc\",\n     \"createdAt\": 1671157456000\n   },\n   {\n     \"name\": \"SerpApi\",\n     \"imgSrc\": \"https://github.com/serpapi.png\",\n-    \"weight\": 88,\n+    \"weight\": 78,\n     \"login\": \"SerpApi\",\n     \"link\": \"https://serpapi.com/?ref=trpc&utm_source=github&utm_medium=referral&utm_campaign=trpc\",\n     \"createdAt\": 1770768000000\n   },\n   {\n     \"name\": \"Unkey\",\n     \"imgSrc\": \"https://avatars.githubusercontent.com/u/138932600?v=4\",\n-    \"weight\": 44,\n+    \"weight\": 40,\n     \"login\": \"unkeyed\",\n     \"link\": \"https://unkey.com/?ref=trpc&utm_source=github&utm_medium=referral&utm_campaign=trpc\",\n     \"createdAt\": 1708012798000\n   }\n ] as const;","label":{"defect":false,"category":"docs","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"docs: sponsors sync","issue_url":null,"pr_url":null},"needs_manual_review":true}
{"id":"trpc-c188dab-1","repo":"trpc/trpc","license":"MIT","commit":"c188dab0822caf3615199e4ac95147bc7560d26f","parent":"cb17317f15654ee7bce1d34f737fee64a8babb43","file":"scripts/version.ts","language":"typescript","hunk_header":"@@ -1,10 +1,14 @@","before":"import fs from 'fs';\nimport path from 'path';\n\nconsole.log('ℹ️ Running custom script to pin versions to each other');\n\n// Packages that should have a prerelease suffix (alpha or beta)\nconst PRERELEASE_PACKAGES = new Map<string, 'alpha' | 'beta'>([\n  ['openapi', 'alpha'],\n]);\n","after":"import fs from 'fs';\nimport path from 'path';\n\nconsole.log('ℹ️ Running custom script to pin versions to each other');\n\nconst repoVersion = JSON.parse(\n  fs.readFileSync(path.join(import.meta.dirname, '..', 'lerna.json'), 'utf8'),\n).version as string;\n\n// Packages that should have a prerelease suffix (alpha or beta)\nconst PRERELEASE_PACKAGES = new Map<string, 'alpha' | 'beta'>([\n  ['openapi', 'alpha'],\n]);\n","diff":"@@ -1,10 +1,14 @@\n import fs from 'fs';\n import path from 'path';\n \n console.log('ℹ️ Running custom script to pin versions to each other');\n \n+const repoVersion = JSON.parse(\n+  fs.readFileSync(path.join(import.meta.dirname, '..', 'lerna.json'), 'utf8'),\n+).version as string;\n+\n // Packages that should have a prerelease suffix (alpha or beta)\n const PRERELEASE_PACKAGES = new Map<string, 'alpha' | 'beta'>([\n   ['openapi', 'alpha'],\n ]);\n ","label":{"defect":false,"category":"config","touches_public_api":null,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"chore(monorepo): remove versions from private workspaces (#7306)","issue_url":null,"pr_url":"https://github.com/trpc/trpc/pull/7306"},"needs_manual_review":true}
{"id":"trpc-d3c4051-1","repo":"trpc/trpc","license":"MIT","commit":"d3c4051f03d0ca1f31177cadc10adff21c9048b8","parent":"d2ea28e14fe0afde553cc5fce177ed7642943731","file":"packages/server/src/@trpc/server/index.ts","language":"typescript","hunk_header":"@@ -11,10 +11,12 @@ export {","before":"  transformTRPCResponse,\n  createFlatProxy as createTRPCFlatProxy,\n  createRecursiveProxy as createTRPCRecursiveProxy,\n  type inferProcedureInput,\n  type inferProcedureOutput,\n  type inferProcedureBuilderResolverOptions,\n  type inferRouterError,\n  type inferRouterInputs,\n  type inferRouterOutputs,\n  type inferRouterContext,","after":"  transformTRPCResponse,\n  createFlatProxy as createTRPCFlatProxy,\n  createRecursiveProxy as createTRPCRecursiveProxy,\n  type inferProcedureInput,\n  type inferProcedureOutput,\n  type inferSubscriptionInput,\n  type inferSubscriptionOutput,\n  type inferProcedureBuilderResolverOptions,\n  type inferRouterError,\n  type inferRouterInputs,\n  type inferRouterOutputs,\n  type inferRouterContext,","diff":"@@ -11,10 +11,12 @@ export {\n   transformTRPCResponse,\n   createFlatProxy as createTRPCFlatProxy,\n   createRecursiveProxy as createTRPCRecursiveProxy,\n   type inferProcedureInput,\n   type inferProcedureOutput,\n+  type inferSubscriptionInput,\n+  type inferSubscriptionOutput,\n   type inferProcedureBuilderResolverOptions,\n   type inferRouterError,\n   type inferRouterInputs,\n   type inferRouterOutputs,\n   type inferRouterContext,","label":{"defect":false,"category":"feature","touches_public_api":true,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"feat: Add subscription inferrence helpers (#7303)","issue_url":null,"pr_url":"https://github.com/trpc/trpc/pull/7303"},"needs_manual_review":true}
{"id":"trpc-fb9fe87-1","repo":"trpc/trpc","license":"MIT","commit":"fb9fe87fd4885f3e2b3f16fe484b1467b77d5324","parent":"492bacf3b9ca28d16cfe5ec32e4eb4d164b7f8f0","file":"packages/openapi/src/heyapi/index.ts","language":"typescript","hunk_header":"@@ -1,45 +1,38 @@","before":"import type {\n  FetchClient as HeyApiFetchClient,\n  UserConfig,\n} from '@hey-api/openapi-ts';\nimport type {\n  TRPCCombinedDataTransformer,\n  TRPCDataTransformer,\n} from '@trpc/server';\n\nexport type DataTransformerOptions =\n  | TRPCDataTransformer\n  | TRPCCombinedDataTransformer;\n\ntype HeyAPIResolvers = Exclude<\n  Extract<\n    Exclude<UserConfig['plugins'], undefined | string>[number],\n    { name: '@hey-api/typescript' }\n  >['~resolvers'],\n  undefined\n>;\n\nfunction resolveTransformer(\n  transformer: DataTransformerOptions,\n): TRPCCombinedDataTransformer {\n  if ('input' in transformer) {\n    return transformer;\n  }\n  return { input: transformer, output: transformer };\n}\n\nexport interface TRPCHeyApiClientOptions {\n  transformer?: DataTransformerOptions;\n}\n\nexport type HeyApiConfig = ReturnType<HeyApiFetchClient['getConfig']>;\nexport type TRPCHeyApiClientConfig = Required<\n  Pick<HeyApiConfig, 'querySerializer'>\n> &\n  Pick<HeyApiConfig, 'bodySerializer' | 'responseTransformer'>;\n\n/**\n * Returns the `~resolvers` object for the `@hey-api/typescript` plugin.\n *\n * Maps `date` and `date-time` string formats to `Date` so that the\n * generated SDK uses `Date` instead of `string` for those fields.","after":"import type { Plugins } from '@hey-api/openapi-ts';\nimport type {\n  TRPCCombinedDataTransformer,\n  TRPCDataTransformer,\n} from '@trpc/server';\n\nexport type DataTransformerOptions =\n  | TRPCDataTransformer\n  | TRPCCombinedDataTransformer;\nexport interface TRPCHeyApiClientOptions {\n  transformer?: DataTransformerOptions;\n}\n\nexport type HeyAPIResolvers = Plugins.HeyApiTypeScript.Resolvers;\n\nexport type HeyApiConfig = ReturnType<\n  Plugins.HeyApiClientFetch.Client['getConfig']\n>;\n\nexport type TRPCHeyApiClientConfig = Required<\n  Pick<HeyApiConfig, 'querySerializer'>\n> &\n  Pick<HeyApiConfig, 'bodySerializer' | 'responseTransformer'>;\n\nfunction resolveTransformer(\n  transformer: DataTransformerOptions,\n): TRPCCombinedDataTransformer {\n  if ('input' in transformer) {\n    return transformer;\n  }\n  return { input: transformer, output: transformer };\n}\n\n/**\n * Returns the `~resolvers` object for the `@hey-api/typescript` plugin.\n *\n * Maps `date` and `date-time` string formats to `Date` so that the\n * generated SDK uses `Date` instead of `string` for those fields.","diff":"@@ -1,45 +1,38 @@\n-import type {\n-  FetchClient as HeyApiFetchClient,\n-  UserConfig,\n-} from '@hey-api/openapi-ts';\n+import type { Plugins } from '@hey-api/openapi-ts';\n import type {\n   TRPCCombinedDataTransformer,\n   TRPCDataTransformer,\n } from '@trpc/server';\n \n export type DataTransformerOptions =\n   | TRPCDataTransformer\n   | TRPCCombinedDataTransformer;\n+export interface TRPCHeyApiClientOptions {\n+  transformer?: DataTransformerOptions;\n+}\n+\n+export type HeyAPIResolvers = Plugins.HeyApiTypeScript.Resolvers;\n \n-type HeyAPIResolvers = Exclude<\n-  Extract<\n-    Exclude<UserConfig['plugins'], undefined | string>[number],\n-    { name: '@hey-api/typescript' }\n-  >['~resolvers'],\n-  undefined\n+export type HeyApiConfig = ReturnType<\n+  Plugins.HeyApiClientFetch.Client['getConfig']\n >;\n \n+export type TRPCHeyApiClientConfig = Required<\n+  Pick<HeyApiConfig, 'querySerializer'>\n+> &\n+  Pick<HeyApiConfig, 'bodySerializer' | 'responseTransformer'>;\n+\n function resolveTransformer(\n   transformer: DataTransformerOptions,\n ): TRPCCombinedDataTransformer {\n   if ('input' in transformer) {\n     return transformer;\n   }\n   return { input: transformer, output: transformer };\n }\n \n-export interface TRPCHeyApiClientOptions {\n-  transformer?: DataTransformerOptions;\n-}\n-\n-export type HeyApiConfig = ReturnType<HeyApiFetchClient['getConfig']>;\n-export type TRPCHeyApiClientConfig = Required<\n-  Pick<HeyApiConfig, 'querySerializer'>\n-> &\n-  Pick<HeyApiConfig, 'bodySerializer' | 'responseTransformer'>;\n-\n /**\n  * Returns the `~resolvers` object for the `@hey-api/typescript` plugin.\n  *\n  * Maps `date` and `date-time` string formats to `Date` so that the\n  * generated SDK uses `Date` instead of `string` for those fields.","label":{"defect":false,"category":"config","touches_public_api":true,"touches_security":null,"source":"commit-heuristic"},"evidence":{"commit_message":"chore: Update Hey API for new type imports (#7298)","issue_url":null,"pr_url":"https://github.com/trpc/trpc/pull/7298"},"needs_manual_review":true}
